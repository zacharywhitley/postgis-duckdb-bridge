// === GENERATED by ducklink-shim-codegen — do not edit by hand ===
#![allow(dead_code, unused_imports)]

//! Scalar-function registration.
//!
//! Phase 1 (2026-06-24): vectorised scalar dispatch wired up;
//! ST_GeomFromText is fully functional. Other scalars are
//! listed below as comments.
//!
//! Dispatch shape (DuckDB is VECTORISED — every invoke is
//! handed a chunk of N rows; SQLite was 1-at-a-time):
//!
//!   For each row in input chunk:
//!     - read varchar / blob / etc. into a FunctionValue
//!     - call shim's ScalarFunctionDef::execute(&[…])
//!     - write the result to the output FlatVector
//!
//! Phase 2 (2026-06-24) covers the top 7 PostGIS signature
//! shapes (about 70 % of the scalar surface):
//!
//!   text          → blob     ST_GeomFromText et al.       (~16)
//!   blob          → blob     ST_Centroid, ST_ConvexHull   (~71)
//!   blob,blob     → boolean  ST_Intersects, ST_Contains   (~33)
//!   blob          → f64      ST_Area, ST_Length, ST_X     (~32)
//!   blob,f64      → blob     ST_Buffer, ST_Simplify       (~23)
//!   blob          → text     ST_AsText, ST_AsGeoJSON      (~22)
//!   blob,blob     → blob     ST_Union, ST_Intersection    (~21)
//!
//! Each shape has its own VScalar marker struct and a
//! `register_<shape>` helper. The codegen detects each
//! scalar's shape and routes to the matching helper.

use std::sync::Arc;

use duckdb::{
    core::{DataChunkHandle, Inserter, LogicalTypeHandle, LogicalTypeId},
    types::DuckString,
    vscalar::{ScalarFunctionSignature, VScalar},
    vtab::arrow::WritableVector,
    Connection, Result,
};
use libduckdb_sys::duckdb_string_t;

use datafission_functions::traits::ScalarFunctionDef;
use datafission_functions::types::FunctionValue;

use crate::registry;

/// Register every scalar matching one of the Phase-2 signature
/// shapes against the given connection. Scalars whose shape is
/// not yet implemented are emitted as comments (with their
/// signature) so a future phase can wire them in.
pub fn register_all(conn: &Connection) -> Result<()> {
    if let Err(e) = register_blob_blob_to_blob(conn, "st_3dclosestpoint") {
        eprintln!("[shim-scalars] skipping `st_3dclosestpoint`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_3d_closest_point") {
        eprintln!("[shim-scalars] skipping `st_3d_closest_point`: {e}");
    } // alias of st_3dclosestpoint
    if let Err(e) = register_blob_blob_to_blob(conn, "st_closest_point_3d") {
        eprintln!("[shim-scalars] skipping `st_closest_point_3d`: {e}");
    } // alias of st_3dclosestpoint
    if let Err(e) = register_blob_blob_to_blob(conn, "st_closest_point_threed") {
        eprintln!("[shim-scalars] skipping `st_closest_point_threed`: {e}");
    } // alias of st_3dclosestpoint
    if let Err(e) = register_blob_blob_to_bool(conn, "st_3dcontains") {
        eprintln!("[shim-scalars] skipping `st_3dcontains`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_3d_contains") {
        eprintln!("[shim-scalars] skipping `st_3d_contains`: {e}");
    } // alias of st_3dcontains
    if let Err(e) = register_blob_blob_to_bool(conn, "st_contains_3d") {
        eprintln!("[shim-scalars] skipping `st_contains_3d`: {e}");
    } // alias of st_3dcontains
    if let Err(e) = register_blob_blob_to_bool(conn, "st_contains_threed") {
        eprintln!("[shim-scalars] skipping `st_contains_threed`: {e}");
    } // alias of st_3dcontains
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_3ddfullywithin") {
        eprintln!("[shim-scalars] skipping `st_3ddfullywithin`: {e}");
    }
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_3d_d_fully_within") {
        eprintln!("[shim-scalars] skipping `st_3d_d_fully_within`: {e}");
    } // alias of st_3ddfullywithin
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_3d_dfully_within") {
        eprintln!("[shim-scalars] skipping `st_3d_dfully_within`: {e}");
    } // alias of st_3ddfullywithin
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_3ddwithin") {
        eprintln!("[shim-scalars] skipping `st_3ddwithin`: {e}");
    }
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_3d_d_within") {
        eprintln!("[shim-scalars] skipping `st_3d_d_within`: {e}");
    } // alias of st_3ddwithin
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_3d_dwithin") {
        eprintln!("[shim-scalars] skipping `st_3d_dwithin`: {e}");
    } // alias of st_3ddwithin
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_dwithin_3d") {
        eprintln!("[shim-scalars] skipping `st_dwithin_3d`: {e}");
    } // alias of st_3ddwithin
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_dwithin_threed") {
        eprintln!("[shim-scalars] skipping `st_dwithin_threed`: {e}");
    } // alias of st_3ddwithin
    if let Err(e) = register_blob_blob_to_bool(conn, "st_3dintersects") {
        eprintln!("[shim-scalars] skipping `st_3dintersects`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_3d_intersects") {
        eprintln!("[shim-scalars] skipping `st_3d_intersects`: {e}");
    } // alias of st_3dintersects
    if let Err(e) = register_blob_blob_to_bool(conn, "st_intersects_3d") {
        eprintln!("[shim-scalars] skipping `st_intersects_3d`: {e}");
    } // alias of st_3dintersects
    if let Err(e) = register_blob_blob_to_bool(conn, "st_intersects_threed") {
        eprintln!("[shim-scalars] skipping `st_intersects_threed`: {e}");
    } // alias of st_3dintersects
    if let Err(e) = register_blob_f64_to_blob(conn, "st_3dlineinterpolatepoint") {
        eprintln!("[shim-scalars] skipping `st_3dlineinterpolatepoint`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_3d_line_interpolate_point") {
        eprintln!("[shim-scalars] skipping `st_3d_line_interpolate_point`: {e}");
    } // alias of st_3dlineinterpolatepoint
    if let Err(e) = register_blob_f64_to_blob(conn, "st_line_interpolate_point_3d") {
        eprintln!("[shim-scalars] skipping `st_line_interpolate_point_3d`: {e}");
    } // alias of st_3dlineinterpolatepoint
    if let Err(e) = register_blob_f64_to_blob(conn, "st_line_interpolate_point_threed") {
        eprintln!("[shim-scalars] skipping `st_line_interpolate_point_threed`: {e}");
    } // alias of st_3dlineinterpolatepoint
    if let Err(e) = register_blob_blob_to_blob(conn, "st_3dlongestline") {
        eprintln!("[shim-scalars] skipping `st_3dlongestline`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_3d_longest_line") {
        eprintln!("[shim-scalars] skipping `st_3d_longest_line`: {e}");
    } // alias of st_3dlongestline
    if let Err(e) = register_blob_blob_to_blob(conn, "st_longest_line_3d") {
        eprintln!("[shim-scalars] skipping `st_longest_line_3d`: {e}");
    } // alias of st_3dlongestline
    if let Err(e) = register_blob_blob_to_blob(conn, "st_longest_line_threed") {
        eprintln!("[shim-scalars] skipping `st_longest_line_threed`: {e}");
    } // alias of st_3dlongestline
    if let Err(e) = register_blob_blob_to_f64(conn, "st_3dmaxdistance") {
        eprintln!("[shim-scalars] skipping `st_3dmaxdistance`: {e}");
    }
    if let Err(e) = register_blob_blob_to_f64(conn, "st_3d_max_distance") {
        eprintln!("[shim-scalars] skipping `st_3d_max_distance`: {e}");
    } // alias of st_3dmaxdistance
    if let Err(e) = register_blob_blob_to_f64(conn, "st_max_distance_3d") {
        eprintln!("[shim-scalars] skipping `st_max_distance_3d`: {e}");
    } // alias of st_3dmaxdistance
    if let Err(e) = register_blob_blob_to_f64(conn, "st_max_distance_threed") {
        eprintln!("[shim-scalars] skipping `st_max_distance_threed`: {e}");
    } // alias of st_3dmaxdistance
    if let Err(e) = register_blob_blob_to_blob(conn, "st_3dshortestline") {
        eprintln!("[shim-scalars] skipping `st_3dshortestline`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_3d_shortest_line") {
        eprintln!("[shim-scalars] skipping `st_3d_shortest_line`: {e}");
    } // alias of st_3dshortestline
    if let Err(e) = register_blob_blob_to_blob(conn, "st_shortest_line_3d") {
        eprintln!("[shim-scalars] skipping `st_shortest_line_3d`: {e}");
    } // alias of st_3dshortestline
    if let Err(e) = register_blob_blob_to_blob(conn, "st_shortest_line_threed") {
        eprintln!("[shim-scalars] skipping `st_shortest_line_threed`: {e}");
    } // alias of st_3dshortestline
    if let Err(e) = register_blob_text_f64_to_blob(conn, "st_addband") {
        eprintln!("[shim-scalars] skipping `st_addband`: {e}");
    }
    if let Err(e) = register_blob_text_f64_to_blob(conn, "st_add_band") {
        eprintln!("[shim-scalars] skipping `st_add_band`: {e}");
    } // alias of st_addband
    if let Err(e) = register_blob_blob_to_blob(conn, "st_addface") {
        eprintln!("[shim-scalars] skipping `st_addface`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_add_face") {
        eprintln!("[shim-scalars] skipping `st_add_face`: {e}");
    } // alias of st_addface
    if let Err(e) = register_blob_blob_to_blob(conn, "topology_add_face") {
        eprintln!("[shim-scalars] skipping `topology_add_face`: {e}");
    } // alias of st_addface
    if let Err(e) = register_blob_blob_to_text(conn, "st_addface2") {
        eprintln!("[shim-scalars] skipping `st_addface2`: {e}");
    }
    if let Err(e) = register_blob_blob_to_text(conn, "st_add_face2") {
        eprintln!("[shim-scalars] skipping `st_add_face2`: {e}");
    } // alias of st_addface2
    if let Err(e) = register_blob_u32_u32_blob_to_blob(conn, "st_addisoedge") {
        eprintln!("[shim-scalars] skipping `st_addisoedge`: {e}");
    }
    if let Err(e) = register_blob_u32_u32_blob_to_blob(conn, "st_add_iso_edge") {
        eprintln!("[shim-scalars] skipping `st_add_iso_edge`: {e}");
    } // alias of st_addisoedge
    if let Err(e) = register_blob_u32_u32_blob_to_blob(conn, "topology_add_iso_edge") {
        eprintln!("[shim-scalars] skipping `topology_add_iso_edge`: {e}");
    } // alias of st_addisoedge
    if let Err(e) = register_blob_u32_u32_blob_to_text(conn, "st_addisoedge2") {
        eprintln!("[shim-scalars] skipping `st_addisoedge2`: {e}");
    }
    if let Err(e) = register_blob_u32_u32_blob_to_text(conn, "st_add_iso_edge2") {
        eprintln!("[shim-scalars] skipping `st_add_iso_edge2`: {e}");
    } // alias of st_addisoedge2
    if let Err(e) = register_blob_i32_blob_to_blob(conn, "st_addisonode") {
        eprintln!("[shim-scalars] skipping `st_addisonode`: {e}");
    }
    if let Err(e) = register_blob_i32_blob_to_blob(conn, "st_add_iso_node") {
        eprintln!("[shim-scalars] skipping `st_add_iso_node`: {e}");
    } // alias of st_addisonode
    if let Err(e) = register_blob_i32_blob_to_blob(conn, "topology_add_iso_node") {
        eprintln!("[shim-scalars] skipping `topology_add_iso_node`: {e}");
    } // alias of st_addisonode
    if let Err(e) = register_blob_i32_blob_to_text(conn, "st_addisonode2") {
        eprintln!("[shim-scalars] skipping `st_addisonode2`: {e}");
    }
    if let Err(e) = register_blob_i32_blob_to_text(conn, "st_add_iso_node2") {
        eprintln!("[shim-scalars] skipping `st_add_iso_node2`: {e}");
    } // alias of st_addisonode2
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_addmeasure") {
        eprintln!("[shim-scalars] skipping `st_addmeasure`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_add_measure") {
        eprintln!("[shim-scalars] skipping `st_add_measure`: {e}");
    } // alias of st_addmeasure
    if let Err(e) = register_blob_blob_to_blob(conn, "st_addpoint") {
        eprintln!("[shim-scalars] skipping `st_addpoint`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_add_point") {
        eprintln!("[shim-scalars] skipping `st_add_point`: {e}");
    } // alias of st_addpoint
    if let Err(e) = register_blob_f64_f64_f64_f64_f64_f64_to_blob(conn, "st_affine") {
        eprintln!("[shim-scalars] skipping `st_affine`: {e}");
    }
    if let Err(e) = register_blob_blob_blob_to_f64(conn, "st_angle") {
        eprintln!("[shim-scalars] skipping `st_angle`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_approximatemedialaxis") {
        eprintln!("[shim-scalars] skipping `st_approximatemedialaxis`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_approximate_medial_axis") {
        eprintln!("[shim-scalars] skipping `st_approximate_medial_axis`: {e}");
    } // alias of st_approximatemedialaxis
    if let Err(e) = register_blob_to_f64(conn, "st_area") {
        eprintln!("[shim-scalars] skipping `st_area`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_area_batch") {
        eprintln!("[shim-scalars] skipping `st_area_batch`: {e}");
    } // alias of st_area
    if let Err(e) = register_blob_to_f64(conn, "st_areathreed") {
        eprintln!("[shim-scalars] skipping `st_areathreed`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_3darea") {
        eprintln!("[shim-scalars] skipping `st_3darea`: {e}");
    } // alias of st_areathreed
    if let Err(e) = register_blob_to_f64(conn, "st_area_threed") {
        eprintln!("[shim-scalars] skipping `st_area_threed`: {e}");
    } // alias of st_areathreed
    if let Err(e) = register_blob_to_u32(conn, "st_arraycols") {
        eprintln!("[shim-scalars] skipping `st_arraycols`: {e}");
    }
    if let Err(e) = register_blob_to_u32(conn, "st_array_cols") {
        eprintln!("[shim-scalars] skipping `st_array_cols`: {e}");
    } // alias of st_arraycols
    if let Err(e) = register_blob_to_u32(conn, "st_arrayrows") {
        eprintln!("[shim-scalars] skipping `st_arrayrows`: {e}");
    }
    if let Err(e) = register_blob_to_u32(conn, "st_array_rows") {
        eprintln!("[shim-scalars] skipping `st_array_rows`: {e}");
    } // alias of st_arrayrows
    if let Err(e) = register_blob_u32_u32_to_f64(conn, "st_arrayvalue") {
        eprintln!("[shim-scalars] skipping `st_arrayvalue`: {e}");
    }
    if let Err(e) = register_blob_u32_u32_to_f64(conn, "st_array_value") {
        eprintln!("[shim-scalars] skipping `st_array_value`: {e}");
    } // alias of st_arrayvalue
    if let Err(e) = register_blob_to_blob(conn, "st_asbinary") {
        eprintln!("[shim-scalars] skipping `st_asbinary`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_as_binary") {
        eprintln!("[shim-scalars] skipping `st_as_binary`: {e}");
    } // alias of st_asbinary
    if let Err(e) = register_blob_to_text(conn, "st_asencodedpolyline") {
        eprintln!("[shim-scalars] skipping `st_asencodedpolyline`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_as_encoded_polyline") {
        eprintln!("[shim-scalars] skipping `st_as_encoded_polyline`: {e}");
    } // alias of st_asencodedpolyline
    if let Err(e) = register_blob_i32_to_text(conn, "st_asencodedpolyline") {
        eprintln!("[shim-scalars] skipping `st_asencodedpolyline`: {e}");
    }
    if let Err(e) = register_blob_i32_to_text(conn, "st_as_encoded_polyline") {
        eprintln!("[shim-scalars] skipping `st_as_encoded_polyline`: {e}");
    } // alias of st_asencodedpolyline
    if let Err(e) = register_blob_to_blob(conn, "st_asewkb") {
        eprintln!("[shim-scalars] skipping `st_asewkb`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_as_ewkb") {
        eprintln!("[shim-scalars] skipping `st_as_ewkb`: {e}");
    } // alias of st_asewkb
    if let Err(e) = register_blob_to_text(conn, "st_asewkt") {
        eprintln!("[shim-scalars] skipping `st_asewkt`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_as_ewkt") {
        eprintln!("[shim-scalars] skipping `st_as_ewkt`: {e}");
    } // alias of st_asewkt
    if let Err(e) = register_blob_text_to_blob(conn, "st_asflatgeobuf") {
        eprintln!("[shim-scalars] skipping `st_asflatgeobuf`: {e}");
    }
    if let Err(e) = register_blob_text_to_blob(conn, "st_as_flatgeobuf") {
        eprintln!("[shim-scalars] skipping `st_as_flatgeobuf`: {e}");
    } // alias of st_asflatgeobuf
    if let Err(e) = register_blob_i32_to_blob(conn, "st_asgeobuf") {
        eprintln!("[shim-scalars] skipping `st_asgeobuf`: {e}");
    }
    if let Err(e) = register_blob_i32_to_blob(conn, "st_as_geobuf") {
        eprintln!("[shim-scalars] skipping `st_as_geobuf`: {e}");
    } // alias of st_asgeobuf
    if let Err(e) = register_blob_to_text(conn, "st_asgeogtext") {
        eprintln!("[shim-scalars] skipping `st_asgeogtext`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_as_geog_text") {
        eprintln!("[shim-scalars] skipping `st_as_geog_text`: {e}");
    } // alias of st_asgeogtext
    if let Err(e) = register_blob_to_text(conn, "st_asgeojson") {
        eprintln!("[shim-scalars] skipping `st_asgeojson`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_as_geojson") {
        eprintln!("[shim-scalars] skipping `st_as_geojson`: {e}");
    } // alias of st_asgeojson
    if let Err(e) = register_blob_to_text(conn, "st_asgml") {
        eprintln!("[shim-scalars] skipping `st_asgml`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_as_gml") {
        eprintln!("[shim-scalars] skipping `st_as_gml`: {e}");
    } // alias of st_asgml
    if let Err(e) = register_blob_to_text(conn, "st_ashexewkb") {
        eprintln!("[shim-scalars] skipping `st_ashexewkb`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_as_hexewkb") {
        eprintln!("[shim-scalars] skipping `st_as_hexewkb`: {e}");
    } // alias of st_ashexewkb
    if let Err(e) = register_blob_to_text(conn, "st_askml") {
        eprintln!("[shim-scalars] skipping `st_askml`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_as_kml") {
        eprintln!("[shim-scalars] skipping `st_as_kml`: {e}");
    } // alias of st_askml
    if let Err(e) = register_blob_to_text(conn, "st_aslatlontext") {
        eprintln!("[shim-scalars] skipping `st_aslatlontext`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_as_lat_lon_text") {
        eprintln!("[shim-scalars] skipping `st_as_lat_lon_text`: {e}");
    } // alias of st_aslatlontext
    if let Err(e) = register_blob_to_text(conn, "st_asmarc21") {
        eprintln!("[shim-scalars] skipping `st_asmarc21`: {e}");
    }
    if let Err(e) = register_blob_text_i32_to_blob(conn, "st_asmvt") {
        eprintln!("[shim-scalars] skipping `st_asmvt`: {e}");
    }
    if let Err(e) = register_blob_text_i32_to_blob(conn, "st_as_mvt") {
        eprintln!("[shim-scalars] skipping `st_as_mvt`: {e}");
    } // alias of st_asmvt
    if let Err(e) = register_blob_4f64_to_blob(conn, "st_asmvtgeom") {
        eprintln!("[shim-scalars] skipping `st_asmvtgeom`: {e}");
    }
    if let Err(e) = register_blob_4f64_to_blob(conn, "st_as_mvt_geom") {
        eprintln!("[shim-scalars] skipping `st_as_mvt_geom`: {e}");
    } // alias of st_asmvtgeom
    if let Err(e) = register_blob_u32_to_blob(conn, "st_aspect") {
        eprintln!("[shim-scalars] skipping `st_aspect`: {e}");
    }
    if let Err(e) = register_blob_u32_to_blob(conn, "st_aspng") {
        eprintln!("[shim-scalars] skipping `st_aspng`: {e}");
    }
    if let Err(e) = register_blob_u32_to_blob(conn, "st_as_png") {
        eprintln!("[shim-scalars] skipping `st_as_png`: {e}");
    } // alias of st_aspng
    if let Err(e) = register_blob_to_text(conn, "st_assvg") {
        eprintln!("[shim-scalars] skipping `st_assvg`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_as_svg") {
        eprintln!("[shim-scalars] skipping `st_as_svg`: {e}");
    } // alias of st_assvg
    if let Err(e) = register_blob_to_text(conn, "st_astext") {
        eprintln!("[shim-scalars] skipping `st_astext`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_as_marc21") {
        eprintln!("[shim-scalars] skipping `st_as_marc21`: {e}");
    } // alias of st_astext
    if let Err(e) = register_blob_to_text(conn, "st_as_text") {
        eprintln!("[shim-scalars] skipping `st_as_text`: {e}");
    } // alias of st_astext
    if let Err(e) = register_blob_to_blob(conn, "st_astiff") {
        eprintln!("[shim-scalars] skipping `st_astiff`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_as_tiff") {
        eprintln!("[shim-scalars] skipping `st_as_tiff`: {e}");
    } // alias of st_astiff
    if let Err(e) = register_blob_to_text(conn, "st_astopojson") {
        eprintln!("[shim-scalars] skipping `st_astopojson`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_as_topo_json") {
        eprintln!("[shim-scalars] skipping `st_as_topo_json`: {e}");
    } // alias of st_astopojson
    if let Err(e) = register_blob_to_text(conn, "topology_as_topojson") {
        eprintln!("[shim-scalars] skipping `topology_as_topojson`: {e}");
    } // alias of st_astopojson
    if let Err(e) = register_blob_to_blob(conn, "st_astwkb") {
        eprintln!("[shim-scalars] skipping `st_astwkb`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_as_twkb") {
        eprintln!("[shim-scalars] skipping `st_as_twkb`: {e}");
    } // alias of st_astwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_astwkb") {
        eprintln!("[shim-scalars] skipping `st_astwkb`: {e}");
    }
    if let Err(e) = register_blob_i32_to_blob(conn, "st_as_twkb") {
        eprintln!("[shim-scalars] skipping `st_as_twkb`: {e}");
    } // alias of st_astwkb
    if let Err(e) = register_blob_i32_i32_to_blob(conn, "st_astwkb") {
        eprintln!("[shim-scalars] skipping `st_astwkb`: {e}");
    }
    if let Err(e) = register_blob_i32_i32_to_blob(conn, "st_as_twkb") {
        eprintln!("[shim-scalars] skipping `st_as_twkb`: {e}");
    } // alias of st_astwkb
    if let Err(e) = register_blob_to_text(conn, "st_asx3d") {
        eprintln!("[shim-scalars] skipping `st_asx3d`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_as_x3d") {
        eprintln!("[shim-scalars] skipping `st_as_x3d`: {e}");
    } // alias of st_asx3d
    if let Err(e) = register_blob_i32_to_text(conn, "st_asx3d") {
        eprintln!("[shim-scalars] skipping `st_asx3d`: {e}");
    }
    if let Err(e) = register_blob_i32_to_text(conn, "st_as_x3d") {
        eprintln!("[shim-scalars] skipping `st_as_x3d`: {e}");
    } // alias of st_asx3d
    if let Err(e) = register_blob_blob_to_f64(conn, "st_azimuth") {
        eprintln!("[shim-scalars] skipping `st_azimuth`: {e}");
    }
    if let Err(e) = register_blob_u32_to_f64(conn, "st_bandnodatavalue") {
        eprintln!("[shim-scalars] skipping `st_bandnodatavalue`: {e}");
    }
    if let Err(e) = register_blob_u32_to_f64(conn, "st_band_nodata_value") {
        eprintln!("[shim-scalars] skipping `st_band_nodata_value`: {e}");
    } // alias of st_bandnodatavalue
    if let Err(e) = register_blob_u32_to_text(conn, "st_bandpixeltype") {
        eprintln!("[shim-scalars] skipping `st_bandpixeltype`: {e}");
    }
    if let Err(e) = register_blob_u32_to_text(conn, "st_band_pixel_type") {
        eprintln!("[shim-scalars] skipping `st_band_pixel_type`: {e}");
    } // alias of st_bandpixeltype
    if let Err(e) = register_blob_blob_to_bool(conn, "st_bboxabove") {
        eprintln!("[shim-scalars] skipping `st_bboxabove`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_bbox_above") {
        eprintln!("[shim-scalars] skipping `st_bbox_above`: {e}");
    } // alias of st_bboxabove
    if let Err(e) = register_blob_blob_to_bool(conn, "st_bboxbelow") {
        eprintln!("[shim-scalars] skipping `st_bboxbelow`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_bbox_below") {
        eprintln!("[shim-scalars] skipping `st_bbox_below`: {e}");
    } // alias of st_bboxbelow
    if let Err(e) = register_blob_blob_to_f64(conn, "st_bboxdistance") {
        eprintln!("[shim-scalars] skipping `st_bboxdistance`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_bboxintersects") {
        eprintln!("[shim-scalars] skipping `st_bboxintersects`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_bboxintersectsnd") {
        eprintln!("[shim-scalars] skipping `st_bboxintersectsnd`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_bboxleftof") {
        eprintln!("[shim-scalars] skipping `st_bboxleftof`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_bbox_left_of") {
        eprintln!("[shim-scalars] skipping `st_bbox_left_of`: {e}");
    } // alias of st_bboxleftof
    if let Err(e) = register_blob_blob_to_bool(conn, "st_bboxoverright") {
        eprintln!("[shim-scalars] skipping `st_bboxoverright`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_bbox_over_right") {
        eprintln!("[shim-scalars] skipping `st_bbox_over_right`: {e}");
    } // alias of st_bboxoverright
    if let Err(e) = register_blob_blob_to_bool(conn, "st_bboxrightof") {
        eprintln!("[shim-scalars] skipping `st_bboxrightof`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_bbox_right_of") {
        eprintln!("[shim-scalars] skipping `st_bbox_right_of`: {e}");
    } // alias of st_bboxrightof
    if let Err(e) = register_text_to_blob(conn, "st_bdmpolyfromtext") {
        eprintln!("[shim-scalars] skipping `st_bdmpolyfromtext`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_bd_mpoly_from_text") {
        eprintln!("[shim-scalars] skipping `st_bd_mpoly_from_text`: {e}");
    } // alias of st_bdmpolyfromtext
    if let Err(e) = register_text_to_blob(conn, "st_bdpolyfromtext") {
        eprintln!("[shim-scalars] skipping `st_bdpolyfromtext`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_bd_poly_from_text") {
        eprintln!("[shim-scalars] skipping `st_bd_poly_from_text`: {e}");
    } // alias of st_bdpolyfromtext
    if let Err(e) = register_blob_to_blob(conn, "st_boundary") {
        eprintln!("[shim-scalars] skipping `st_boundary`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_boundary_batch") {
        eprintln!("[shim-scalars] skipping `st_boundary_batch`: {e}");
    } // alias of st_boundary
    if let Err(e) = register_blob_to_blob(conn, "st_boundarythreed") {
        eprintln!("[shim-scalars] skipping `st_boundarythreed`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_3dboundary") {
        eprintln!("[shim-scalars] skipping `st_3dboundary`: {e}");
    } // alias of st_boundarythreed
    if let Err(e) = register_blob_to_blob(conn, "st_boundary_threed") {
        eprintln!("[shim-scalars] skipping `st_boundary_threed`: {e}");
    } // alias of st_boundarythreed
    if let Err(e) = register_blob_to_blob(conn, "st_boundingdiagonal") {
        eprintln!("[shim-scalars] skipping `st_boundingdiagonal`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_bounding_diagonal") {
        eprintln!("[shim-scalars] skipping `st_bounding_diagonal`: {e}");
    } // alias of st_boundingdiagonal
    if let Err(e) = register_text_to_blob(conn, "st_boxfromgeohash") {
        eprintln!("[shim-scalars] skipping `st_boxfromgeohash`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_box2d_from_geohash") {
        eprintln!("[shim-scalars] skipping `st_box2d_from_geohash`: {e}");
    } // alias of st_boxfromgeohash
    if let Err(e) = register_text_to_blob(conn, "st_box_from_geohash") {
        eprintln!("[shim-scalars] skipping `st_box_from_geohash`: {e}");
    } // alias of st_boxfromgeohash
    if let Err(e) = register_blob_f64_to_blob(conn, "st_buffer") {
        eprintln!("[shim-scalars] skipping `st_buffer`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_buffer_batch") {
        eprintln!("[shim-scalars] skipping `st_buffer_batch`: {e}");
    } // alias of st_buffer
    if let Err(e) = register_blob_f64_i32_to_blob(conn, "st_bufferwithsegs") {
        eprintln!("[shim-scalars] skipping `st_bufferwithsegs`: {e}");
    }
    if let Err(e) = register_blob_f64_i32_to_blob(conn, "st_buffer_with_segs") {
        eprintln!("[shim-scalars] skipping `st_buffer_with_segs`: {e}");
    } // alias of st_bufferwithsegs
    if let Err(e) = register_blob_to_blob(conn, "st_buildarea") {
        eprintln!("[shim-scalars] skipping `st_buildarea`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_build_area") {
        eprintln!("[shim-scalars] skipping `st_build_area`: {e}");
    } // alias of st_buildarea
    if let Err(e) = register_blob_to_blob(conn, "st_centroid") {
        eprintln!("[shim-scalars] skipping `st_centroid`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_centroid_batch") {
        eprintln!("[shim-scalars] skipping `st_centroid_batch`: {e}");
    } // alias of st_centroid
    if let Err(e) = register_blob_to_blob(conn, "st_centroidthreed") {
        eprintln!("[shim-scalars] skipping `st_centroidthreed`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_3dcentroid") {
        eprintln!("[shim-scalars] skipping `st_3dcentroid`: {e}");
    } // alias of st_centroidthreed
    if let Err(e) = register_blob_to_blob(conn, "st_centroid_threed") {
        eprintln!("[shim-scalars] skipping `st_centroid_threed`: {e}");
    } // alias of st_centroidthreed
    if let Err(e) = register_blob_i32_to_blob(conn, "st_chaikinsmoothing") {
        eprintln!("[shim-scalars] skipping `st_chaikinsmoothing`: {e}");
    }
    if let Err(e) = register_blob_i32_to_blob(conn, "st_chaikin_smoothing") {
        eprintln!("[shim-scalars] skipping `st_chaikin_smoothing`: {e}");
    } // alias of st_chaikinsmoothing
    if let Err(e) = register_blob_4f64_to_blob(conn, "st_clipbybox2d") {
        eprintln!("[shim-scalars] skipping `st_clipbybox2d`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_closestpoint") {
        eprintln!("[shim-scalars] skipping `st_closestpoint`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_closest_point") {
        eprintln!("[shim-scalars] skipping `st_closest_point`: {e}");
    } // alias of st_closestpoint
    if let Err(e) = register_blob_blob_to_blob(conn, "st_geog_closest_point") {
        eprintln!("[shim-scalars] skipping `st_geog_closest_point`: {e}");
    } // alias of st_closestpoint
    if let Err(e) = register_blob_blob_to_f64(conn, "st_closestpointofapproach") {
        eprintln!("[shim-scalars] skipping `st_closestpointofapproach`: {e}");
    }
    if let Err(e) = register_blob_blob_to_f64(conn, "st_closest_point_of_approach") {
        eprintln!("[shim-scalars] skipping `st_closest_point_of_approach`: {e}");
    } // alias of st_closestpointofapproach
    if let Err(e) = register_blob_f64_i32_to_text(conn, "st_clusterdbscan") {
        eprintln!("[shim-scalars] skipping `st_clusterdbscan`: {e}");
    }
    if let Err(e) = register_blob_f64_i32_to_text(conn, "st_cluster_dbscan") {
        eprintln!("[shim-scalars] skipping `st_cluster_dbscan`: {e}");
    } // alias of st_clusterdbscan
    if let Err(e) = register_blob_to_blob(conn, "st_clusterintersecting") {
        eprintln!("[shim-scalars] skipping `st_clusterintersecting`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_cluster_intersecting") {
        eprintln!("[shim-scalars] skipping `st_cluster_intersecting`: {e}");
    } // alias of st_clusterintersecting
    if let Err(e) = register_blob_i32_to_text(conn, "st_clusterkmeans") {
        eprintln!("[shim-scalars] skipping `st_clusterkmeans`: {e}");
    }
    if let Err(e) = register_blob_i32_to_text(conn, "st_cluster_kmeans") {
        eprintln!("[shim-scalars] skipping `st_cluster_kmeans`: {e}");
    } // alias of st_clusterkmeans
    if let Err(e) = register_blob_f64_to_blob(conn, "st_clusterwithin") {
        eprintln!("[shim-scalars] skipping `st_clusterwithin`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_cluster_within") {
        eprintln!("[shim-scalars] skipping `st_cluster_within`: {e}");
    } // alias of st_clusterwithin
    if let Err(e) = register_blob_to_blob(conn, "st_collect") {
        eprintln!("[shim-scalars] skipping `st_collect`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_collect") {
        eprintln!("[shim-scalars] skipping `st_collect`: {e}");
    }
    if let Err(e) = register_blob_i32_to_blob(conn, "st_collectionextract") {
        eprintln!("[shim-scalars] skipping `st_collectionextract`: {e}");
    }
    if let Err(e) = register_blob_i32_to_blob(conn, "st_collection_extract") {
        eprintln!("[shim-scalars] skipping `st_collection_extract`: {e}");
    } // alias of st_collectionextract
    if let Err(e) = register_blob_to_blob(conn, "st_collectionhomogenize") {
        eprintln!("[shim-scalars] skipping `st_collectionhomogenize`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_collection_homogenize") {
        eprintln!("[shim-scalars] skipping `st_collection_homogenize`: {e}");
    } // alias of st_collectionhomogenize
    if let Err(e) = register_blob_f64_to_blob(conn, "st_concavehull") {
        eprintln!("[shim-scalars] skipping `st_concavehull`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_concave_hull") {
        eprintln!("[shim-scalars] skipping `st_concave_hull`: {e}");
    } // alias of st_concavehull
    if let Err(e) = register_blob_blob_to_bool(conn, "st_contains") {
        eprintln!("[shim-scalars] skipping `st_contains`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_contains_batch") {
        eprintln!("[shim-scalars] skipping `st_contains_batch`: {e}");
    } // alias of st_contains
    if let Err(e) = register_blob_blob_to_bool(conn, "st_containsproperly") {
        eprintln!("[shim-scalars] skipping `st_containsproperly`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_contains_properly") {
        eprintln!("[shim-scalars] skipping `st_contains_properly`: {e}");
    } // alias of st_containsproperly
    if let Err(e) = register_blob_to_blob(conn, "st_convexhull") {
        eprintln!("[shim-scalars] skipping `st_convexhull`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_convex_hull") {
        eprintln!("[shim-scalars] skipping `st_convex_hull`: {e}");
    } // alias of st_convexhull
    if let Err(e) = register_blob_to_blob(conn, "st_convex_hull_batch") {
        eprintln!("[shim-scalars] skipping `st_convex_hull_batch`: {e}");
    } // alias of st_convexhull
    if let Err(e) = register_blob_to_blob(conn, "st_geog_convex_hull") {
        eprintln!("[shim-scalars] skipping `st_geog_convex_hull`: {e}");
    } // alias of st_convexhull
    if let Err(e) = register_blob_to_blob(conn, "st_convexhullthreed") {
        eprintln!("[shim-scalars] skipping `st_convexhullthreed`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_3dconvexhull") {
        eprintln!("[shim-scalars] skipping `st_3dconvexhull`: {e}");
    } // alias of st_convexhullthreed
    if let Err(e) = register_blob_to_blob(conn, "st_convex_hull_threed") {
        eprintln!("[shim-scalars] skipping `st_convex_hull_threed`: {e}");
    } // alias of st_convexhullthreed
    if let Err(e) = register_blob_to_i32(conn, "st_coorddim") {
        eprintln!("[shim-scalars] skipping `st_coorddim`: {e}");
    }
    if let Err(e) = register_blob_to_i32(conn, "st_coord_dim") {
        eprintln!("[shim-scalars] skipping `st_coord_dim`: {e}");
    } // alias of st_coorddim
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_coverageclean") {
        eprintln!("[shim-scalars] skipping `st_coverageclean`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_coverage_clean") {
        eprintln!("[shim-scalars] skipping `st_coverage_clean`: {e}");
    } // alias of st_coverageclean
    if let Err(e) = register_blob_f64_to_blob(conn, "st_coverageinvalidedges") {
        eprintln!("[shim-scalars] skipping `st_coverageinvalidedges`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_coverage_invalid_edges") {
        eprintln!("[shim-scalars] skipping `st_coverage_invalid_edges`: {e}");
    } // alias of st_coverageinvalidedges
    if let Err(e) = register_blob_f64_bool_to_blob(conn, "st_coveragesimplify") {
        eprintln!("[shim-scalars] skipping `st_coveragesimplify`: {e}");
    }
    if let Err(e) = register_blob_f64_bool_to_blob(conn, "st_coverage_simplify") {
        eprintln!("[shim-scalars] skipping `st_coverage_simplify`: {e}");
    } // alias of st_coveragesimplify
    if let Err(e) = register_blob_blob_to_bool(conn, "st_coveredby") {
        eprintln!("[shim-scalars] skipping `st_coveredby`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_covered_by") {
        eprintln!("[shim-scalars] skipping `st_covered_by`: {e}");
    } // alias of st_coveredby
    if let Err(e) = register_blob_blob_to_bool(conn, "st_covers") {
        eprintln!("[shim-scalars] skipping `st_covers`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_covers_batch") {
        eprintln!("[shim-scalars] skipping `st_covers_batch`: {e}");
    } // alias of st_covers
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_cpawithin") {
        eprintln!("[shim-scalars] skipping `st_cpawithin`: {e}");
    }
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_cpa_within") {
        eprintln!("[shim-scalars] skipping `st_cpa_within`: {e}");
    } // alias of st_cpawithin
    if let Err(e) = register_blob_u32_text_to_text(conn, "st_createtopogeom") {
        eprintln!("[shim-scalars] skipping `st_createtopogeom`: {e}");
    }
    if let Err(e) = register_blob_u32_text_to_text(conn, "st_create_topo_geom") {
        eprintln!("[shim-scalars] skipping `st_create_topo_geom`: {e}");
    } // alias of st_createtopogeom
    if let Err(e) = register_text_i32_f64_to_blob(conn, "st_createtopology") {
        eprintln!("[shim-scalars] skipping `st_createtopology`: {e}");
    }
    if let Err(e) = register_text_i32_f64_to_blob(conn, "st_create_topology") {
        eprintln!("[shim-scalars] skipping `st_create_topology`: {e}");
    } // alias of st_createtopology
    if let Err(e) = register_text_i32_f64_to_blob(conn, "topology_create") {
        eprintln!("[shim-scalars] skipping `topology_create`: {e}");
    } // alias of st_createtopology
    if let Err(e) = register_blob_blob_to_bool(conn, "st_crosses") {
        eprintln!("[shim-scalars] skipping `st_crosses`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_crosses_batch") {
        eprintln!("[shim-scalars] skipping `st_crosses_batch`: {e}");
    } // alias of st_crosses
    if let Err(e) = register_blob_to_blob(conn, "st_curvetoline") {
        eprintln!("[shim-scalars] skipping `st_curvetoline`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_curve_to_line") {
        eprintln!("[shim-scalars] skipping `st_curve_to_line`: {e}");
    } // alias of st_curvetoline
    if let Err(e) = register_blob_f64_to_blob(conn, "st_curvetoline") {
        eprintln!("[shim-scalars] skipping `st_curvetoline`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_curve_to_line") {
        eprintln!("[shim-scalars] skipping `st_curve_to_line`: {e}");
    } // alias of st_curvetoline
    if let Err(e) = register_blob_f64_i32_to_blob(conn, "st_curvetoline") {
        eprintln!("[shim-scalars] skipping `st_curvetoline`: {e}");
    }
    if let Err(e) = register_blob_f64_i32_to_blob(conn, "st_curve_to_line") {
        eprintln!("[shim-scalars] skipping `st_curve_to_line`: {e}");
    } // alias of st_curvetoline
    if let Err(e) = register_blob_to_blob(conn, "st_delaunaytriangles") {
        eprintln!("[shim-scalars] skipping `st_delaunaytriangles`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_constrained_delaunay_triangles") {
        eprintln!("[shim-scalars] skipping `st_constrained_delaunay_triangles`: {e}");
    } // alias of st_delaunaytriangles
    if let Err(e) = register_blob_to_blob(conn, "st_constraineddelaunaytriangles") {
        eprintln!("[shim-scalars] skipping `st_constraineddelaunaytriangles`: {e}");
    } // alias of st_delaunaytriangles
    if let Err(e) = register_blob_to_blob(conn, "st_constrainedelaunaytriangles") {
        eprintln!("[shim-scalars] skipping `st_constrainedelaunaytriangles`: {e}");
    } // alias of st_delaunaytriangles
    if let Err(e) = register_blob_to_blob(conn, "st_delaunay_triangles") {
        eprintln!("[shim-scalars] skipping `st_delaunay_triangles`: {e}");
    } // alias of st_delaunaytriangles
    if let Err(e) = register_blob_f64_to_blob(conn, "st_delaunaytriangles") {
        eprintln!("[shim-scalars] skipping `st_delaunaytriangles`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_constrained_delaunay_triangles") {
        eprintln!("[shim-scalars] skipping `st_constrained_delaunay_triangles`: {e}");
    } // alias of st_delaunaytriangles
    if let Err(e) = register_blob_f64_to_blob(conn, "st_constraineddelaunaytriangles") {
        eprintln!("[shim-scalars] skipping `st_constraineddelaunaytriangles`: {e}");
    } // alias of st_delaunaytriangles
    if let Err(e) = register_blob_f64_to_blob(conn, "st_constrainedelaunaytriangles") {
        eprintln!("[shim-scalars] skipping `st_constrainedelaunaytriangles`: {e}");
    } // alias of st_delaunaytriangles
    if let Err(e) = register_blob_f64_to_blob(conn, "st_delaunay_triangles") {
        eprintln!("[shim-scalars] skipping `st_delaunay_triangles`: {e}");
    } // alias of st_delaunaytriangles
    if let Err(e) = register_blob_f64_i32_to_blob(conn, "st_delaunaytriangles") {
        eprintln!("[shim-scalars] skipping `st_delaunaytriangles`: {e}");
    }
    if let Err(e) = register_blob_f64_i32_to_blob(conn, "st_constrained_delaunay_triangles") {
        eprintln!("[shim-scalars] skipping `st_constrained_delaunay_triangles`: {e}");
    } // alias of st_delaunaytriangles
    if let Err(e) = register_blob_f64_i32_to_blob(conn, "st_constraineddelaunaytriangles") {
        eprintln!("[shim-scalars] skipping `st_constraineddelaunaytriangles`: {e}");
    } // alias of st_delaunaytriangles
    if let Err(e) = register_blob_f64_i32_to_blob(conn, "st_constrainedelaunaytriangles") {
        eprintln!("[shim-scalars] skipping `st_constrainedelaunaytriangles`: {e}");
    } // alias of st_delaunaytriangles
    if let Err(e) = register_blob_f64_i32_to_blob(conn, "st_delaunay_triangles") {
        eprintln!("[shim-scalars] skipping `st_delaunay_triangles`: {e}");
    } // alias of st_delaunaytriangles
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_dfullywithin") {
        eprintln!("[shim-scalars] skipping `st_dfullywithin`: {e}");
    }
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_dfully_within") {
        eprintln!("[shim-scalars] skipping `st_dfully_within`: {e}");
    } // alias of st_dfullywithin
    if let Err(e) = register_blob_blob_to_blob(conn, "st_difference") {
        eprintln!("[shim-scalars] skipping `st_difference`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_difference_batch") {
        eprintln!("[shim-scalars] skipping `st_difference_batch`: {e}");
    } // alias of st_difference
    if let Err(e) = register_blob_blob_to_blob(conn, "st_geog_difference") {
        eprintln!("[shim-scalars] skipping `st_geog_difference`: {e}");
    } // alias of st_difference
    if let Err(e) = register_blob_blob_to_blob(conn, "st_differencethreed") {
        eprintln!("[shim-scalars] skipping `st_differencethreed`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_3ddifference") {
        eprintln!("[shim-scalars] skipping `st_3ddifference`: {e}");
    } // alias of st_differencethreed
    if let Err(e) = register_blob_blob_to_blob(conn, "st_difference_threed") {
        eprintln!("[shim-scalars] skipping `st_difference_threed`: {e}");
    } // alias of st_differencethreed
    if let Err(e) = register_blob_to_i32(conn, "st_dimension") {
        eprintln!("[shim-scalars] skipping `st_dimension`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_disjoint") {
        eprintln!("[shim-scalars] skipping `st_disjoint`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_disjoint_batch") {
        eprintln!("[shim-scalars] skipping `st_disjoint_batch`: {e}");
    } // alias of st_disjoint
    if let Err(e) = register_blob_blob_to_f64(conn, "st_distance") {
        eprintln!("[shim-scalars] skipping `st_distance`: {e}");
    }
    if let Err(e) = register_blob_blob_to_f64(conn, "st_distance_batch") {
        eprintln!("[shim-scalars] skipping `st_distance_batch`: {e}");
    } // alias of st_distance
    if let Err(e) = register_blob_blob_to_f64(conn, "st_distancecpa") {
        eprintln!("[shim-scalars] skipping `st_distancecpa`: {e}");
    }
    if let Err(e) = register_blob_blob_to_f64(conn, "st_distance_cpa") {
        eprintln!("[shim-scalars] skipping `st_distance_cpa`: {e}");
    } // alias of st_distancecpa
    if let Err(e) = register_blob_blob_to_f64(conn, "st_distancesphere") {
        eprintln!("[shim-scalars] skipping `st_distancesphere`: {e}");
    }
    if let Err(e) = register_blob_blob_to_f64(conn, "st_distance_sphere") {
        eprintln!("[shim-scalars] skipping `st_distance_sphere`: {e}");
    } // alias of st_distancesphere
    if let Err(e) = register_blob_blob_to_f64(conn, "st_distance_sphere_batch") {
        eprintln!("[shim-scalars] skipping `st_distance_sphere_batch`: {e}");
    } // alias of st_distancesphere
    if let Err(e) = register_blob_blob_to_f64(conn, "st_distancespheroid") {
        eprintln!("[shim-scalars] skipping `st_distancespheroid`: {e}");
    }
    if let Err(e) = register_blob_blob_to_f64(conn, "st_distance_spheroid") {
        eprintln!("[shim-scalars] skipping `st_distance_spheroid`: {e}");
    } // alias of st_distancespheroid
    if let Err(e) = register_blob_blob_to_f64(conn, "st_distance_spheroid_batch") {
        eprintln!("[shim-scalars] skipping `st_distance_spheroid_batch`: {e}");
    } // alias of st_distancespheroid
    if let Err(e) = register_blob_blob_to_f64(conn, "st_distancethreed") {
        eprintln!("[shim-scalars] skipping `st_distancethreed`: {e}");
    }
    if let Err(e) = register_blob_blob_to_f64(conn, "st_3ddistance") {
        eprintln!("[shim-scalars] skipping `st_3ddistance`: {e}");
    } // alias of st_distancethreed
    if let Err(e) = register_blob_blob_to_f64(conn, "st_distance_threed") {
        eprintln!("[shim-scalars] skipping `st_distance_threed`: {e}");
    } // alias of st_distancethreed
    if let Err(e) = register_blob_u32_to_text(conn, "st_dumpaspolygons") {
        eprintln!("[shim-scalars] skipping `st_dumpaspolygons`: {e}");
    }
    if let Err(e) = register_blob_u32_to_text(conn, "st_dump_as_polygons") {
        eprintln!("[shim-scalars] skipping `st_dump_as_polygons`: {e}");
    } // alias of st_dumpaspolygons
    if let Err(e) = register_blob_u32_to_blob(conn, "st_dumpvalues") {
        eprintln!("[shim-scalars] skipping `st_dumpvalues`: {e}");
    }
    if let Err(e) = register_blob_u32_to_blob(conn, "st_dump_values") {
        eprintln!("[shim-scalars] skipping `st_dump_values`: {e}");
    } // alias of st_dumpvalues
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_dwithin") {
        eprintln!("[shim-scalars] skipping `st_dwithin`: {e}");
    }
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_d_within") {
        eprintln!("[shim-scalars] skipping `st_d_within`: {e}");
    } // alias of st_dwithin
    if let Err(e) = register_blob_to_blob(conn, "st_endpoint") {
        eprintln!("[shim-scalars] skipping `st_endpoint`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_end_point") {
        eprintln!("[shim-scalars] skipping `st_end_point`: {e}");
    } // alias of st_endpoint
    if let Err(e) = register_blob_to_blob(conn, "st_end_point_batch") {
        eprintln!("[shim-scalars] skipping `st_end_point_batch`: {e}");
    } // alias of st_endpoint
    if let Err(e) = register_blob_to_blob(conn, "st_envelope") {
        eprintln!("[shim-scalars] skipping `st_envelope`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_envelope_batch") {
        eprintln!("[shim-scalars] skipping `st_envelope_batch`: {e}");
    } // alias of st_envelope
    if let Err(e) = register_blob_to_blob(conn, "st_envelopethreed") {
        eprintln!("[shim-scalars] skipping `st_envelopethreed`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_3denvelope") {
        eprintln!("[shim-scalars] skipping `st_3denvelope`: {e}");
    } // alias of st_envelopethreed
    if let Err(e) = register_blob_to_blob(conn, "st_envelope_threed") {
        eprintln!("[shim-scalars] skipping `st_envelope_threed`: {e}");
    } // alias of st_envelopethreed
    if let Err(e) = register_blob_blob_to_bool(conn, "st_equals") {
        eprintln!("[shim-scalars] skipping `st_equals`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_equals_batch") {
        eprintln!("[shim-scalars] skipping `st_equals_batch`: {e}");
    } // alias of st_equals
    if let Err(e) = register_blob_blob_to_bool(conn, "st_equalsexact") {
        eprintln!("[shim-scalars] skipping `st_equalsexact`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_equals_exact") {
        eprintln!("[shim-scalars] skipping `st_equals_exact`: {e}");
    } // alias of st_equalsexact
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_equalsexact") {
        eprintln!("[shim-scalars] skipping `st_equalsexact`: {e}");
    }
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_equals_exact") {
        eprintln!("[shim-scalars] skipping `st_equals_exact`: {e}");
    } // alias of st_equalsexact
    if let Err(e) = register_blob_f64_to_blob(conn, "st_expand") {
        eprintln!("[shim-scalars] skipping `st_expand`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_exteriorring") {
        eprintln!("[shim-scalars] skipping `st_exteriorring`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_exterior_ring") {
        eprintln!("[shim-scalars] skipping `st_exterior_ring`: {e}");
    } // alias of st_exteriorring
    if let Err(e) = register_blob_f64_f64_f64_to_blob(conn, "st_extrude") {
        eprintln!("[shim-scalars] skipping `st_extrude`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_filterbym") {
        eprintln!("[shim-scalars] skipping `st_filterbym`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_filter_by_m") {
        eprintln!("[shim-scalars] skipping `st_filter_by_m`: {e}");
    } // alias of st_filterbym
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_filterbym") {
        eprintln!("[shim-scalars] skipping `st_filterbym`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_filter_by_m") {
        eprintln!("[shim-scalars] skipping `st_filter_by_m`: {e}");
    } // alias of st_filterbym
    if let Err(e) = register_blob_to_blob(conn, "st_flipcoordinates") {
        eprintln!("[shim-scalars] skipping `st_flipcoordinates`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_flip_coordinates") {
        eprintln!("[shim-scalars] skipping `st_flip_coordinates`: {e}");
    } // alias of st_flipcoordinates
    if let Err(e) = register_blob_to_blob(conn, "st_force2d") {
        eprintln!("[shim-scalars] skipping `st_force2d`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_force_2d") {
        eprintln!("[shim-scalars] skipping `st_force_2d`: {e}");
    } // alias of st_force2d
    if let Err(e) = register_blob_to_blob(conn, "st_force_twod") {
        eprintln!("[shim-scalars] skipping `st_force_twod`: {e}");
    } // alias of st_force2d
    if let Err(e) = register_blob_to_blob(conn, "st_forcetwod") {
        eprintln!("[shim-scalars] skipping `st_forcetwod`: {e}");
    } // alias of st_force2d
    if let Err(e) = register_blob_to_blob(conn, "st_force3d") {
        eprintln!("[shim-scalars] skipping `st_force3d`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_force3dz") {
        eprintln!("[shim-scalars] skipping `st_force3dz`: {e}");
    } // alias of st_force3d
    if let Err(e) = register_blob_to_blob(conn, "st_force_3d") {
        eprintln!("[shim-scalars] skipping `st_force_3d`: {e}");
    } // alias of st_force3d
    if let Err(e) = register_blob_to_blob(conn, "st_force_threed") {
        eprintln!("[shim-scalars] skipping `st_force_threed`: {e}");
    } // alias of st_force3d
    if let Err(e) = register_blob_to_blob(conn, "st_force_threedz") {
        eprintln!("[shim-scalars] skipping `st_force_threedz`: {e}");
    } // alias of st_force3d
    if let Err(e) = register_blob_to_blob(conn, "st_forcethreed") {
        eprintln!("[shim-scalars] skipping `st_forcethreed`: {e}");
    } // alias of st_force3d
    if let Err(e) = register_blob_to_blob(conn, "st_forcethreedz") {
        eprintln!("[shim-scalars] skipping `st_forcethreedz`: {e}");
    } // alias of st_force3d
    if let Err(e) = register_blob_to_blob(conn, "st_forcecollection") {
        eprintln!("[shim-scalars] skipping `st_forcecollection`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_force_collection") {
        eprintln!("[shim-scalars] skipping `st_force_collection`: {e}");
    } // alias of st_forcecollection
    if let Err(e) = register_blob_to_blob(conn, "st_forcecurve") {
        eprintln!("[shim-scalars] skipping `st_forcecurve`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_force_curve") {
        eprintln!("[shim-scalars] skipping `st_force_curve`: {e}");
    } // alias of st_forcecurve
    if let Err(e) = register_blob_to_blob(conn, "st_forcefourd") {
        eprintln!("[shim-scalars] skipping `st_forcefourd`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_force4d") {
        eprintln!("[shim-scalars] skipping `st_force4d`: {e}");
    } // alias of st_forcefourd
    if let Err(e) = register_blob_to_blob(conn, "st_force_4d") {
        eprintln!("[shim-scalars] skipping `st_force_4d`: {e}");
    } // alias of st_forcefourd
    if let Err(e) = register_blob_to_blob(conn, "st_force_fourd") {
        eprintln!("[shim-scalars] skipping `st_force_fourd`: {e}");
    } // alias of st_forcefourd
    if let Err(e) = register_blob_f64_to_blob(conn, "st_forcefourd") {
        eprintln!("[shim-scalars] skipping `st_forcefourd`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_force4d") {
        eprintln!("[shim-scalars] skipping `st_force4d`: {e}");
    } // alias of st_forcefourd
    if let Err(e) = register_blob_f64_to_blob(conn, "st_force_4d") {
        eprintln!("[shim-scalars] skipping `st_force_4d`: {e}");
    } // alias of st_forcefourd
    if let Err(e) = register_blob_f64_to_blob(conn, "st_force_fourd") {
        eprintln!("[shim-scalars] skipping `st_force_fourd`: {e}");
    } // alias of st_forcefourd
    if let Err(e) = register_blob_to_blob(conn, "st_forcepolygonccw") {
        eprintln!("[shim-scalars] skipping `st_forcepolygonccw`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_force_polygon_ccw") {
        eprintln!("[shim-scalars] skipping `st_force_polygon_ccw`: {e}");
    } // alias of st_forcepolygonccw
    if let Err(e) = register_blob_to_blob(conn, "st_forcepolygoncw") {
        eprintln!("[shim-scalars] skipping `st_forcepolygoncw`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_force_polygon_cw") {
        eprintln!("[shim-scalars] skipping `st_force_polygon_cw`: {e}");
    } // alias of st_forcepolygoncw
    if let Err(e) = register_blob_to_blob(conn, "st_forcerhr") {
        eprintln!("[shim-scalars] skipping `st_forcerhr`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_force_rhr") {
        eprintln!("[shim-scalars] skipping `st_force_rhr`: {e}");
    } // alias of st_forcerhr
    if let Err(e) = register_blob_to_blob(conn, "st_forcesfs") {
        eprintln!("[shim-scalars] skipping `st_forcesfs`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_force_sfs") {
        eprintln!("[shim-scalars] skipping `st_force_sfs`: {e}");
    } // alias of st_forcesfs
    if let Err(e) = register_blob_text_to_blob(conn, "st_forcesfs") {
        eprintln!("[shim-scalars] skipping `st_forcesfs`: {e}");
    }
    if let Err(e) = register_blob_text_to_blob(conn, "st_force_sfs") {
        eprintln!("[shim-scalars] skipping `st_force_sfs`: {e}");
    } // alias of st_forcesfs
    if let Err(e) = register_blob_to_blob(conn, "st_forcethreedm") {
        eprintln!("[shim-scalars] skipping `st_forcethreedm`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_force3dm") {
        eprintln!("[shim-scalars] skipping `st_force3dm`: {e}");
    } // alias of st_forcethreedm
    if let Err(e) = register_blob_to_blob(conn, "st_force_3dm") {
        eprintln!("[shim-scalars] skipping `st_force_3dm`: {e}");
    } // alias of st_forcethreedm
    if let Err(e) = register_blob_to_blob(conn, "st_force_threedm") {
        eprintln!("[shim-scalars] skipping `st_force_threedm`: {e}");
    } // alias of st_forcethreedm
    if let Err(e) = register_blob_blob_to_f64(conn, "st_frechetdistance") {
        eprintln!("[shim-scalars] skipping `st_frechetdistance`: {e}");
    }
    if let Err(e) = register_blob_blob_to_f64(conn, "st_frechet_distance") {
        eprintln!("[shim-scalars] skipping `st_frechet_distance`: {e}");
    } // alias of st_frechetdistance
    if let Err(e) = register_blob_to_blob(conn, "st_fromflatgeobuf") {
        eprintln!("[shim-scalars] skipping `st_fromflatgeobuf`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_from_flatgeobuf") {
        eprintln!("[shim-scalars] skipping `st_from_flatgeobuf`: {e}");
    } // alias of st_fromflatgeobuf
    if let Err(e) = register_blob_i32_to_blob(conn, "st_generatepoints") {
        eprintln!("[shim-scalars] skipping `st_generatepoints`: {e}");
    }
    if let Err(e) = register_blob_i32_to_blob(conn, "st_generate_points") {
        eprintln!("[shim-scalars] skipping `st_generate_points`: {e}");
    } // alias of st_generatepoints
    if let Err(e) = register_blob_i32_i32_to_blob(conn, "st_generatepoints") {
        eprintln!("[shim-scalars] skipping `st_generatepoints`: {e}");
    }
    if let Err(e) = register_blob_i32_i32_to_blob(conn, "st_generate_points") {
        eprintln!("[shim-scalars] skipping `st_generate_points`: {e}");
    } // alias of st_generatepoints
    if let Err(e) = register_blob_to_f64(conn, "st_geogarea") {
        eprintln!("[shim-scalars] skipping `st_geogarea`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_geog_area") {
        eprintln!("[shim-scalars] skipping `st_geog_area`: {e}");
    } // alias of st_geogarea
    if let Err(e) = register_blob_to_f64(conn, "st_geog_area_batch") {
        eprintln!("[shim-scalars] skipping `st_geog_area_batch`: {e}");
    } // alias of st_geogarea
    if let Err(e) = register_blob_blob_to_f64(conn, "st_geogazimuth") {
        eprintln!("[shim-scalars] skipping `st_geogazimuth`: {e}");
    }
    if let Err(e) = register_blob_blob_to_f64(conn, "st_geog_azimuth") {
        eprintln!("[shim-scalars] skipping `st_geog_azimuth`: {e}");
    } // alias of st_geogazimuth
    if let Err(e) = register_blob_blob_to_f64(conn, "st_geog_azimuth_batch") {
        eprintln!("[shim-scalars] skipping `st_geog_azimuth_batch`: {e}");
    } // alias of st_geogazimuth
    if let Err(e) = register_blob_f64_to_blob(conn, "st_geogbuffer") {
        eprintln!("[shim-scalars] skipping `st_geogbuffer`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_geog_buffer") {
        eprintln!("[shim-scalars] skipping `st_geog_buffer`: {e}");
    } // alias of st_geogbuffer
    if let Err(e) = register_blob_f64_i32_to_blob(conn, "st_geogbufferwithsegs") {
        eprintln!("[shim-scalars] skipping `st_geogbufferwithsegs`: {e}");
    }
    if let Err(e) = register_blob_f64_i32_to_blob(conn, "st_geog_buffer_with_segs") {
        eprintln!("[shim-scalars] skipping `st_geog_buffer_with_segs`: {e}");
    } // alias of st_geogbufferwithsegs
    if let Err(e) = register_blob_to_blob(conn, "st_geogcentroid") {
        eprintln!("[shim-scalars] skipping `st_geogcentroid`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_geog_centroid") {
        eprintln!("[shim-scalars] skipping `st_geog_centroid`: {e}");
    } // alias of st_geogcentroid
    if let Err(e) = register_blob_blob_to_bool(conn, "st_geogcoveredby") {
        eprintln!("[shim-scalars] skipping `st_geogcoveredby`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_geog_covered_by") {
        eprintln!("[shim-scalars] skipping `st_geog_covered_by`: {e}");
    } // alias of st_geogcoveredby
    if let Err(e) = register_blob_blob_to_bool(conn, "st_geogcovers") {
        eprintln!("[shim-scalars] skipping `st_geogcovers`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_geog_covers") {
        eprintln!("[shim-scalars] skipping `st_geog_covers`: {e}");
    } // alias of st_geogcovers
    if let Err(e) = register_blob_blob_to_f64(conn, "st_geogdistance") {
        eprintln!("[shim-scalars] skipping `st_geogdistance`: {e}");
    }
    if let Err(e) = register_blob_blob_to_f64(conn, "st_geog_distance") {
        eprintln!("[shim-scalars] skipping `st_geog_distance`: {e}");
    } // alias of st_geogdistance
    if let Err(e) = register_blob_blob_to_f64(conn, "st_geog_distance_batch") {
        eprintln!("[shim-scalars] skipping `st_geog_distance_batch`: {e}");
    } // alias of st_geogdistance
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_geogdwithin") {
        eprintln!("[shim-scalars] skipping `st_geogdwithin`: {e}");
    }
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_geog_dwithin") {
        eprintln!("[shim-scalars] skipping `st_geog_dwithin`: {e}");
    } // alias of st_geogdwithin
    if let Err(e) = register_blob_blob_f64_to_bool(conn, "st_geog_dwithin_batch") {
        eprintln!("[shim-scalars] skipping `st_geog_dwithin_batch`: {e}");
    } // alias of st_geogdwithin
    if let Err(e) = register_blob_f64_to_blob(conn, "st_geogexpand") {
        eprintln!("[shim-scalars] skipping `st_geogexpand`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_geog_expand") {
        eprintln!("[shim-scalars] skipping `st_geog_expand`: {e}");
    } // alias of st_geogexpand
    if let Err(e) = register_text_to_blob(conn, "st_geogfromewkt") {
        eprintln!("[shim-scalars] skipping `st_geogfromewkt`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_geog_from_ewkt") {
        eprintln!("[shim-scalars] skipping `st_geog_from_ewkt`: {e}");
    } // alias of st_geogfromewkt
    if let Err(e) = register_text_to_blob(conn, "st_geogfromtext") {
        eprintln!("[shim-scalars] skipping `st_geogfromtext`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_geog_from_text") {
        eprintln!("[shim-scalars] skipping `st_geog_from_text`: {e}");
    } // alias of st_geogfromtext
    if let Err(e) = register_blob_to_blob(conn, "st_geogfromwkb") {
        eprintln!("[shim-scalars] skipping `st_geogfromwkb`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_geog_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_geog_from_wkb`: {e}");
    } // alias of st_geogfromwkb
    if let Err(e) = register_blob_to_text(conn, "st_geoggeometrytype") {
        eprintln!("[shim-scalars] skipping `st_geoggeometrytype`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_geog_geometry_type") {
        eprintln!("[shim-scalars] skipping `st_geog_geometry_type`: {e}");
    } // alias of st_geoggeometrytype
    if let Err(e) = register_blob_blob_to_bool(conn, "st_geogintersects") {
        eprintln!("[shim-scalars] skipping `st_geogintersects`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_geog_intersects") {
        eprintln!("[shim-scalars] skipping `st_geog_intersects`: {e}");
    } // alias of st_geogintersects
    if let Err(e) = register_blob_to_bool(conn, "st_geogisclosed") {
        eprintln!("[shim-scalars] skipping `st_geogisclosed`: {e}");
    }
    if let Err(e) = register_blob_to_bool(conn, "st_geog_is_closed") {
        eprintln!("[shim-scalars] skipping `st_geog_is_closed`: {e}");
    } // alias of st_geogisclosed
    if let Err(e) = register_blob_to_bool(conn, "st_geogisempty") {
        eprintln!("[shim-scalars] skipping `st_geogisempty`: {e}");
    }
    if let Err(e) = register_blob_to_bool(conn, "st_geog_is_empty") {
        eprintln!("[shim-scalars] skipping `st_geog_is_empty`: {e}");
    } // alias of st_geogisempty
    if let Err(e) = register_blob_to_bool(conn, "st_geogissimple") {
        eprintln!("[shim-scalars] skipping `st_geogissimple`: {e}");
    }
    if let Err(e) = register_blob_to_bool(conn, "st_geog_is_simple") {
        eprintln!("[shim-scalars] skipping `st_geog_is_simple`: {e}");
    } // alias of st_geogissimple
    if let Err(e) = register_blob_to_f64(conn, "st_geoglength") {
        eprintln!("[shim-scalars] skipping `st_geoglength`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_geog_length") {
        eprintln!("[shim-scalars] skipping `st_geog_length`: {e}");
    } // alias of st_geoglength
    if let Err(e) = register_blob_to_f64(conn, "st_geog_length_batch") {
        eprintln!("[shim-scalars] skipping `st_geog_length_batch`: {e}");
    } // alias of st_geoglength
    if let Err(e) = register_blob_to_i32(conn, "st_geognpoints") {
        eprintln!("[shim-scalars] skipping `st_geognpoints`: {e}");
    }
    if let Err(e) = register_blob_to_i32(conn, "st_geog_npoints") {
        eprintln!("[shim-scalars] skipping `st_geog_npoints`: {e}");
    } // alias of st_geognpoints
    if let Err(e) = register_blob_to_f64(conn, "st_geogperimeter") {
        eprintln!("[shim-scalars] skipping `st_geogperimeter`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_geog_perimeter") {
        eprintln!("[shim-scalars] skipping `st_geog_perimeter`: {e}");
    } // alias of st_geogperimeter
    if let Err(e) = register_blob_to_f64(conn, "st_geog_perimeter_batch") {
        eprintln!("[shim-scalars] skipping `st_geog_perimeter_batch`: {e}");
    } // alias of st_geogperimeter
    if let Err(e) = register_f64_f64_to_blob(conn, "st_geogpoint") {
        eprintln!("[shim-scalars] skipping `st_geogpoint`: {e}");
    }
    if let Err(e) = register_f64_f64_to_blob(conn, "st_geog_point") {
        eprintln!("[shim-scalars] skipping `st_geog_point`: {e}");
    } // alias of st_geogpoint
    if let Err(e) = register_blob_to_text(conn, "st_geogsummary") {
        eprintln!("[shim-scalars] skipping `st_geogsummary`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_geog_summary") {
        eprintln!("[shim-scalars] skipping `st_geog_summary`: {e}");
    } // alias of st_geogsummary
    if let Err(e) = register_blob_to_blob(conn, "st_geogtogeom") {
        eprintln!("[shim-scalars] skipping `st_geogtogeom`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_geog_to_geom") {
        eprintln!("[shim-scalars] skipping `st_geog_to_geom`: {e}");
    } // alias of st_geogtogeom
    if let Err(e) = register_blob_to_text(conn, "st_geohash") {
        eprintln!("[shim-scalars] skipping `st_geohash`: {e}");
    }
    if let Err(e) = register_blob_i32_to_text(conn, "st_geohash") {
        eprintln!("[shim-scalars] skipping `st_geohash`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_geomequal") {
        eprintln!("[shim-scalars] skipping `st_geomequal`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_geometricmedian") {
        eprintln!("[shim-scalars] skipping `st_geometricmedian`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_geometric_median") {
        eprintln!("[shim-scalars] skipping `st_geometric_median`: {e}");
    } // alias of st_geometricmedian
    if let Err(e) = register_blob_f64_to_blob(conn, "st_geometricmedian") {
        eprintln!("[shim-scalars] skipping `st_geometricmedian`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_geometric_median") {
        eprintln!("[shim-scalars] skipping `st_geometric_median`: {e}");
    } // alias of st_geometricmedian
    if let Err(e) = register_blob_f64_i32_to_blob(conn, "st_geometricmedian") {
        eprintln!("[shim-scalars] skipping `st_geometricmedian`: {e}");
    }
    if let Err(e) = register_blob_f64_i32_to_blob(conn, "st_geometric_median") {
        eprintln!("[shim-scalars] skipping `st_geometric_median`: {e}");
    } // alias of st_geometricmedian
    if let Err(e) = register_blob_i32_to_blob(conn, "st_geometryn") {
        eprintln!("[shim-scalars] skipping `st_geometryn`: {e}");
    }
    if let Err(e) = register_blob_i32_to_blob(conn, "st_curve_n") {
        eprintln!("[shim-scalars] skipping `st_curve_n`: {e}");
    } // alias of st_geometryn
    if let Err(e) = register_blob_i32_to_blob(conn, "st_curven") {
        eprintln!("[shim-scalars] skipping `st_curven`: {e}");
    } // alias of st_geometryn
    if let Err(e) = register_blob_i32_to_blob(conn, "st_geometry_n") {
        eprintln!("[shim-scalars] skipping `st_geometry_n`: {e}");
    } // alias of st_geometryn
    if let Err(e) = register_blob_to_text(conn, "st_geometrytype") {
        eprintln!("[shim-scalars] skipping `st_geometrytype`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_geometry_type") {
        eprintln!("[shim-scalars] skipping `st_geometry_type`: {e}");
    } // alias of st_geometrytype
    if let Err(e) = register_blob_to_text(conn, "st_geometry_type_batch") {
        eprintln!("[shim-scalars] skipping `st_geometry_type_batch`: {e}");
    } // alias of st_geometrytype
    if let Err(e) = register_blob_to_blob(conn, "st_geomfromewkb") {
        eprintln!("[shim-scalars] skipping `st_geomfromewkb`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_geom_from_ewkb") {
        eprintln!("[shim-scalars] skipping `st_geom_from_ewkb`: {e}");
    } // alias of st_geomfromewkb
    if let Err(e) = register_blob_to_blob(conn, "st_geomfromgeobuf") {
        eprintln!("[shim-scalars] skipping `st_geomfromgeobuf`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_geom_from_geobuf") {
        eprintln!("[shim-scalars] skipping `st_geom_from_geobuf`: {e}");
    } // alias of st_geomfromgeobuf
    if let Err(e) = register_text_to_blob(conn, "st_geomfromgeohash") {
        eprintln!("[shim-scalars] skipping `st_geomfromgeohash`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_box2dfromgeohash") {
        eprintln!("[shim-scalars] skipping `st_box2dfromgeohash`: {e}");
    } // alias of st_geomfromgeohash
    if let Err(e) = register_text_to_blob(conn, "st_geom_from_geohash") {
        eprintln!("[shim-scalars] skipping `st_geom_from_geohash`: {e}");
    } // alias of st_geomfromgeohash
    if let Err(e) = register_text_i32_to_blob(conn, "st_geomfromgeohash") {
        eprintln!("[shim-scalars] skipping `st_geomfromgeohash`: {e}");
    }
    if let Err(e) = register_text_i32_to_blob(conn, "st_box2dfromgeohash") {
        eprintln!("[shim-scalars] skipping `st_box2dfromgeohash`: {e}");
    } // alias of st_geomfromgeohash
    if let Err(e) = register_text_i32_to_blob(conn, "st_geom_from_geohash") {
        eprintln!("[shim-scalars] skipping `st_geom_from_geohash`: {e}");
    } // alias of st_geomfromgeohash
    if let Err(e) = register_text_to_blob(conn, "st_geomfromgeojson") {
        eprintln!("[shim-scalars] skipping `st_geomfromgeojson`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_geom_from_geojson") {
        eprintln!("[shim-scalars] skipping `st_geom_from_geojson`: {e}");
    } // alias of st_geomfromgeojson
    if let Err(e) = register_text_to_blob(conn, "st_geomfromgml") {
        eprintln!("[shim-scalars] skipping `st_geomfromgml`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_geom_from_gml") {
        eprintln!("[shim-scalars] skipping `st_geom_from_gml`: {e}");
    } // alias of st_geomfromgml
    if let Err(e) = register_text_i32_to_blob(conn, "st_geomfromgml") {
        eprintln!("[shim-scalars] skipping `st_geomfromgml`: {e}");
    }
    if let Err(e) = register_text_i32_to_blob(conn, "st_geom_from_gml") {
        eprintln!("[shim-scalars] skipping `st_geom_from_gml`: {e}");
    } // alias of st_geomfromgml
    if let Err(e) = register_text_i32_to_blob(conn, "st_geomfromgmlsrid") {
        eprintln!("[shim-scalars] skipping `st_geomfromgmlsrid`: {e}");
    }
    if let Err(e) = register_text_i32_to_blob(conn, "st_geom_from_gml_srid") {
        eprintln!("[shim-scalars] skipping `st_geom_from_gml_srid`: {e}");
    } // alias of st_geomfromgmlsrid
    if let Err(e) = register_text_to_blob(conn, "st_geomfromhexewkb") {
        eprintln!("[shim-scalars] skipping `st_geomfromhexewkb`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_geom_from_hexewkb") {
        eprintln!("[shim-scalars] skipping `st_geom_from_hexewkb`: {e}");
    } // alias of st_geomfromhexewkb
    if let Err(e) = register_text_to_blob(conn, "st_geomfromkml") {
        eprintln!("[shim-scalars] skipping `st_geomfromkml`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_geom_from_kml") {
        eprintln!("[shim-scalars] skipping `st_geom_from_kml`: {e}");
    } // alias of st_geomfromkml
    if let Err(e) = register_text_to_blob(conn, "st_geomfrommarc21") {
        eprintln!("[shim-scalars] skipping `st_geomfrommarc21`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_geomfromtext") {
        eprintln!("[shim-scalars] skipping `st_geomfromtext`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_geography_from_text") {
        eprintln!("[shim-scalars] skipping `st_geography_from_text`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_geographyfromtext") {
        eprintln!("[shim-scalars] skipping `st_geographyfromtext`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_geom_from_ewkt") {
        eprintln!("[shim-scalars] skipping `st_geom_from_ewkt`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_geom_from_marc21") {
        eprintln!("[shim-scalars] skipping `st_geom_from_marc21`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_geom_from_text") {
        eprintln!("[shim-scalars] skipping `st_geom_from_text`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_geomcoll_from_text") {
        eprintln!("[shim-scalars] skipping `st_geomcoll_from_text`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_geomcollfromtext") {
        eprintln!("[shim-scalars] skipping `st_geomcollfromtext`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_geometry_from_text") {
        eprintln!("[shim-scalars] skipping `st_geometry_from_text`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_geometryfromtext") {
        eprintln!("[shim-scalars] skipping `st_geometryfromtext`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_geomfromewkt") {
        eprintln!("[shim-scalars] skipping `st_geomfromewkt`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_line_from_text") {
        eprintln!("[shim-scalars] skipping `st_line_from_text`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_linefromtext") {
        eprintln!("[shim-scalars] skipping `st_linefromtext`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_mline_from_text") {
        eprintln!("[shim-scalars] skipping `st_mline_from_text`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_mlinefromtext") {
        eprintln!("[shim-scalars] skipping `st_mlinefromtext`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_mpoint_from_text") {
        eprintln!("[shim-scalars] skipping `st_mpoint_from_text`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_mpointfromtext") {
        eprintln!("[shim-scalars] skipping `st_mpointfromtext`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_mpoly_from_text") {
        eprintln!("[shim-scalars] skipping `st_mpoly_from_text`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_mpolyfromtext") {
        eprintln!("[shim-scalars] skipping `st_mpolyfromtext`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_point_from_text") {
        eprintln!("[shim-scalars] skipping `st_point_from_text`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_pointfromtext") {
        eprintln!("[shim-scalars] skipping `st_pointfromtext`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_polygon_from_text") {
        eprintln!("[shim-scalars] skipping `st_polygon_from_text`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_polygonfromtext") {
        eprintln!("[shim-scalars] skipping `st_polygonfromtext`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_to_blob(conn, "st_wkttosql") {
        eprintln!("[shim-scalars] skipping `st_wkttosql`: {e}");
    } // alias of st_geomfromtext
    if let Err(e) = register_text_i32_to_blob(conn, "st_geomfromtextsrid") {
        eprintln!("[shim-scalars] skipping `st_geomfromtextsrid`: {e}");
    }
    if let Err(e) = register_text_i32_to_blob(conn, "st_geom_from_text_srid") {
        eprintln!("[shim-scalars] skipping `st_geom_from_text_srid`: {e}");
    } // alias of st_geomfromtextsrid
    if let Err(e) = register_blob_to_blob(conn, "st_geomfromtwkb") {
        eprintln!("[shim-scalars] skipping `st_geomfromtwkb`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_geom_from_twkb") {
        eprintln!("[shim-scalars] skipping `st_geom_from_twkb`: {e}");
    } // alias of st_geomfromtwkb
    if let Err(e) = register_blob_to_blob(conn, "st_geomfromwkb") {
        eprintln!("[shim-scalars] skipping `st_geomfromwkb`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_geom_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_geom_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_to_blob(conn, "st_geomcoll_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_geomcoll_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_to_blob(conn, "st_geomcollfromwkb") {
        eprintln!("[shim-scalars] skipping `st_geomcollfromwkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_to_blob(conn, "st_line_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_line_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_to_blob(conn, "st_linefromwkb") {
        eprintln!("[shim-scalars] skipping `st_linefromwkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_to_blob(conn, "st_mline_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_mline_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_to_blob(conn, "st_mlinefromwkb") {
        eprintln!("[shim-scalars] skipping `st_mlinefromwkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_to_blob(conn, "st_mpoint_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_mpoint_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_to_blob(conn, "st_mpointfromwkb") {
        eprintln!("[shim-scalars] skipping `st_mpointfromwkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_to_blob(conn, "st_mpoly_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_mpoly_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_to_blob(conn, "st_mpolyfromwkb") {
        eprintln!("[shim-scalars] skipping `st_mpolyfromwkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_to_blob(conn, "st_point_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_point_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_to_blob(conn, "st_pointfromwkb") {
        eprintln!("[shim-scalars] skipping `st_pointfromwkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_to_blob(conn, "st_polygon_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_polygon_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_to_blob(conn, "st_polygonfromwkb") {
        eprintln!("[shim-scalars] skipping `st_polygonfromwkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_to_blob(conn, "st_wkbtosql") {
        eprintln!("[shim-scalars] skipping `st_wkbtosql`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_geomfromwkb") {
        eprintln!("[shim-scalars] skipping `st_geomfromwkb`: {e}");
    }
    if let Err(e) = register_blob_i32_to_blob(conn, "st_geom_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_geom_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_geomcoll_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_geomcoll_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_geomcollfromwkb") {
        eprintln!("[shim-scalars] skipping `st_geomcollfromwkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_line_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_line_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_linefromwkb") {
        eprintln!("[shim-scalars] skipping `st_linefromwkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_mline_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_mline_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_mlinefromwkb") {
        eprintln!("[shim-scalars] skipping `st_mlinefromwkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_mpoint_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_mpoint_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_mpointfromwkb") {
        eprintln!("[shim-scalars] skipping `st_mpointfromwkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_mpoly_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_mpoly_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_mpolyfromwkb") {
        eprintln!("[shim-scalars] skipping `st_mpolyfromwkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_point_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_point_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_pointfromwkb") {
        eprintln!("[shim-scalars] skipping `st_pointfromwkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_polygon_from_wkb") {
        eprintln!("[shim-scalars] skipping `st_polygon_from_wkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_polygonfromwkb") {
        eprintln!("[shim-scalars] skipping `st_polygonfromwkb`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_i32_to_blob(conn, "st_wkbtosql") {
        eprintln!("[shim-scalars] skipping `st_wkbtosql`: {e}");
    } // alias of st_geomfromwkb
    if let Err(e) = register_blob_blob_f64_to_u32(conn, "st_getedgebypoint") {
        eprintln!("[shim-scalars] skipping `st_getedgebypoint`: {e}");
    }
    if let Err(e) = register_blob_blob_f64_to_u32(conn, "st_get_edge_by_point") {
        eprintln!("[shim-scalars] skipping `st_get_edge_by_point`: {e}");
    } // alias of st_getedgebypoint
    if let Err(e) = register_blob_blob_f64_to_u32(conn, "topology_get_edge_by_point") {
        eprintln!("[shim-scalars] skipping `topology_get_edge_by_point`: {e}");
    } // alias of st_getedgebypoint
    if let Err(e) = register_blob_u32_to_blob(conn, "st_getedgegeometry") {
        eprintln!("[shim-scalars] skipping `st_getedgegeometry`: {e}");
    }
    if let Err(e) = register_blob_u32_to_blob(conn, "st_get_edge_geometry") {
        eprintln!("[shim-scalars] skipping `st_get_edge_geometry`: {e}");
    } // alias of st_getedgegeometry
    if let Err(e) = register_blob_u32_to_blob(conn, "topology_get_edge_geometry") {
        eprintln!("[shim-scalars] skipping `topology_get_edge_geometry`: {e}");
    } // alias of st_getedgegeometry
    if let Err(e) = register_blob_u32_to_blob(conn, "st_getfacegeometry") {
        eprintln!("[shim-scalars] skipping `st_getfacegeometry`: {e}");
    }
    if let Err(e) = register_blob_u32_to_blob(conn, "st_get_face_geometry") {
        eprintln!("[shim-scalars] skipping `st_get_face_geometry`: {e}");
    } // alias of st_getfacegeometry
    if let Err(e) = register_blob_u32_to_blob(conn, "topology_get_face_geometry") {
        eprintln!("[shim-scalars] skipping `topology_get_face_geometry`: {e}");
    } // alias of st_getfacegeometry
    if let Err(e) = register_blob_blob_f64_to_u32(conn, "st_getnodebypoint") {
        eprintln!("[shim-scalars] skipping `st_getnodebypoint`: {e}");
    }
    if let Err(e) = register_blob_blob_f64_to_u32(conn, "st_get_node_by_point") {
        eprintln!("[shim-scalars] skipping `st_get_node_by_point`: {e}");
    } // alias of st_getnodebypoint
    if let Err(e) = register_blob_blob_f64_to_u32(conn, "topology_get_node_by_point") {
        eprintln!("[shim-scalars] skipping `topology_get_node_by_point`: {e}");
    } // alias of st_getnodebypoint
    if let Err(e) = register_blob_u32_to_blob(conn, "st_getnodegeometry") {
        eprintln!("[shim-scalars] skipping `st_getnodegeometry`: {e}");
    }
    if let Err(e) = register_blob_u32_to_blob(conn, "st_get_node_geometry") {
        eprintln!("[shim-scalars] skipping `st_get_node_geometry`: {e}");
    } // alias of st_getnodegeometry
    if let Err(e) = register_blob_u32_to_blob(conn, "topology_get_node_geometry") {
        eprintln!("[shim-scalars] skipping `topology_get_node_geometry`: {e}");
    } // alias of st_getnodegeometry
    if let Err(e) = register_text_to_blob(conn, "st_gmltosql") {
        eprintln!("[shim-scalars] skipping `st_gmltosql`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_gml_to_sql") {
        eprintln!("[shim-scalars] skipping `st_gml_to_sql`: {e}");
    } // alias of st_gmltosql
    if let Err(e) = register_blob_to_bool(conn, "st_hasarc") {
        eprintln!("[shim-scalars] skipping `st_hasarc`: {e}");
    }
    if let Err(e) = register_blob_to_bool(conn, "st_has_arc") {
        eprintln!("[shim-scalars] skipping `st_has_arc`: {e}");
    } // alias of st_hasarc
    if let Err(e) = register_blob_to_bool(conn, "st_hasm") {
        eprintln!("[shim-scalars] skipping `st_hasm`: {e}");
    }
    if let Err(e) = register_blob_to_bool(conn, "st_has_m") {
        eprintln!("[shim-scalars] skipping `st_has_m`: {e}");
    } // alias of st_hasm
    if let Err(e) = register_blob_u32_to_bool(conn, "st_hasnoband") {
        eprintln!("[shim-scalars] skipping `st_hasnoband`: {e}");
    }
    if let Err(e) = register_blob_u32_to_bool(conn, "st_has_no_band") {
        eprintln!("[shim-scalars] skipping `st_has_no_band`: {e}");
    } // alias of st_hasnoband
    if let Err(e) = register_blob_to_bool(conn, "st_hasz") {
        eprintln!("[shim-scalars] skipping `st_hasz`: {e}");
    }
    if let Err(e) = register_blob_to_bool(conn, "st_has_z") {
        eprintln!("[shim-scalars] skipping `st_has_z`: {e}");
    } // alias of st_hasz
    if let Err(e) = register_blob_blob_to_f64(conn, "st_hausdorffdistance") {
        eprintln!("[shim-scalars] skipping `st_hausdorffdistance`: {e}");
    }
    if let Err(e) = register_blob_blob_to_f64(conn, "st_hausdorff_distance") {
        eprintln!("[shim-scalars] skipping `st_hausdorff_distance`: {e}");
    } // alias of st_hausdorffdistance
    if let Err(e) = register_blob_to_u32(conn, "st_height") {
        eprintln!("[shim-scalars] skipping `st_height`: {e}");
    }
    if let Err(e) = register_blob_to_u32(conn, "st_rast_height_dfer_batch") {
        eprintln!("[shim-scalars] skipping `st_rast_height_dfer_batch`: {e}");
    } // alias of st_height
    if let Err(e) = register_blob_to_u32(conn, "st_rastheight") {
        eprintln!("[shim-scalars] skipping `st_rastheight`: {e}");
    } // alias of st_height
    if let Err(e) = register_f64_i32_i32_to_blob(conn, "st_hexagon") {
        eprintln!("[shim-scalars] skipping `st_hexagon`: {e}");
    }
    if let Err(e) = register_blob_u32_f64_f64_to_blob(conn, "st_hillshade") {
        eprintln!("[shim-scalars] skipping `st_hillshade`: {e}");
    }
    if let Err(e) = register_blob_u32_f64_f64_to_blob(conn, "st_hill_shade") {
        eprintln!("[shim-scalars] skipping `st_hill_shade`: {e}");
    } // alias of st_hillshade
    if let Err(e) = register_blob_u32_u32_to_text(conn, "st_histogram") {
        eprintln!("[shim-scalars] skipping `st_histogram`: {e}");
    }
    if let Err(e) = register_blob_i32_to_blob(conn, "st_interiorringn") {
        eprintln!("[shim-scalars] skipping `st_interiorringn`: {e}");
    }
    if let Err(e) = register_blob_i32_to_blob(conn, "st_interior_ring_n") {
        eprintln!("[shim-scalars] skipping `st_interior_ring_n`: {e}");
    } // alias of st_interiorringn
    if let Err(e) = register_blob_blob_to_f64(conn, "st_interpolatepoint") {
        eprintln!("[shim-scalars] skipping `st_interpolatepoint`: {e}");
    }
    if let Err(e) = register_blob_blob_to_f64(conn, "st_interpolate_m") {
        eprintln!("[shim-scalars] skipping `st_interpolate_m`: {e}");
    } // alias of st_interpolatepoint
    if let Err(e) = register_blob_blob_to_f64(conn, "st_interpolate_point") {
        eprintln!("[shim-scalars] skipping `st_interpolate_point`: {e}");
    } // alias of st_interpolatepoint
    if let Err(e) = register_blob_blob_to_blob(conn, "st_intersection") {
        eprintln!("[shim-scalars] skipping `st_intersection`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_clip_by_box2d") {
        eprintln!("[shim-scalars] skipping `st_clip_by_box2d`: {e}");
    } // alias of st_intersection
    if let Err(e) = register_blob_blob_to_blob(conn, "st_geog_intersection") {
        eprintln!("[shim-scalars] skipping `st_geog_intersection`: {e}");
    } // alias of st_intersection
    if let Err(e) = register_blob_blob_to_blob(conn, "st_intersection_batch") {
        eprintln!("[shim-scalars] skipping `st_intersection_batch`: {e}");
    } // alias of st_intersection
    if let Err(e) = register_blob_blob_to_blob(conn, "st_intersectionthreed") {
        eprintln!("[shim-scalars] skipping `st_intersectionthreed`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_3dintersection") {
        eprintln!("[shim-scalars] skipping `st_3dintersection`: {e}");
    } // alias of st_intersectionthreed
    if let Err(e) = register_blob_blob_to_blob(conn, "st_intersection_threed") {
        eprintln!("[shim-scalars] skipping `st_intersection_threed`: {e}");
    } // alias of st_intersectionthreed
    if let Err(e) = register_blob_blob_to_bool(conn, "st_intersects") {
        eprintln!("[shim-scalars] skipping `st_intersects`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_intersects_batch") {
        eprintln!("[shim-scalars] skipping `st_intersects_batch`: {e}");
    } // alias of st_intersects
    if let Err(e) = register_blob_text_to_blob(conn, "st_inversetransformpipeline") {
        eprintln!("[shim-scalars] skipping `st_inversetransformpipeline`: {e}");
    }
    if let Err(e) = register_blob_text_to_blob(conn, "st_inverse_transform_pipeline") {
        eprintln!("[shim-scalars] skipping `st_inverse_transform_pipeline`: {e}");
    } // alias of st_inversetransformpipeline
    if let Err(e) = register_blob_to_bool(conn, "st_isclosed") {
        eprintln!("[shim-scalars] skipping `st_isclosed`: {e}");
    }
    if let Err(e) = register_blob_to_bool(conn, "st_is_closed") {
        eprintln!("[shim-scalars] skipping `st_is_closed`: {e}");
    } // alias of st_isclosed
    if let Err(e) = register_blob_to_bool(conn, "st_iscollection") {
        eprintln!("[shim-scalars] skipping `st_iscollection`: {e}");
    }
    if let Err(e) = register_blob_to_bool(conn, "st_is_collection") {
        eprintln!("[shim-scalars] skipping `st_is_collection`: {e}");
    } // alias of st_iscollection
    if let Err(e) = register_blob_to_bool(conn, "st_isempty") {
        eprintln!("[shim-scalars] skipping `st_isempty`: {e}");
    }
    if let Err(e) = register_blob_to_bool(conn, "st_is_empty") {
        eprintln!("[shim-scalars] skipping `st_is_empty`: {e}");
    } // alias of st_isempty
    if let Err(e) = register_blob_to_bool(conn, "st_is_empty_batch") {
        eprintln!("[shim-scalars] skipping `st_is_empty_batch`: {e}");
    } // alias of st_isempty
    if let Err(e) = register_blob_to_bool(conn, "st_ispolygonccw") {
        eprintln!("[shim-scalars] skipping `st_ispolygonccw`: {e}");
    }
    if let Err(e) = register_blob_to_bool(conn, "st_is_polygon_ccw") {
        eprintln!("[shim-scalars] skipping `st_is_polygon_ccw`: {e}");
    } // alias of st_ispolygonccw
    if let Err(e) = register_blob_to_bool(conn, "st_ispolygoncw") {
        eprintln!("[shim-scalars] skipping `st_ispolygoncw`: {e}");
    }
    if let Err(e) = register_blob_to_bool(conn, "st_is_polygon_cw") {
        eprintln!("[shim-scalars] skipping `st_is_polygon_cw`: {e}");
    } // alias of st_ispolygoncw
    if let Err(e) = register_blob_to_bool(conn, "st_isring") {
        eprintln!("[shim-scalars] skipping `st_isring`: {e}");
    }
    if let Err(e) = register_blob_to_bool(conn, "st_is_ring") {
        eprintln!("[shim-scalars] skipping `st_is_ring`: {e}");
    } // alias of st_isring
    if let Err(e) = register_blob_to_bool(conn, "st_issimple") {
        eprintln!("[shim-scalars] skipping `st_issimple`: {e}");
    }
    if let Err(e) = register_blob_to_bool(conn, "st_is_simple") {
        eprintln!("[shim-scalars] skipping `st_is_simple`: {e}");
    } // alias of st_issimple
    if let Err(e) = register_blob_to_bool(conn, "st_is_simple_batch") {
        eprintln!("[shim-scalars] skipping `st_is_simple_batch`: {e}");
    } // alias of st_issimple
    if let Err(e) = register_blob_to_bool(conn, "st_isvalid") {
        eprintln!("[shim-scalars] skipping `st_isvalid`: {e}");
    }
    if let Err(e) = register_blob_to_bool(conn, "st_is_valid") {
        eprintln!("[shim-scalars] skipping `st_is_valid`: {e}");
    } // alias of st_isvalid
    if let Err(e) = register_blob_to_bool(conn, "st_is_valid_batch") {
        eprintln!("[shim-scalars] skipping `st_is_valid_batch`: {e}");
    } // alias of st_isvalid
    if let Err(e) = register_blob_to_text(conn, "st_isvaliddetail") {
        eprintln!("[shim-scalars] skipping `st_isvaliddetail`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_is_valid_detail") {
        eprintln!("[shim-scalars] skipping `st_is_valid_detail`: {e}");
    } // alias of st_isvaliddetail
    if let Err(e) = register_blob_to_text(conn, "st_isvalidreason") {
        eprintln!("[shim-scalars] skipping `st_isvalidreason`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_is_valid_reason") {
        eprintln!("[shim-scalars] skipping `st_is_valid_reason`: {e}");
    } // alias of st_isvalidreason
    if let Err(e) = register_blob_to_bool(conn, "st_isvalidtrajectory") {
        eprintln!("[shim-scalars] skipping `st_isvalidtrajectory`: {e}");
    }
    if let Err(e) = register_blob_to_bool(conn, "st_is_valid_trajectory") {
        eprintln!("[shim-scalars] skipping `st_is_valid_trajectory`: {e}");
    } // alias of st_isvalidtrajectory
    if let Err(e) = register_blob_blob_to_f64(conn, "st_knndistance") {
        eprintln!("[shim-scalars] skipping `st_knndistance`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_largestemptycircle") {
        eprintln!("[shim-scalars] skipping `st_largestemptycircle`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_largest_empty_circle") {
        eprintln!("[shim-scalars] skipping `st_largest_empty_circle`: {e}");
    } // alias of st_largestemptycircle
    if let Err(e) = register_blob_f64_to_blob(conn, "st_largestemptycircle") {
        eprintln!("[shim-scalars] skipping `st_largestemptycircle`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_largest_empty_circle") {
        eprintln!("[shim-scalars] skipping `st_largest_empty_circle`: {e}");
    } // alias of st_largestemptycircle
    if let Err(e) = register_blob_to_f64(conn, "st_length") {
        eprintln!("[shim-scalars] skipping `st_length`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_length2d") {
        eprintln!("[shim-scalars] skipping `st_length2d`: {e}");
    } // alias of st_length
    if let Err(e) = register_blob_to_f64(conn, "st_length_2d") {
        eprintln!("[shim-scalars] skipping `st_length_2d`: {e}");
    } // alias of st_length
    if let Err(e) = register_blob_to_f64(conn, "st_length_batch") {
        eprintln!("[shim-scalars] skipping `st_length_batch`: {e}");
    } // alias of st_length
    if let Err(e) = register_blob_to_f64(conn, "st_length_twod") {
        eprintln!("[shim-scalars] skipping `st_length_twod`: {e}");
    } // alias of st_length
    if let Err(e) = register_blob_to_f64(conn, "st_lengthspheroid") {
        eprintln!("[shim-scalars] skipping `st_lengthspheroid`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_length_spheroid") {
        eprintln!("[shim-scalars] skipping `st_length_spheroid`: {e}");
    } // alias of st_lengthspheroid
    if let Err(e) = register_blob_to_f64(conn, "st_lengththreed") {
        eprintln!("[shim-scalars] skipping `st_lengththreed`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_3dlength") {
        eprintln!("[shim-scalars] skipping `st_3dlength`: {e}");
    } // alias of st_lengththreed
    if let Err(e) = register_blob_to_f64(conn, "st_length_threed") {
        eprintln!("[shim-scalars] skipping `st_length_threed`: {e}");
    } // alias of st_lengththreed
    if let Err(e) = register_blob_text_to_blob(conn, "st_letters") {
        eprintln!("[shim-scalars] skipping `st_letters`: {e}");
    }
    if let Err(e) = register_blob_blob_to_i32(conn, "st_linecrossingdirection") {
        eprintln!("[shim-scalars] skipping `st_linecrossingdirection`: {e}");
    }
    if let Err(e) = register_blob_blob_to_i32(conn, "st_line_crossing_direction") {
        eprintln!("[shim-scalars] skipping `st_line_crossing_direction`: {e}");
    } // alias of st_linecrossingdirection
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_lineextend") {
        eprintln!("[shim-scalars] skipping `st_lineextend`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_line_extend") {
        eprintln!("[shim-scalars] skipping `st_line_extend`: {e}");
    } // alias of st_lineextend
    if let Err(e) = register_text_to_blob(conn, "st_linefromencodedpolyline") {
        eprintln!("[shim-scalars] skipping `st_linefromencodedpolyline`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_line_from_encoded_polyline") {
        eprintln!("[shim-scalars] skipping `st_line_from_encoded_polyline`: {e}");
    } // alias of st_linefromencodedpolyline
    if let Err(e) = register_text_i32_to_blob(conn, "st_linefromencodedpolyline") {
        eprintln!("[shim-scalars] skipping `st_linefromencodedpolyline`: {e}");
    }
    if let Err(e) = register_text_i32_to_blob(conn, "st_line_from_encoded_polyline") {
        eprintln!("[shim-scalars] skipping `st_line_from_encoded_polyline`: {e}");
    } // alias of st_linefromencodedpolyline
    if let Err(e) = register_blob_to_blob(conn, "st_linefrommultipoint") {
        eprintln!("[shim-scalars] skipping `st_linefrommultipoint`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_line_from_multi_point") {
        eprintln!("[shim-scalars] skipping `st_line_from_multi_point`: {e}");
    } // alias of st_linefrommultipoint
    if let Err(e) = register_blob_f64_to_blob(conn, "st_lineinterpolatepoint") {
        eprintln!("[shim-scalars] skipping `st_lineinterpolatepoint`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_line_interpolate_point") {
        eprintln!("[shim-scalars] skipping `st_line_interpolate_point`: {e}");
    } // alias of st_lineinterpolatepoint
    if let Err(e) = register_blob_f64_bool_to_blob(conn, "st_lineinterpolatepoints") {
        eprintln!("[shim-scalars] skipping `st_lineinterpolatepoints`: {e}");
    }
    if let Err(e) = register_blob_f64_bool_to_blob(conn, "st_line_interpolate_points") {
        eprintln!("[shim-scalars] skipping `st_line_interpolate_points`: {e}");
    } // alias of st_lineinterpolatepoints
    if let Err(e) = register_blob_blob_to_f64(conn, "st_linelocatepoint") {
        eprintln!("[shim-scalars] skipping `st_linelocatepoint`: {e}");
    }
    if let Err(e) = register_blob_blob_to_f64(conn, "st_line_locate_point") {
        eprintln!("[shim-scalars] skipping `st_line_locate_point`: {e}");
    } // alias of st_linelocatepoint
    if let Err(e) = register_blob_to_blob(conn, "st_linemerge") {
        eprintln!("[shim-scalars] skipping `st_linemerge`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_line_merge") {
        eprintln!("[shim-scalars] skipping `st_line_merge`: {e}");
    } // alias of st_linemerge
    if let Err(e) = register_blob_to_blob(conn, "st_line_merge_directed") {
        eprintln!("[shim-scalars] skipping `st_line_merge_directed`: {e}");
    } // alias of st_linemerge
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_linesubstring") {
        eprintln!("[shim-scalars] skipping `st_linesubstring`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_geog_line_substring") {
        eprintln!("[shim-scalars] skipping `st_geog_line_substring`: {e}");
    } // alias of st_linesubstring
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_line_substring") {
        eprintln!("[shim-scalars] skipping `st_line_substring`: {e}");
    } // alias of st_linesubstring
    if let Err(e) = register_blob_to_blob(conn, "st_linetocurve") {
        eprintln!("[shim-scalars] skipping `st_linetocurve`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_line_to_curve") {
        eprintln!("[shim-scalars] skipping `st_line_to_curve`: {e}");
    } // alias of st_linetocurve
    if let Err(e) = register_blob_f64_to_blob(conn, "st_locatealong") {
        eprintln!("[shim-scalars] skipping `st_locatealong`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_locate_along") {
        eprintln!("[shim-scalars] skipping `st_locate_along`: {e}");
    } // alias of st_locatealong
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_locatebetween") {
        eprintln!("[shim-scalars] skipping `st_locatebetween`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_locate_between") {
        eprintln!("[shim-scalars] skipping `st_locate_between`: {e}");
    } // alias of st_locatebetween
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_locatebetweenelevations") {
        eprintln!("[shim-scalars] skipping `st_locatebetweenelevations`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_locate_between_elevations") {
        eprintln!("[shim-scalars] skipping `st_locate_between_elevations`: {e}");
    } // alias of st_locatebetweenelevations
    if let Err(e) = register_blob_blob_to_blob(conn, "st_longestline") {
        eprintln!("[shim-scalars] skipping `st_longestline`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_longest_line") {
        eprintln!("[shim-scalars] skipping `st_longest_line`: {e}");
    } // alias of st_longestline
    if let Err(e) = register_blob_to_f64(conn, "st_m") {
        eprintln!("[shim-scalars] skipping `st_m`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_m_batch") {
        eprintln!("[shim-scalars] skipping `st_m_batch`: {e}");
    } // alias of st_m
    if let Err(e) = register_blob_blob_to_blob(conn, "st_makebox2d") {
        eprintln!("[shim-scalars] skipping `st_makebox2d`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_make_box2d") {
        eprintln!("[shim-scalars] skipping `st_make_box2d`: {e}");
    } // alias of st_makebox2d
    if let Err(e) = register_blob_blob_to_blob(conn, "st_makebox") {
        eprintln!("[shim-scalars] skipping `st_makebox`: {e}");
    } // alias of st_makebox2d
    if let Err(e) = register_blob_blob_to_blob(conn, "st_makebox3d") {
        eprintln!("[shim-scalars] skipping `st_makebox3d`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_3d_make_box") {
        eprintln!("[shim-scalars] skipping `st_3d_make_box`: {e}");
    } // alias of st_makebox3d
    if let Err(e) = register_blob_blob_to_blob(conn, "st_3dmakebox") {
        eprintln!("[shim-scalars] skipping `st_3dmakebox`: {e}");
    } // alias of st_makebox3d
    if let Err(e) = register_blob_blob_to_blob(conn, "st_make_box3d") {
        eprintln!("[shim-scalars] skipping `st_make_box3d`: {e}");
    } // alias of st_makebox3d
    if let Err(e) = register_u32_u32_to_blob(conn, "st_makeemptyraster") {
        eprintln!("[shim-scalars] skipping `st_makeemptyraster`: {e}");
    }
    if let Err(e) = register_u32_u32_to_blob(conn, "st_make_empty_raster") {
        eprintln!("[shim-scalars] skipping `st_make_empty_raster`: {e}");
    } // alias of st_makeemptyraster
    if let Err(e) = register_f64_f64_f64_f64_to_blob(conn, "st_makeenvelope") {
        eprintln!("[shim-scalars] skipping `st_makeenvelope`: {e}");
    }
    if let Err(e) = register_f64_f64_f64_f64_to_blob(conn, "st_make_envelope") {
        eprintln!("[shim-scalars] skipping `st_make_envelope`: {e}");
    } // alias of st_makeenvelope
    if let Err(e) = register_f64_f64_f64_f64_i32_to_blob(conn, "st_makeenvelope") {
        eprintln!("[shim-scalars] skipping `st_makeenvelope`: {e}");
    }
    if let Err(e) = register_f64_f64_f64_f64_i32_to_blob(conn, "st_make_envelope") {
        eprintln!("[shim-scalars] skipping `st_make_envelope`: {e}");
    } // alias of st_makeenvelope
    if let Err(e) = register_f64_f64_f64_f64_i32_to_blob(conn, "st_makeenvelopesrid") {
        eprintln!("[shim-scalars] skipping `st_makeenvelopesrid`: {e}");
    }
    if let Err(e) = register_f64_f64_f64_f64_i32_to_blob(conn, "st_make_envelope_srid") {
        eprintln!("[shim-scalars] skipping `st_make_envelope_srid`: {e}");
    } // alias of st_makeenvelopesrid
    if let Err(e) = register_f64_f64_to_blob(conn, "st_makepoint") {
        eprintln!("[shim-scalars] skipping `st_makepoint`: {e}");
    }
    if let Err(e) = register_f64_f64_to_blob(conn, "st_make_point") {
        eprintln!("[shim-scalars] skipping `st_make_point`: {e}");
    } // alias of st_makepoint
    if let Err(e) = register_f64_f64_to_blob(conn, "st_point") {
        eprintln!("[shim-scalars] skipping `st_point`: {e}");
    } // alias of st_makepoint
    if let Err(e) = register_f64_f64_f64_to_blob(conn, "st_makepointm") {
        eprintln!("[shim-scalars] skipping `st_makepointm`: {e}");
    }
    if let Err(e) = register_f64_f64_f64_to_blob(conn, "st_make_point_m") {
        eprintln!("[shim-scalars] skipping `st_make_point_m`: {e}");
    } // alias of st_makepointm
    if let Err(e) = register_f64_f64_f64_to_blob(conn, "st_point_m") {
        eprintln!("[shim-scalars] skipping `st_point_m`: {e}");
    } // alias of st_makepointm
    if let Err(e) = register_f64_f64_f64_to_blob(conn, "st_pointm") {
        eprintln!("[shim-scalars] skipping `st_pointm`: {e}");
    } // alias of st_makepointm
    if let Err(e) = register_f64_f64_f64_to_blob(conn, "st_makepointz") {
        eprintln!("[shim-scalars] skipping `st_makepointz`: {e}");
    }
    if let Err(e) = register_f64_f64_f64_to_blob(conn, "st_make_point_z") {
        eprintln!("[shim-scalars] skipping `st_make_point_z`: {e}");
    } // alias of st_makepointz
    if let Err(e) = register_f64_f64_f64_to_blob(conn, "st_point_z") {
        eprintln!("[shim-scalars] skipping `st_point_z`: {e}");
    } // alias of st_makepointz
    if let Err(e) = register_f64_f64_f64_to_blob(conn, "st_pointz") {
        eprintln!("[shim-scalars] skipping `st_pointz`: {e}");
    } // alias of st_makepointz
    if let Err(e) = register_f64_f64_f64_f64_to_blob(conn, "st_makepointzm") {
        eprintln!("[shim-scalars] skipping `st_makepointzm`: {e}");
    }
    if let Err(e) = register_f64_f64_f64_f64_to_blob(conn, "st_make_point_zm") {
        eprintln!("[shim-scalars] skipping `st_make_point_zm`: {e}");
    } // alias of st_makepointzm
    if let Err(e) = register_f64_f64_f64_f64_to_blob(conn, "st_point_zm") {
        eprintln!("[shim-scalars] skipping `st_point_zm`: {e}");
    } // alias of st_makepointzm
    if let Err(e) = register_f64_f64_f64_f64_to_blob(conn, "st_pointzm") {
        eprintln!("[shim-scalars] skipping `st_pointzm`: {e}");
    } // alias of st_makepointzm
    if let Err(e) = register_blob_to_blob(conn, "st_makepolygon") {
        eprintln!("[shim-scalars] skipping `st_makepolygon`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_make_polygon") {
        eprintln!("[shim-scalars] skipping `st_make_polygon`: {e}");
    } // alias of st_makepolygon
    if let Err(e) = register_blob_to_blob(conn, "st_makevalid") {
        eprintln!("[shim-scalars] skipping `st_makevalid`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_make_valid") {
        eprintln!("[shim-scalars] skipping `st_make_valid`: {e}");
    } // alias of st_makevalid
    if let Err(e) = register_blob_u32_text_text_to_blob(conn, "st_mapalgebra") {
        eprintln!("[shim-scalars] skipping `st_mapalgebra`: {e}");
    }
    if let Err(e) = register_blob_u32_text_text_to_blob(conn, "st_map_algebra") {
        eprintln!("[shim-scalars] skipping `st_map_algebra`: {e}");
    } // alias of st_mapalgebra
    if let Err(e) = register_blob_u32_blob_u32_text_text_to_blob(conn, "st_mapalgebra2") {
        eprintln!("[shim-scalars] skipping `st_mapalgebra2`: {e}");
    }
    if let Err(e) = register_blob_u32_blob_u32_text_text_to_blob(conn, "st_map_algebra2") {
        eprintln!("[shim-scalars] skipping `st_map_algebra2`: {e}");
    } // alias of st_mapalgebra2
    if let Err(e) = register_blob_blob_to_f64(conn, "st_maxdistance") {
        eprintln!("[shim-scalars] skipping `st_maxdistance`: {e}");
    }
    if let Err(e) = register_blob_blob_to_f64(conn, "st_max_distance") {
        eprintln!("[shim-scalars] skipping `st_max_distance`: {e}");
    } // alias of st_maxdistance
    if let Err(e) = register_blob_to_blob(conn, "st_maximuminscribedcircle") {
        eprintln!("[shim-scalars] skipping `st_maximuminscribedcircle`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_maximum_inscribed_circle") {
        eprintln!("[shim-scalars] skipping `st_maximum_inscribed_circle`: {e}");
    } // alias of st_maximuminscribedcircle
    if let Err(e) = register_blob_to_i32(conn, "st_memsize") {
        eprintln!("[shim-scalars] skipping `st_memsize`: {e}");
    }
    if let Err(e) = register_blob_to_i32(conn, "st_mem_size") {
        eprintln!("[shim-scalars] skipping `st_mem_size`: {e}");
    } // alias of st_memsize
    if let Err(e) = register_blob_to_blob(conn, "st_minimumboundingcircle") {
        eprintln!("[shim-scalars] skipping `st_minimumboundingcircle`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_minimum_bounding_circle") {
        eprintln!("[shim-scalars] skipping `st_minimum_bounding_circle`: {e}");
    } // alias of st_minimumboundingcircle
    if let Err(e) = register_blob_to_f64(conn, "st_minimumboundingradius") {
        eprintln!("[shim-scalars] skipping `st_minimumboundingradius`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_minimum_bounding_radius") {
        eprintln!("[shim-scalars] skipping `st_minimum_bounding_radius`: {e}");
    } // alias of st_minimumboundingradius
    if let Err(e) = register_blob_to_f64(conn, "st_minimumclearance") {
        eprintln!("[shim-scalars] skipping `st_minimumclearance`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_minimum_clearance") {
        eprintln!("[shim-scalars] skipping `st_minimum_clearance`: {e}");
    } // alias of st_minimumclearance
    if let Err(e) = register_blob_to_blob(conn, "st_minimumclearanceline") {
        eprintln!("[shim-scalars] skipping `st_minimumclearanceline`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_minimum_clearance_line") {
        eprintln!("[shim-scalars] skipping `st_minimum_clearance_line`: {e}");
    } // alias of st_minimumclearanceline
    if let Err(e) = register_blob_blob_to_blob(conn, "st_minkowskisum") {
        eprintln!("[shim-scalars] skipping `st_minkowskisum`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_minkowski_sum") {
        eprintln!("[shim-scalars] skipping `st_minkowski_sum`: {e}");
    } // alias of st_minkowskisum
    if let Err(e) = register_blob_to_f64(conn, "st_mmax") {
        eprintln!("[shim-scalars] skipping `st_mmax`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_mmin") {
        eprintln!("[shim-scalars] skipping `st_mmin`: {e}");
    }
    if let Err(e) = register_blob_u32_u32_to_blob(conn, "st_modedgeheal") {
        eprintln!("[shim-scalars] skipping `st_modedgeheal`: {e}");
    }
    if let Err(e) = register_blob_u32_u32_to_blob(conn, "st_mod_edge_heal") {
        eprintln!("[shim-scalars] skipping `st_mod_edge_heal`: {e}");
    } // alias of st_modedgeheal
    if let Err(e) = register_blob_u32_u32_to_blob(conn, "topology_mod_edge_heal") {
        eprintln!("[shim-scalars] skipping `topology_mod_edge_heal`: {e}");
    } // alias of st_modedgeheal
    if let Err(e) = register_blob_u32_u32_to_text(conn, "st_modedgeheal2") {
        eprintln!("[shim-scalars] skipping `st_modedgeheal2`: {e}");
    }
    if let Err(e) = register_blob_u32_u32_to_text(conn, "st_mod_edge_heal2") {
        eprintln!("[shim-scalars] skipping `st_mod_edge_heal2`: {e}");
    } // alias of st_modedgeheal2
    if let Err(e) = register_blob_u32_blob_to_blob(conn, "st_modedgesplit") {
        eprintln!("[shim-scalars] skipping `st_modedgesplit`: {e}");
    }
    if let Err(e) = register_blob_u32_blob_to_blob(conn, "st_mod_edge_split") {
        eprintln!("[shim-scalars] skipping `st_mod_edge_split`: {e}");
    } // alias of st_modedgesplit
    if let Err(e) = register_blob_u32_blob_to_blob(conn, "topology_mod_edge_split") {
        eprintln!("[shim-scalars] skipping `topology_mod_edge_split`: {e}");
    } // alias of st_modedgesplit
    if let Err(e) = register_blob_u32_blob_to_text(conn, "st_modedgesplit2") {
        eprintln!("[shim-scalars] skipping `st_modedgesplit2`: {e}");
    }
    if let Err(e) = register_blob_u32_blob_to_text(conn, "st_mod_edge_split2") {
        eprintln!("[shim-scalars] skipping `st_mod_edge_split2`: {e}");
    } // alias of st_modedgesplit2
    if let Err(e) = register_blob_to_blob(conn, "st_multi") {
        eprintln!("[shim-scalars] skipping `st_multi`: {e}");
    }
    if let Err(e) = register_blob_to_u32(conn, "st_ndims") {
        eprintln!("[shim-scalars] skipping `st_ndims`: {e}");
    }
    if let Err(e) = register_blob_to_u32(conn, "st_n_dims") {
        eprintln!("[shim-scalars] skipping `st_n_dims`: {e}");
    } // alias of st_ndims
    if let Err(e) = register_blob_to_u32(conn, "st_n_dims_batch") {
        eprintln!("[shim-scalars] skipping `st_n_dims_batch`: {e}");
    } // alias of st_ndims
    if let Err(e) = register_blob_u32_u32_u32_to_f64(conn, "st_nearestvalue") {
        eprintln!("[shim-scalars] skipping `st_nearestvalue`: {e}");
    }
    if let Err(e) = register_blob_u32_u32_u32_to_f64(conn, "st_nearest_value") {
        eprintln!("[shim-scalars] skipping `st_nearest_value`: {e}");
    } // alias of st_nearestvalue
    if let Err(e) = register_blob_u32_blob_to_blob(conn, "st_newedgessplit") {
        eprintln!("[shim-scalars] skipping `st_newedgessplit`: {e}");
    }
    if let Err(e) = register_blob_u32_blob_to_blob(conn, "st_new_edges_split") {
        eprintln!("[shim-scalars] skipping `st_new_edges_split`: {e}");
    } // alias of st_newedgessplit
    if let Err(e) = register_blob_u32_blob_to_blob(conn, "topology_new_edges_split") {
        eprintln!("[shim-scalars] skipping `topology_new_edges_split`: {e}");
    } // alias of st_newedgessplit
    if let Err(e) = register_blob_u32_blob_to_text(conn, "st_newedgessplit2") {
        eprintln!("[shim-scalars] skipping `st_newedgessplit2`: {e}");
    }
    if let Err(e) = register_blob_u32_blob_to_text(conn, "st_new_edges_split2") {
        eprintln!("[shim-scalars] skipping `st_new_edges_split2`: {e}");
    } // alias of st_newedgessplit2
    if let Err(e) = register_blob_to_blob(conn, "st_node") {
        eprintln!("[shim-scalars] skipping `st_node`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_normalize") {
        eprintln!("[shim-scalars] skipping `st_normalize`: {e}");
    }
    if let Err(e) = register_text_to_text(conn, "st_normalizeaddress") {
        eprintln!("[shim-scalars] skipping `st_normalizeaddress`: {e}");
    }
    if let Err(e) = register_text_to_text(conn, "st_normalize_address") {
        eprintln!("[shim-scalars] skipping `st_normalize_address`: {e}");
    } // alias of st_normalizeaddress
    if let Err(e) = register_blob_to_u32(conn, "st_npoints") {
        eprintln!("[shim-scalars] skipping `st_npoints`: {e}");
    }
    if let Err(e) = register_blob_to_u32(conn, "st_num_points") {
        eprintln!("[shim-scalars] skipping `st_num_points`: {e}");
    } // alias of st_npoints
    if let Err(e) = register_blob_to_u32(conn, "st_num_points_batch") {
        eprintln!("[shim-scalars] skipping `st_num_points_batch`: {e}");
    } // alias of st_npoints
    if let Err(e) = register_blob_to_u32(conn, "st_numpoints") {
        eprintln!("[shim-scalars] skipping `st_numpoints`: {e}");
    } // alias of st_npoints
    if let Err(e) = register_blob_to_u32(conn, "st_nrings") {
        eprintln!("[shim-scalars] skipping `st_nrings`: {e}");
    }
    if let Err(e) = register_blob_to_u32(conn, "st_numbands") {
        eprintln!("[shim-scalars] skipping `st_numbands`: {e}");
    }
    if let Err(e) = register_blob_to_u32(conn, "st_num_bands") {
        eprintln!("[shim-scalars] skipping `st_num_bands`: {e}");
    } // alias of st_numbands
    if let Err(e) = register_blob_to_u32(conn, "st_rastnumbands") {
        eprintln!("[shim-scalars] skipping `st_rastnumbands`: {e}");
    } // alias of st_numbands
    if let Err(e) = register_blob_to_u32(conn, "st_numgeometries") {
        eprintln!("[shim-scalars] skipping `st_numgeometries`: {e}");
    }
    if let Err(e) = register_blob_to_u32(conn, "st_num_curves") {
        eprintln!("[shim-scalars] skipping `st_num_curves`: {e}");
    } // alias of st_numgeometries
    if let Err(e) = register_blob_to_u32(conn, "st_num_geometries") {
        eprintln!("[shim-scalars] skipping `st_num_geometries`: {e}");
    } // alias of st_numgeometries
    if let Err(e) = register_blob_to_u32(conn, "st_num_geometries_batch") {
        eprintln!("[shim-scalars] skipping `st_num_geometries_batch`: {e}");
    } // alias of st_numgeometries
    if let Err(e) = register_blob_to_u32(conn, "st_numcurves") {
        eprintln!("[shim-scalars] skipping `st_numcurves`: {e}");
    } // alias of st_numgeometries
    if let Err(e) = register_blob_to_u32(conn, "st_numinteriorrings") {
        eprintln!("[shim-scalars] skipping `st_numinteriorrings`: {e}");
    }
    if let Err(e) = register_blob_to_u32(conn, "st_num_interior_rings") {
        eprintln!("[shim-scalars] skipping `st_num_interior_rings`: {e}");
    } // alias of st_numinteriorrings
    if let Err(e) = register_blob_to_u32(conn, "st_numinteriorring") {
        eprintln!("[shim-scalars] skipping `st_numinteriorring`: {e}");
    } // alias of st_numinteriorrings
    if let Err(e) = register_blob_f64_to_blob(conn, "st_offsetcurve") {
        eprintln!("[shim-scalars] skipping `st_offsetcurve`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_offset_curve") {
        eprintln!("[shim-scalars] skipping `st_offset_curve`: {e}");
    } // alias of st_offsetcurve
    if let Err(e) = register_blob_blob_to_bool(conn, "st_orderingequals") {
        eprintln!("[shim-scalars] skipping `st_orderingequals`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_ordering_equals") {
        eprintln!("[shim-scalars] skipping `st_ordering_equals`: {e}");
    } // alias of st_orderingequals
    if let Err(e) = register_blob_to_blob(conn, "st_orientedenvelope") {
        eprintln!("[shim-scalars] skipping `st_orientedenvelope`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_oriented_envelope") {
        eprintln!("[shim-scalars] skipping `st_oriented_envelope`: {e}");
    } // alias of st_orientedenvelope
    if let Err(e) = register_blob_blob_to_bool(conn, "st_overlaps") {
        eprintln!("[shim-scalars] skipping `st_overlaps`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_overlaps_batch") {
        eprintln!("[shim-scalars] skipping `st_overlaps_batch`: {e}");
    } // alias of st_overlaps
    if let Err(e) = register_text_to_text(conn, "st_parseaddress") {
        eprintln!("[shim-scalars] skipping `st_parseaddress`: {e}");
    }
    if let Err(e) = register_text_to_text(conn, "st_parse_address") {
        eprintln!("[shim-scalars] skipping `st_parse_address`: {e}");
    } // alias of st_parseaddress
    if let Err(e) = register_blob_to_f64(conn, "st_perimeter") {
        eprintln!("[shim-scalars] skipping `st_perimeter`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_perimeter2d") {
        eprintln!("[shim-scalars] skipping `st_perimeter2d`: {e}");
    } // alias of st_perimeter
    if let Err(e) = register_blob_to_f64(conn, "st_perimeter_2d") {
        eprintln!("[shim-scalars] skipping `st_perimeter_2d`: {e}");
    } // alias of st_perimeter
    if let Err(e) = register_blob_to_f64(conn, "st_perimeter_batch") {
        eprintln!("[shim-scalars] skipping `st_perimeter_batch`: {e}");
    } // alias of st_perimeter
    if let Err(e) = register_blob_to_f64(conn, "st_perimeter_twod") {
        eprintln!("[shim-scalars] skipping `st_perimeter_twod`: {e}");
    } // alias of st_perimeter
    if let Err(e) = register_blob_to_f64(conn, "st_perimeterthreed") {
        eprintln!("[shim-scalars] skipping `st_perimeterthreed`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_3dperimeter") {
        eprintln!("[shim-scalars] skipping `st_3dperimeter`: {e}");
    } // alias of st_perimeterthreed
    if let Err(e) = register_blob_to_f64(conn, "st_perimeter_threed") {
        eprintln!("[shim-scalars] skipping `st_perimeter_threed`: {e}");
    } // alias of st_perimeterthreed
    if let Err(e) = register_blob_u32_u32_to_blob(conn, "st_pixelascentroid") {
        eprintln!("[shim-scalars] skipping `st_pixelascentroid`: {e}");
    }
    if let Err(e) = register_blob_u32_u32_to_blob(conn, "st_pixel_as_centroid") {
        eprintln!("[shim-scalars] skipping `st_pixel_as_centroid`: {e}");
    } // alias of st_pixelascentroid
    if let Err(e) = register_blob_u32_u32_to_blob(conn, "st_pixelaspoint") {
        eprintln!("[shim-scalars] skipping `st_pixelaspoint`: {e}");
    }
    if let Err(e) = register_blob_u32_u32_to_blob(conn, "st_pixel_as_point") {
        eprintln!("[shim-scalars] skipping `st_pixel_as_point`: {e}");
    } // alias of st_pixelaspoint
    if let Err(e) = register_blob_u32_u32_to_blob(conn, "st_pixelaspolygon") {
        eprintln!("[shim-scalars] skipping `st_pixelaspolygon`: {e}");
    }
    if let Err(e) = register_blob_u32_u32_to_blob(conn, "st_pixel_as_polygon") {
        eprintln!("[shim-scalars] skipping `st_pixel_as_polygon`: {e}");
    } // alias of st_pixelaspolygon
    if let Err(e) = register_blob_u32_to_text(conn, "st_pixelaspolygons") {
        eprintln!("[shim-scalars] skipping `st_pixelaspolygons`: {e}");
    }
    if let Err(e) = register_blob_u32_to_text(conn, "st_pixel_as_polygons") {
        eprintln!("[shim-scalars] skipping `st_pixel_as_polygons`: {e}");
    } // alias of st_pixelaspolygons
    if let Err(e) = register_blob_u32_f64_to_text(conn, "st_pixelofvalue") {
        eprintln!("[shim-scalars] skipping `st_pixelofvalue`: {e}");
    }
    if let Err(e) = register_blob_u32_f64_to_text(conn, "st_pixel_of_value") {
        eprintln!("[shim-scalars] skipping `st_pixel_of_value`: {e}");
    } // alias of st_pixelofvalue
    if let Err(e) = register_text_to_blob(conn, "st_pointfromgeohash") {
        eprintln!("[shim-scalars] skipping `st_pointfromgeohash`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_point_from_geohash") {
        eprintln!("[shim-scalars] skipping `st_point_from_geohash`: {e}");
    } // alias of st_pointfromgeohash
    if let Err(e) = register_text_i32_to_blob(conn, "st_pointfromgeohash") {
        eprintln!("[shim-scalars] skipping `st_pointfromgeohash`: {e}");
    }
    if let Err(e) = register_text_i32_to_blob(conn, "st_point_from_geohash") {
        eprintln!("[shim-scalars] skipping `st_point_from_geohash`: {e}");
    } // alias of st_pointfromgeohash
    if let Err(e) = register_blob_f64_f64_f64_to_bool(conn, "st_pointinsidecircle") {
        eprintln!("[shim-scalars] skipping `st_pointinsidecircle`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_f64_to_bool(conn, "st_point_inside_circle") {
        eprintln!("[shim-scalars] skipping `st_point_inside_circle`: {e}");
    } // alias of st_pointinsidecircle
    if let Err(e) = register_blob_i32_to_blob(conn, "st_pointn") {
        eprintln!("[shim-scalars] skipping `st_pointn`: {e}");
    }
    if let Err(e) = register_blob_i32_to_blob(conn, "st_point_n") {
        eprintln!("[shim-scalars] skipping `st_point_n`: {e}");
    } // alias of st_pointn
    if let Err(e) = register_blob_i32_to_blob(conn, "st_point_n_batch") {
        eprintln!("[shim-scalars] skipping `st_point_n_batch`: {e}");
    } // alias of st_pointn
    if let Err(e) = register_blob_to_blob(conn, "st_pointonsurface") {
        eprintln!("[shim-scalars] skipping `st_pointonsurface`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_point_on_surface") {
        eprintln!("[shim-scalars] skipping `st_point_on_surface`: {e}");
    } // alias of st_pointonsurface
    if let Err(e) = register_blob_to_blob(conn, "st_point_on_surface_batch") {
        eprintln!("[shim-scalars] skipping `st_point_on_surface_batch`: {e}");
    } // alias of st_pointonsurface
    if let Err(e) = register_blob_i32_to_blob(conn, "st_polygon") {
        eprintln!("[shim-scalars] skipping `st_polygon`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_project") {
        eprintln!("[shim-scalars] skipping `st_project`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_project_batch") {
        eprintln!("[shim-scalars] skipping `st_project_batch`: {e}");
    } // alias of st_project
    if let Err(e) = register_blob_u32_f64_to_f64(conn, "st_quantile") {
        eprintln!("[shim-scalars] skipping `st_quantile`: {e}");
    }
    if let Err(e) = register_blob_i32_i32_to_blob(conn, "st_quantizecoordinates") {
        eprintln!("[shim-scalars] skipping `st_quantizecoordinates`: {e}");
    }
    if let Err(e) = register_blob_i32_i32_to_blob(conn, "st_quantize_coordinates") {
        eprintln!("[shim-scalars] skipping `st_quantize_coordinates`: {e}");
    } // alias of st_quantizecoordinates
    if let Err(e) = register_blob_blob_to_bool(conn, "st_rastercontains") {
        eprintln!("[shim-scalars] skipping `st_rastercontains`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_raster_contains") {
        eprintln!("[shim-scalars] skipping `st_raster_contains`: {e}");
    } // alias of st_rastercontains
    if let Err(e) = register_blob_blob_to_bool(conn, "st_rastercontainsgeom") {
        eprintln!("[shim-scalars] skipping `st_rastercontainsgeom`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_raster_contains_geom") {
        eprintln!("[shim-scalars] skipping `st_raster_contains_geom`: {e}");
    } // alias of st_rastercontainsgeom
    if let Err(e) = register_blob_to_blob(conn, "st_rasterconvexhull") {
        eprintln!("[shim-scalars] skipping `st_rasterconvexhull`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_raster_convex_hull") {
        eprintln!("[shim-scalars] skipping `st_raster_convex_hull`: {e}");
    } // alias of st_rasterconvexhull
    if let Err(e) = register_blob_blob_to_bool(conn, "st_rastercovers") {
        eprintln!("[shim-scalars] skipping `st_rastercovers`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_raster_covers") {
        eprintln!("[shim-scalars] skipping `st_raster_covers`: {e}");
    } // alias of st_rastercovers
    if let Err(e) = register_blob_blob_to_bool(conn, "st_rasterintersects") {
        eprintln!("[shim-scalars] skipping `st_rasterintersects`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_raster_intersects") {
        eprintln!("[shim-scalars] skipping `st_raster_intersects`: {e}");
    } // alias of st_rasterintersects
    if let Err(e) = register_blob_blob_to_bool(conn, "st_rasterintersectsgeom") {
        eprintln!("[shim-scalars] skipping `st_rasterintersectsgeom`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_raster_intersects_geom") {
        eprintln!("[shim-scalars] skipping `st_raster_intersects_geom`: {e}");
    } // alias of st_rasterintersectsgeom
    if let Err(e) = register_blob_blob_to_bool(conn, "st_rasteroverlaps") {
        eprintln!("[shim-scalars] skipping `st_rasteroverlaps`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_raster_overlaps") {
        eprintln!("[shim-scalars] skipping `st_raster_overlaps`: {e}");
    } // alias of st_rasteroverlaps
    if let Err(e) = register_blob_u32_to_blob(conn, "st_rasterpolygon") {
        eprintln!("[shim-scalars] skipping `st_rasterpolygon`: {e}");
    }
    if let Err(e) = register_blob_u32_to_blob(conn, "st_raster_polygon") {
        eprintln!("[shim-scalars] skipping `st_raster_polygon`: {e}");
    } // alias of st_rasterpolygon
    if let Err(e) = register_blob_u32_to_blob(conn, "st_rastpolygon") {
        eprintln!("[shim-scalars] skipping `st_rastpolygon`: {e}");
    } // alias of st_rasterpolygon
    if let Err(e) = register_blob_u32_u32_to_f64(conn, "st_rastertoworldcoordx") {
        eprintln!("[shim-scalars] skipping `st_rastertoworldcoordx`: {e}");
    }
    if let Err(e) = register_blob_u32_u32_to_f64(conn, "st_raster_to_world_coord_x") {
        eprintln!("[shim-scalars] skipping `st_raster_to_world_coord_x`: {e}");
    } // alias of st_rastertoworldcoordx
    if let Err(e) = register_blob_u32_u32_to_f64(conn, "st_rastertoworldcoordy") {
        eprintln!("[shim-scalars] skipping `st_rastertoworldcoordy`: {e}");
    }
    if let Err(e) = register_blob_u32_u32_to_f64(conn, "st_raster_to_world_coord_y") {
        eprintln!("[shim-scalars] skipping `st_raster_to_world_coord_y`: {e}");
    } // alias of st_rastertoworldcoordy
    if let Err(e) = register_blob_blob_to_bool(conn, "st_rasterwithin") {
        eprintln!("[shim-scalars] skipping `st_rasterwithin`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_raster_within") {
        eprintln!("[shim-scalars] skipping `st_raster_within`: {e}");
    } // alias of st_rasterwithin
    if let Err(e) = register_blob_to_i32(conn, "st_rastsrid") {
        eprintln!("[shim-scalars] skipping `st_rastsrid`: {e}");
    }
    if let Err(e) = register_blob_to_i32(conn, "st_rast_srid") {
        eprintln!("[shim-scalars] skipping `st_rast_srid`: {e}");
    } // alias of st_rastsrid
    if let Err(e) = register_blob_u32_text_text_to_blob(conn, "st_reclass") {
        eprintln!("[shim-scalars] skipping `st_reclass`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_reduceprecision") {
        eprintln!("[shim-scalars] skipping `st_reduceprecision`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_reduce_precision") {
        eprintln!("[shim-scalars] skipping `st_reduce_precision`: {e}");
    } // alias of st_reduceprecision
    if let Err(e) = register_blob_blob_to_text(conn, "st_relate") {
        eprintln!("[shim-scalars] skipping `st_relate`: {e}");
    }
    if let Err(e) = register_text_text_to_bool(conn, "st_relatematch") {
        eprintln!("[shim-scalars] skipping `st_relatematch`: {e}");
    }
    if let Err(e) = register_text_text_to_bool(conn, "st_relate_match") {
        eprintln!("[shim-scalars] skipping `st_relate_match`: {e}");
    } // alias of st_relatematch
    if let Err(e) = register_blob_u32_to_blob(conn, "st_removeface") {
        eprintln!("[shim-scalars] skipping `st_removeface`: {e}");
    }
    if let Err(e) = register_blob_u32_to_blob(conn, "st_remove_face") {
        eprintln!("[shim-scalars] skipping `st_remove_face`: {e}");
    } // alias of st_removeface
    if let Err(e) = register_blob_u32_to_blob(conn, "topology_remove_face") {
        eprintln!("[shim-scalars] skipping `topology_remove_face`: {e}");
    } // alias of st_removeface
    if let Err(e) = register_blob_i32_to_blob(conn, "st_removepoint") {
        eprintln!("[shim-scalars] skipping `st_removepoint`: {e}");
    }
    if let Err(e) = register_blob_i32_to_blob(conn, "st_remove_point") {
        eprintln!("[shim-scalars] skipping `st_remove_point`: {e}");
    } // alias of st_removepoint
    if let Err(e) = register_blob_to_blob(conn, "st_removerepeatedpoints") {
        eprintln!("[shim-scalars] skipping `st_removerepeatedpoints`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_remove_repeated_points") {
        eprintln!("[shim-scalars] skipping `st_remove_repeated_points`: {e}");
    } // alias of st_removerepeatedpoints
    if let Err(e) = register_blob_f64_to_blob(conn, "st_removerepeatedpoints") {
        eprintln!("[shim-scalars] skipping `st_removerepeatedpoints`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_remove_repeated_points") {
        eprintln!("[shim-scalars] skipping `st_remove_repeated_points`: {e}");
    } // alias of st_removerepeatedpoints
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_removesmallparts") {
        eprintln!("[shim-scalars] skipping `st_removesmallparts`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_remove_small_parts") {
        eprintln!("[shim-scalars] skipping `st_remove_small_parts`: {e}");
    } // alias of st_removesmallparts
    if let Err(e) = register_blob_f64_f64_text_to_blob(conn, "st_rescale") {
        eprintln!("[shim-scalars] skipping `st_rescale`: {e}");
    }
    if let Err(e) = register_blob_u32_u32_text_to_blob(conn, "st_resize") {
        eprintln!("[shim-scalars] skipping `st_resize`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_reverse") {
        eprintln!("[shim-scalars] skipping `st_reverse`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_reversethreed") {
        eprintln!("[shim-scalars] skipping `st_reversethreed`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_3dreverse") {
        eprintln!("[shim-scalars] skipping `st_3dreverse`: {e}");
    } // alias of st_reversethreed
    if let Err(e) = register_blob_to_blob(conn, "st_reverse_threed") {
        eprintln!("[shim-scalars] skipping `st_reverse_threed`: {e}");
    } // alias of st_reversethreed
    if let Err(e) = register_blob_f64_to_blob(conn, "st_rotate") {
        eprintln!("[shim-scalars] skipping `st_rotate`: {e}");
    }
    if let Err(e) = register_blob_4f64_to_blob(conn, "st_rotatethreed") {
        eprintln!("[shim-scalars] skipping `st_rotatethreed`: {e}");
    }
    if let Err(e) = register_blob_4f64_to_blob(conn, "st_3drotate") {
        eprintln!("[shim-scalars] skipping `st_3drotate`: {e}");
    } // alias of st_rotatethreed
    if let Err(e) = register_blob_4f64_to_blob(conn, "st_rotate_threed") {
        eprintln!("[shim-scalars] skipping `st_rotate_threed`: {e}");
    } // alias of st_rotatethreed
    if let Err(e) = register_blob_f64_to_blob(conn, "st_rotatex") {
        eprintln!("[shim-scalars] skipping `st_rotatex`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_rotate_x") {
        eprintln!("[shim-scalars] skipping `st_rotate_x`: {e}");
    } // alias of st_rotatex
    if let Err(e) = register_blob_f64_to_blob(conn, "st_rotatey") {
        eprintln!("[shim-scalars] skipping `st_rotatey`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_rotate_y") {
        eprintln!("[shim-scalars] skipping `st_rotate_y`: {e}");
    } // alias of st_rotatey
    if let Err(e) = register_blob_f64_to_blob(conn, "st_rotatez") {
        eprintln!("[shim-scalars] skipping `st_rotatez`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_rotate_z") {
        eprintln!("[shim-scalars] skipping `st_rotate_z`: {e}");
    } // alias of st_rotatez
    if let Err(e) = register_blob_u32_to_blob(conn, "st_roughness") {
        eprintln!("[shim-scalars] skipping `st_roughness`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_scale") {
        eprintln!("[shim-scalars] skipping `st_scale`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_f64_to_blob(conn, "st_scalethreed") {
        eprintln!("[shim-scalars] skipping `st_scalethreed`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_f64_to_blob(conn, "st_3dscale") {
        eprintln!("[shim-scalars] skipping `st_3dscale`: {e}");
    } // alias of st_scalethreed
    if let Err(e) = register_blob_f64_f64_f64_to_blob(conn, "st_scale_threed") {
        eprintln!("[shim-scalars] skipping `st_scale_threed`: {e}");
    } // alias of st_scalethreed
    if let Err(e) = register_blob_to_f64(conn, "st_scalex") {
        eprintln!("[shim-scalars] skipping `st_scalex`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_rast_scale_x_dfer_batch") {
        eprintln!("[shim-scalars] skipping `st_rast_scale_x_dfer_batch`: {e}");
    } // alias of st_scalex
    if let Err(e) = register_blob_to_f64(conn, "st_scale_x") {
        eprintln!("[shim-scalars] skipping `st_scale_x`: {e}");
    } // alias of st_scalex
    if let Err(e) = register_blob_to_f64(conn, "st_scaley") {
        eprintln!("[shim-scalars] skipping `st_scaley`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_rast_scale_y_dfer_batch") {
        eprintln!("[shim-scalars] skipping `st_rast_scale_y_dfer_batch`: {e}");
    } // alias of st_scaley
    if let Err(e) = register_blob_to_f64(conn, "st_scale_y") {
        eprintln!("[shim-scalars] skipping `st_scale_y`: {e}");
    } // alias of st_scaley
    if let Err(e) = register_blob_blob_to_blob(conn, "st_scroll") {
        eprintln!("[shim-scalars] skipping `st_scroll`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_segmentize") {
        eprintln!("[shim-scalars] skipping `st_segmentize`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_seteffectivearea") {
        eprintln!("[shim-scalars] skipping `st_seteffectivearea`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_set_effective_area") {
        eprintln!("[shim-scalars] skipping `st_set_effective_area`: {e}");
    } // alias of st_seteffectivearea
    if let Err(e) = register_blob_f64_to_blob(conn, "st_seteffectivearea") {
        eprintln!("[shim-scalars] skipping `st_seteffectivearea`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_set_effective_area") {
        eprintln!("[shim-scalars] skipping `st_set_effective_area`: {e}");
    } // alias of st_seteffectivearea
    if let Err(e) = register_blob_i32_blob_to_blob(conn, "st_setpoint") {
        eprintln!("[shim-scalars] skipping `st_setpoint`: {e}");
    }
    if let Err(e) = register_blob_i32_blob_to_blob(conn, "st_set_point") {
        eprintln!("[shim-scalars] skipping `st_set_point`: {e}");
    } // alias of st_setpoint
    if let Err(e) = register_blob_i32_to_blob(conn, "st_setsrid") {
        eprintln!("[shim-scalars] skipping `st_setsrid`: {e}");
    }
    if let Err(e) = register_blob_i32_to_blob(conn, "st_set_srid") {
        eprintln!("[shim-scalars] skipping `st_set_srid`: {e}");
    } // alias of st_setsrid
    if let Err(e) = register_blob_u32_u32_u32_f64_to_blob(conn, "st_setvalue") {
        eprintln!("[shim-scalars] skipping `st_setvalue`: {e}");
    }
    if let Err(e) = register_blob_u32_u32_u32_f64_to_blob(conn, "st_set_value") {
        eprintln!("[shim-scalars] skipping `st_set_value`: {e}");
    } // alias of st_setvalue
    if let Err(e) = register_blob_u32_u32_u32_blob_to_blob(conn, "st_setvalues") {
        eprintln!("[shim-scalars] skipping `st_setvalues`: {e}");
    }
    if let Err(e) = register_blob_u32_u32_u32_blob_to_blob(conn, "st_set_values") {
        eprintln!("[shim-scalars] skipping `st_set_values`: {e}");
    } // alias of st_setvalues
    if let Err(e) = register_blob_blob_to_blob(conn, "st_sharedpaths") {
        eprintln!("[shim-scalars] skipping `st_sharedpaths`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_shared_paths") {
        eprintln!("[shim-scalars] skipping `st_shared_paths`: {e}");
    } // alias of st_sharedpaths
    if let Err(e) = register_blob_to_blob(conn, "st_shiftlongitude") {
        eprintln!("[shim-scalars] skipping `st_shiftlongitude`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_shift_longitude") {
        eprintln!("[shim-scalars] skipping `st_shift_longitude`: {e}");
    } // alias of st_shiftlongitude
    if let Err(e) = register_blob_blob_to_blob(conn, "st_shortestline") {
        eprintln!("[shim-scalars] skipping `st_shortestline`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_shortest_line") {
        eprintln!("[shim-scalars] skipping `st_shortest_line`: {e}");
    } // alias of st_shortestline
    if let Err(e) = register_blob_f64_to_blob(conn, "st_simplify") {
        eprintln!("[shim-scalars] skipping `st_simplify`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_simplify_batch") {
        eprintln!("[shim-scalars] skipping `st_simplify_batch`: {e}");
    } // alias of st_simplify
    if let Err(e) = register_blob_f64_to_blob(conn, "st_simplifypolygonhull") {
        eprintln!("[shim-scalars] skipping `st_simplifypolygonhull`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_simplify_polygon_hull") {
        eprintln!("[shim-scalars] skipping `st_simplify_polygon_hull`: {e}");
    } // alias of st_simplifypolygonhull
    if let Err(e) = register_blob_f64_bool_to_blob(conn, "st_simplifypolygonhull") {
        eprintln!("[shim-scalars] skipping `st_simplifypolygonhull`: {e}");
    }
    if let Err(e) = register_blob_f64_bool_to_blob(conn, "st_simplify_polygon_hull") {
        eprintln!("[shim-scalars] skipping `st_simplify_polygon_hull`: {e}");
    } // alias of st_simplifypolygonhull
    if let Err(e) = register_blob_f64_to_blob(conn, "st_simplifypreservetopology") {
        eprintln!("[shim-scalars] skipping `st_simplifypreservetopology`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_simplify_preserve_topology") {
        eprintln!("[shim-scalars] skipping `st_simplify_preserve_topology`: {e}");
    } // alias of st_simplifypreservetopology
    if let Err(e) = register_blob_f64_to_blob(conn, "st_simplify_preserve_topology_batch") {
        eprintln!("[shim-scalars] skipping `st_simplify_preserve_topology_batch`: {e}");
    } // alias of st_simplifypreservetopology
    if let Err(e) = register_blob_f64_to_blob(conn, "st_simplifyvw") {
        eprintln!("[shim-scalars] skipping `st_simplifyvw`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_simplify_vw") {
        eprintln!("[shim-scalars] skipping `st_simplify_vw`: {e}");
    } // alias of st_simplifyvw
    if let Err(e) = register_blob_to_f64(conn, "st_skewx") {
        eprintln!("[shim-scalars] skipping `st_skewx`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_rast_skew_x_dfer_batch") {
        eprintln!("[shim-scalars] skipping `st_rast_skew_x_dfer_batch`: {e}");
    } // alias of st_skewx
    if let Err(e) = register_blob_to_f64(conn, "st_skew_x") {
        eprintln!("[shim-scalars] skipping `st_skew_x`: {e}");
    } // alias of st_skewx
    if let Err(e) = register_blob_to_f64(conn, "st_skewy") {
        eprintln!("[shim-scalars] skipping `st_skewy`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_rast_skew_y_dfer_batch") {
        eprintln!("[shim-scalars] skipping `st_rast_skew_y_dfer_batch`: {e}");
    } // alias of st_skewy
    if let Err(e) = register_blob_to_f64(conn, "st_skew_y") {
        eprintln!("[shim-scalars] skipping `st_skew_y`: {e}");
    } // alias of st_skewy
    if let Err(e) = register_blob_u32_to_blob(conn, "st_slope") {
        eprintln!("[shim-scalars] skipping `st_slope`: {e}");
    }
    if let Err(e) = register_blob_blob_f64_to_blob(conn, "st_snap") {
        eprintln!("[shim-scalars] skipping `st_snap`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_snaptogrid") {
        eprintln!("[shim-scalars] skipping `st_snaptogrid`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_snap_to_grid") {
        eprintln!("[shim-scalars] skipping `st_snap_to_grid`: {e}");
    } // alias of st_snaptogrid
    if let Err(e) = register_blob_blob_to_blob(conn, "st_split") {
        eprintln!("[shim-scalars] skipping `st_split`: {e}");
    }
    if let Err(e) = register_f64_i32_i32_to_blob(conn, "st_square") {
        eprintln!("[shim-scalars] skipping `st_square`: {e}");
    }
    if let Err(e) = register_blob_to_i32(conn, "st_srid") {
        eprintln!("[shim-scalars] skipping `st_srid`: {e}");
    }
    if let Err(e) = register_blob_to_i32(conn, "st_rast_srid_dfer_batch") {
        eprintln!("[shim-scalars] skipping `st_rast_srid_dfer_batch`: {e}");
    } // alias of st_srid
    if let Err(e) = register_blob_to_blob(conn, "st_startpoint") {
        eprintln!("[shim-scalars] skipping `st_startpoint`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_start_point") {
        eprintln!("[shim-scalars] skipping `st_start_point`: {e}");
    } // alias of st_startpoint
    if let Err(e) = register_blob_to_blob(conn, "st_start_point_batch") {
        eprintln!("[shim-scalars] skipping `st_start_point_batch`: {e}");
    } // alias of st_startpoint
    if let Err(e) = register_blob_to_blob(conn, "st_straightskeleton") {
        eprintln!("[shim-scalars] skipping `st_straightskeleton`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_straight_skeleton") {
        eprintln!("[shim-scalars] skipping `st_straight_skeleton`: {e}");
    } // alias of st_straightskeleton
    if let Err(e) = register_blob_to_text(conn, "st_summary") {
        eprintln!("[shim-scalars] skipping `st_summary`: {e}");
    }
    if let Err(e) = register_blob_u32_to_text(conn, "st_summarystats") {
        eprintln!("[shim-scalars] skipping `st_summarystats`: {e}");
    }
    if let Err(e) = register_blob_u32_to_text(conn, "st_summary_stats") {
        eprintln!("[shim-scalars] skipping `st_summary_stats`: {e}");
    } // alias of st_summarystats
    if let Err(e) = register_blob_text_to_blob(conn, "st_swapordinates") {
        eprintln!("[shim-scalars] skipping `st_swapordinates`: {e}");
    }
    if let Err(e) = register_blob_text_to_blob(conn, "st_swap_ordinates") {
        eprintln!("[shim-scalars] skipping `st_swap_ordinates`: {e}");
    } // alias of st_swapordinates
    if let Err(e) = register_blob_blob_to_blob(conn, "st_symdifference") {
        eprintln!("[shim-scalars] skipping `st_symdifference`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_geog_sym_difference") {
        eprintln!("[shim-scalars] skipping `st_geog_sym_difference`: {e}");
    } // alias of st_symdifference
    if let Err(e) = register_blob_blob_to_blob(conn, "st_sym_difference") {
        eprintln!("[shim-scalars] skipping `st_sym_difference`: {e}");
    } // alias of st_symdifference
    if let Err(e) = register_blob_blob_to_blob(conn, "st_sym_difference_batch") {
        eprintln!("[shim-scalars] skipping `st_sym_difference_batch`: {e}");
    } // alias of st_symdifference
    if let Err(e) = register_blob_to_blob(conn, "st_tesselate") {
        eprintln!("[shim-scalars] skipping `st_tesselate`: {e}");
    }
    if let Err(e) = register_i32_i32_i32_to_blob(conn, "st_tileenvelope") {
        eprintln!("[shim-scalars] skipping `st_tileenvelope`: {e}");
    }
    if let Err(e) = register_i32_i32_i32_to_blob(conn, "st_tile_envelope") {
        eprintln!("[shim-scalars] skipping `st_tile_envelope`: {e}");
    } // alias of st_tileenvelope
    if let Err(e) = register_text_to_u32(conn, "st_topogeomelementcount") {
        eprintln!("[shim-scalars] skipping `st_topogeomelementcount`: {e}");
    }
    if let Err(e) = register_text_to_u32(conn, "st_topo_geom_element_count") {
        eprintln!("[shim-scalars] skipping `st_topo_geom_element_count`: {e}");
    } // alias of st_topogeomelementcount
    if let Err(e) = register_text_to_blob(conn, "st_topogeomgeometry") {
        eprintln!("[shim-scalars] skipping `st_topogeomgeometry`: {e}");
    }
    if let Err(e) = register_text_to_blob(conn, "st_topo_geom_geometry") {
        eprintln!("[shim-scalars] skipping `st_topo_geom_geometry`: {e}");
    } // alias of st_topogeomgeometry
    if let Err(e) = register_text_to_blob(conn, "topogeom_geometry") {
        eprintln!("[shim-scalars] skipping `topogeom_geometry`: {e}");
    } // alias of st_topogeomgeometry
    if let Err(e) = register_text_to_text(conn, "st_topogeomgetelements") {
        eprintln!("[shim-scalars] skipping `st_topogeomgetelements`: {e}");
    }
    if let Err(e) = register_text_to_text(conn, "st_topo_geom_get_elements") {
        eprintln!("[shim-scalars] skipping `st_topo_geom_get_elements`: {e}");
    } // alias of st_topogeomgetelements
    if let Err(e) = register_text_to_u32(conn, "st_topogeomtopotype") {
        eprintln!("[shim-scalars] skipping `st_topogeomtopotype`: {e}");
    }
    if let Err(e) = register_text_to_u32(conn, "st_topo_geom_topo_type") {
        eprintln!("[shim-scalars] skipping `st_topo_geom_topo_type`: {e}");
    } // alias of st_topogeomtopotype
    if let Err(e) = register_blob_to_u32(conn, "st_topologyedgecount") {
        eprintln!("[shim-scalars] skipping `st_topologyedgecount`: {e}");
    }
    if let Err(e) = register_blob_to_u32(conn, "st_topology_edge_count") {
        eprintln!("[shim-scalars] skipping `st_topology_edge_count`: {e}");
    } // alias of st_topologyedgecount
    if let Err(e) = register_blob_to_u32(conn, "st_topology_edge_count_batch") {
        eprintln!("[shim-scalars] skipping `st_topology_edge_count_batch`: {e}");
    } // alias of st_topologyedgecount
    if let Err(e) = register_blob_to_u32(conn, "topology_edge_count") {
        eprintln!("[shim-scalars] skipping `topology_edge_count`: {e}");
    } // alias of st_topologyedgecount
    if let Err(e) = register_blob_to_u32(conn, "st_topologyfacecount") {
        eprintln!("[shim-scalars] skipping `st_topologyfacecount`: {e}");
    }
    if let Err(e) = register_blob_to_u32(conn, "st_topology_face_count") {
        eprintln!("[shim-scalars] skipping `st_topology_face_count`: {e}");
    } // alias of st_topologyfacecount
    if let Err(e) = register_blob_to_u32(conn, "st_topology_face_count_batch") {
        eprintln!("[shim-scalars] skipping `st_topology_face_count_batch`: {e}");
    } // alias of st_topologyfacecount
    if let Err(e) = register_blob_to_u32(conn, "topology_face_count") {
        eprintln!("[shim-scalars] skipping `topology_face_count`: {e}");
    } // alias of st_topologyfacecount
    if let Err(e) = register_blob_to_blob(conn, "st_topologyfrombytes") {
        eprintln!("[shim-scalars] skipping `st_topologyfrombytes`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_topology_from_bytes") {
        eprintln!("[shim-scalars] skipping `st_topology_from_bytes`: {e}");
    } // alias of st_topologyfrombytes
    if let Err(e) = register_blob_to_blob(conn, "topology_from_bytes") {
        eprintln!("[shim-scalars] skipping `topology_from_bytes`: {e}");
    } // alias of st_topologyfrombytes
    if let Err(e) = register_blob_to_text(conn, "st_topologyname") {
        eprintln!("[shim-scalars] skipping `st_topologyname`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_topology_name") {
        eprintln!("[shim-scalars] skipping `st_topology_name`: {e}");
    } // alias of st_topologyname
    if let Err(e) = register_blob_to_text(conn, "st_topology_name_batch") {
        eprintln!("[shim-scalars] skipping `st_topology_name_batch`: {e}");
    } // alias of st_topologyname
    if let Err(e) = register_blob_to_text(conn, "topology_name") {
        eprintln!("[shim-scalars] skipping `topology_name`: {e}");
    } // alias of st_topologyname
    if let Err(e) = register_blob_to_u32(conn, "st_topologynodecount") {
        eprintln!("[shim-scalars] skipping `st_topologynodecount`: {e}");
    }
    if let Err(e) = register_blob_to_u32(conn, "st_topology_node_count") {
        eprintln!("[shim-scalars] skipping `st_topology_node_count`: {e}");
    } // alias of st_topologynodecount
    if let Err(e) = register_blob_to_u32(conn, "st_topology_node_count_batch") {
        eprintln!("[shim-scalars] skipping `st_topology_node_count_batch`: {e}");
    } // alias of st_topologynodecount
    if let Err(e) = register_blob_to_u32(conn, "topology_node_count") {
        eprintln!("[shim-scalars] skipping `topology_node_count`: {e}");
    } // alias of st_topologynodecount
    if let Err(e) = register_blob_to_f64(conn, "st_topologyprecision") {
        eprintln!("[shim-scalars] skipping `st_topologyprecision`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_topology_precision") {
        eprintln!("[shim-scalars] skipping `st_topology_precision`: {e}");
    } // alias of st_topologyprecision
    if let Err(e) = register_blob_to_f64(conn, "st_topology_precision_batch") {
        eprintln!("[shim-scalars] skipping `st_topology_precision_batch`: {e}");
    } // alias of st_topologyprecision
    if let Err(e) = register_blob_to_f64(conn, "topology_precision") {
        eprintln!("[shim-scalars] skipping `topology_precision`: {e}");
    } // alias of st_topologyprecision
    if let Err(e) = register_blob_to_i32(conn, "st_topologysrid") {
        eprintln!("[shim-scalars] skipping `st_topologysrid`: {e}");
    }
    if let Err(e) = register_blob_to_i32(conn, "st_topology_srid") {
        eprintln!("[shim-scalars] skipping `st_topology_srid`: {e}");
    } // alias of st_topologysrid
    if let Err(e) = register_blob_to_i32(conn, "st_topology_srid_batch") {
        eprintln!("[shim-scalars] skipping `st_topology_srid_batch`: {e}");
    } // alias of st_topologysrid
    if let Err(e) = register_blob_to_i32(conn, "topology_srid") {
        eprintln!("[shim-scalars] skipping `topology_srid`: {e}");
    } // alias of st_topologysrid
    if let Err(e) = register_blob_to_blob(conn, "st_topologytobytes") {
        eprintln!("[shim-scalars] skipping `st_topologytobytes`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_topology_to_bytes") {
        eprintln!("[shim-scalars] skipping `st_topology_to_bytes`: {e}");
    } // alias of st_topologytobytes
    if let Err(e) = register_blob_to_blob(conn, "topology_to_bytes") {
        eprintln!("[shim-scalars] skipping `topology_to_bytes`: {e}");
    } // alias of st_topologytobytes
    if let Err(e) = register_blob_blob_to_bool(conn, "st_touches") {
        eprintln!("[shim-scalars] skipping `st_touches`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_touches_batch") {
        eprintln!("[shim-scalars] skipping `st_touches_batch`: {e}");
    } // alias of st_touches
    if let Err(e) = register_blob_u32_to_blob(conn, "st_tpi") {
        eprintln!("[shim-scalars] skipping `st_tpi`: {e}");
    }
    if let Err(e) = register_blob_i32_to_blob(conn, "st_transform") {
        eprintln!("[shim-scalars] skipping `st_transform`: {e}");
    }
    if let Err(e) = register_blob_text_to_blob(conn, "st_transformpipeline") {
        eprintln!("[shim-scalars] skipping `st_transformpipeline`: {e}");
    }
    if let Err(e) = register_blob_text_to_blob(conn, "st_transform_pipeline") {
        eprintln!("[shim-scalars] skipping `st_transform_pipeline`: {e}");
    } // alias of st_transformpipeline
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_translate") {
        eprintln!("[shim-scalars] skipping `st_translate`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_f64_to_blob(conn, "st_translatethreed") {
        eprintln!("[shim-scalars] skipping `st_translatethreed`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_f64_to_blob(conn, "st_3dtranslate") {
        eprintln!("[shim-scalars] skipping `st_3dtranslate`: {e}");
    } // alias of st_translatethreed
    if let Err(e) = register_blob_f64_f64_f64_to_blob(conn, "st_translate_threed") {
        eprintln!("[shim-scalars] skipping `st_translate_threed`: {e}");
    } // alias of st_translatethreed
    if let Err(e) = register_blob_4f64_to_blob(conn, "st_transscale") {
        eprintln!("[shim-scalars] skipping `st_transscale`: {e}");
    }
    if let Err(e) = register_blob_u32_to_blob(conn, "st_tri") {
        eprintln!("[shim-scalars] skipping `st_tri`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_triangulatepolygon") {
        eprintln!("[shim-scalars] skipping `st_triangulatepolygon`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_triangulate_polygon") {
        eprintln!("[shim-scalars] skipping `st_triangulate_polygon`: {e}");
    } // alias of st_triangulatepolygon
    if let Err(e) = register_blob_to_blob(conn, "st_unaryunion") {
        eprintln!("[shim-scalars] skipping `st_unaryunion`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_coverage_union") {
        eprintln!("[shim-scalars] skipping `st_coverage_union`: {e}");
    } // alias of st_unaryunion
    if let Err(e) = register_blob_to_blob(conn, "st_unary_union") {
        eprintln!("[shim-scalars] skipping `st_unary_union`: {e}");
    } // alias of st_unaryunion
    if let Err(e) = register_blob_blob_to_blob(conn, "st_union") {
        eprintln!("[shim-scalars] skipping `st_union`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_geog_union") {
        eprintln!("[shim-scalars] skipping `st_geog_union`: {e}");
    } // alias of st_union
    if let Err(e) = register_blob_blob_to_blob(conn, "st_rast_union_aggregate") {
        eprintln!("[shim-scalars] skipping `st_rast_union_aggregate`: {e}");
    } // alias of st_union
    if let Err(e) = register_blob_blob_to_blob(conn, "st_union_aggregate_batch") {
        eprintln!("[shim-scalars] skipping `st_union_aggregate_batch`: {e}");
    } // alias of st_union
    if let Err(e) = register_blob_blob_to_blob(conn, "st_union_batch") {
        eprintln!("[shim-scalars] skipping `st_union_batch`: {e}");
    } // alias of st_union
    if let Err(e) = register_blob_blob_to_blob(conn, "st_unionthreed") {
        eprintln!("[shim-scalars] skipping `st_unionthreed`: {e}");
    }
    if let Err(e) = register_blob_blob_to_blob(conn, "st_3dunion") {
        eprintln!("[shim-scalars] skipping `st_3dunion`: {e}");
    } // alias of st_unionthreed
    if let Err(e) = register_blob_blob_to_blob(conn, "st_union_threed") {
        eprintln!("[shim-scalars] skipping `st_union_threed`: {e}");
    } // alias of st_unionthreed
    if let Err(e) = register_blob_to_f64(conn, "st_upperleftx") {
        eprintln!("[shim-scalars] skipping `st_upperleftx`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_rast_upper_left_x_dfer_batch") {
        eprintln!("[shim-scalars] skipping `st_rast_upper_left_x_dfer_batch`: {e}");
    } // alias of st_upperleftx
    if let Err(e) = register_blob_to_f64(conn, "st_upper_left_x") {
        eprintln!("[shim-scalars] skipping `st_upper_left_x`: {e}");
    } // alias of st_upperleftx
    if let Err(e) = register_blob_to_f64(conn, "st_upperlefty") {
        eprintln!("[shim-scalars] skipping `st_upperlefty`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_rast_upper_left_y_dfer_batch") {
        eprintln!("[shim-scalars] skipping `st_rast_upper_left_y_dfer_batch`: {e}");
    } // alias of st_upperlefty
    if let Err(e) = register_blob_to_f64(conn, "st_upper_left_y") {
        eprintln!("[shim-scalars] skipping `st_upper_left_y`: {e}");
    } // alias of st_upperlefty
    if let Err(e) = register_blob_to_text(conn, "st_validatetopology") {
        eprintln!("[shim-scalars] skipping `st_validatetopology`: {e}");
    }
    if let Err(e) = register_blob_to_text(conn, "st_validate_topology") {
        eprintln!("[shim-scalars] skipping `st_validate_topology`: {e}");
    } // alias of st_validatetopology
    if let Err(e) = register_blob_to_text(conn, "topology_validate") {
        eprintln!("[shim-scalars] skipping `topology_validate`: {e}");
    } // alias of st_validatetopology
    if let Err(e) = register_blob_u32_u32_u32_to_f64(conn, "st_value") {
        eprintln!("[shim-scalars] skipping `st_value`: {e}");
    }
    if let Err(e) = register_blob_u32_to_text(conn, "st_valuecount") {
        eprintln!("[shim-scalars] skipping `st_valuecount`: {e}");
    }
    if let Err(e) = register_blob_u32_to_text(conn, "st_value_count") {
        eprintln!("[shim-scalars] skipping `st_value_count`: {e}");
    } // alias of st_valuecount
    if let Err(e) = register_blob_to_f64(conn, "st_volume") {
        eprintln!("[shim-scalars] skipping `st_volume`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_voronoilines") {
        eprintln!("[shim-scalars] skipping `st_voronoilines`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_voronoi_lines") {
        eprintln!("[shim-scalars] skipping `st_voronoi_lines`: {e}");
    } // alias of st_voronoilines
    if let Err(e) = register_blob_f64_to_blob(conn, "st_voronoilines") {
        eprintln!("[shim-scalars] skipping `st_voronoilines`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_voronoi_lines") {
        eprintln!("[shim-scalars] skipping `st_voronoi_lines`: {e}");
    } // alias of st_voronoilines
    if let Err(e) = register_blob_to_blob(conn, "st_voronoipolygons") {
        eprintln!("[shim-scalars] skipping `st_voronoipolygons`: {e}");
    }
    if let Err(e) = register_blob_to_blob(conn, "st_voronoi_polygons") {
        eprintln!("[shim-scalars] skipping `st_voronoi_polygons`: {e}");
    } // alias of st_voronoipolygons
    if let Err(e) = register_blob_f64_to_blob(conn, "st_voronoipolygons") {
        eprintln!("[shim-scalars] skipping `st_voronoipolygons`: {e}");
    }
    if let Err(e) = register_blob_f64_to_blob(conn, "st_voronoi_polygons") {
        eprintln!("[shim-scalars] skipping `st_voronoi_polygons`: {e}");
    } // alias of st_voronoipolygons
    if let Err(e) = register_blob_to_u32(conn, "st_width") {
        eprintln!("[shim-scalars] skipping `st_width`: {e}");
    }
    if let Err(e) = register_blob_to_u32(conn, "st_rast_width_dfer_batch") {
        eprintln!("[shim-scalars] skipping `st_rast_width_dfer_batch`: {e}");
    } // alias of st_width
    if let Err(e) = register_blob_to_u32(conn, "st_rastwidth") {
        eprintln!("[shim-scalars] skipping `st_rastwidth`: {e}");
    } // alias of st_width
    if let Err(e) = register_blob_blob_to_bool(conn, "st_within") {
        eprintln!("[shim-scalars] skipping `st_within`: {e}");
    }
    if let Err(e) = register_blob_blob_to_bool(conn, "st_within_batch") {
        eprintln!("[shim-scalars] skipping `st_within_batch`: {e}");
    } // alias of st_within
    if let Err(e) = register_blob_f64_f64_to_text(conn, "st_worldtorastercoord") {
        eprintln!("[shim-scalars] skipping `st_worldtorastercoord`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_to_text(conn, "st_world_to_raster_coord") {
        eprintln!("[shim-scalars] skipping `st_world_to_raster_coord`: {e}");
    } // alias of st_worldtorastercoord
    if let Err(e) = register_blob_f64_f64_to_i32(conn, "st_worldtorastercoordcol") {
        eprintln!("[shim-scalars] skipping `st_worldtorastercoordcol`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_to_i32(conn, "st_world_to_raster_coord_col") {
        eprintln!("[shim-scalars] skipping `st_world_to_raster_coord_col`: {e}");
    } // alias of st_worldtorastercoordcol
    if let Err(e) = register_blob_f64_f64_to_i32(conn, "st_worldtorastercoordrow") {
        eprintln!("[shim-scalars] skipping `st_worldtorastercoordrow`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_to_i32(conn, "st_world_to_raster_coord_row") {
        eprintln!("[shim-scalars] skipping `st_world_to_raster_coord_row`: {e}");
    } // alias of st_worldtorastercoordrow
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_wrapx") {
        eprintln!("[shim-scalars] skipping `st_wrapx`: {e}");
    }
    if let Err(e) = register_blob_f64_f64_to_blob(conn, "st_wrap_x") {
        eprintln!("[shim-scalars] skipping `st_wrap_x`: {e}");
    } // alias of st_wrapx
    if let Err(e) = register_blob_to_f64(conn, "st_x") {
        eprintln!("[shim-scalars] skipping `st_x`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_x_batch") {
        eprintln!("[shim-scalars] skipping `st_x_batch`: {e}");
    } // alias of st_x
    if let Err(e) = register_blob_to_f64(conn, "st_xmax") {
        eprintln!("[shim-scalars] skipping `st_xmax`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_xmin") {
        eprintln!("[shim-scalars] skipping `st_xmin`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_y") {
        eprintln!("[shim-scalars] skipping `st_y`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_y_batch") {
        eprintln!("[shim-scalars] skipping `st_y_batch`: {e}");
    } // alias of st_y
    if let Err(e) = register_blob_to_f64(conn, "st_ymax") {
        eprintln!("[shim-scalars] skipping `st_ymax`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_ymin") {
        eprintln!("[shim-scalars] skipping `st_ymin`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_z") {
        eprintln!("[shim-scalars] skipping `st_z`: {e}");
    }
    if let Err(e) = register_blob_to_f64(conn, "st_z_batch") {
        eprintln!("[shim-scalars] skipping `st_z_batch`: {e}");
    } // alias of st_z
    if let Err(e) = register_blob_to_f64(conn, "st_zmax") {
        eprintln!("[shim-scalars] skipping `st_zmax`: {e}");
    }
    if let Err(e) = register_blob_to_i32(conn, "st_zmflag") {
        eprintln!("[shim-scalars] skipping `st_zmflag`: {e}");
    }
    if let Err(e) = register_blob_to_i32(conn, "st_zmflag") {
        eprintln!("[shim-scalars] skipping `st_zmflag`: {e}");
    } // alias of st_zmflag
    if let Err(e) = register_blob_to_f64(conn, "st_zmin") {
        eprintln!("[shim-scalars] skipping `st_zmin`: {e}");
    }
    // Phase 2: 1011 names registered (69 shapes), 17 scalars deferred to Phase 3+ (14 unique shapes).
    Ok(())
}

fn duckdb_sys_error(msg: String) -> duckdb::Error {
    // The duckdb crate doesn't expose a "userland error" variant
    // we can construct directly; round-trip through
    // ToSqlConversionFailure until they ship one.
    duckdb::Error::ToSqlConversionFailure(msg.into())
}

fn lookup(sql_name: &str) -> Result<Arc<dyn ScalarFunctionDef>> {
    registry::lookup_scalar(sql_name)
        .ok_or_else(|| duckdb_sys_error(format!("scalar `{sql_name}` not registered by the shim")))
}

// Phase 3b (2026-06-24): NULL propagation.
//
// Per-shape `invoke` impls call `state.propagates_null()` once
// at the top of the loop and, for each row, check the held
// `FlatVector::row_is_null(row as u64)` on each input. We can't
// share a `row_has_null_input(input, row, n)` helper because
// the existing per-row code already holds the FlatVectors
// (which borrow `input`) — the helper would re-borrow `input`
// and trigger the borrow checker. Each shape inlines the check
// using its already-held v0 (and v1, v2 where applicable).

// ====================================================================
// Per-shape register_ + VScalar impls.
//
// Each shape has the same structure:
//   - register_<shape>(conn, name) looks up the ScalarFunctionDef
//     by name and registers it via register_scalar_function_with_state.
//   - <Shape>Scalar struct impls VScalar with State = Arc<…> and a
//     fixed (input types) → (output type) signature.
//   - invoke iterates rows in the DataChunk, reads each row's
//     args as FunctionValue, calls ScalarFunctionDef::execute,
//     writes the result to the output FlatVector.
//
// The varchar/blob read pattern uses duckdb_string_t + DuckString
// (handles both inline-short and pointer-long strings); the
// primitive (i64/f64/bool) reads use FlatVector::as_slice_with_len.
// ====================================================================

// ---- (varchar) -> blob ----
fn register_text_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<TextToBlobScalar>(sql_name, &def)
}
struct TextToBlobScalar;
impl VScalar for TextToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let raw = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && v0.row_is_null(i as u64) {
                out.set_null(i);
                continue;
            }
            let mut s_raw = raw[i];
            let s: String = DuckString::new(&mut s_raw).as_str().into_owned();
            let r = state
                .execute(&[FunctionValue::String(s)])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![LogicalTypeHandle::from(LogicalTypeId::Varchar)],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (blob) -> blob ----
fn register_blob_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobToBlobScalar>(sql_name, &def)
}
struct BlobToBlobScalar;
impl VScalar for BlobToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let raw = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && v0.row_is_null(i as u64) {
                out.set_null(i);
                continue;
            }
            let mut s_raw = raw[i];
            // BLOBs and VARCHAR share the duckdb_string_t shape;
            // DuckString::as_bytes returns the raw bytes (no UTF-8
            // conversion) — required for WKB round-tripping.
            let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
            let r = state
                .execute(&[FunctionValue::Binary(bytes)])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![LogicalTypeHandle::from(LogicalTypeId::Blob)],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (blob, blob) -> boolean ----
fn register_blob_blob_to_bool(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobToBoolScalar>(sql_name, &def)
}
struct BlobBlobToBoolScalar;
impl VScalar for BlobBlobToBoolScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        // Same two-pass pattern as BlobToF64Scalar: can't hold the
        // mutable slice borrow AND call set_null in the same scope.
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<bool>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut a = r0[i];
                let mut b = r1[i];
                let ab: Vec<u8> = DuckString::new(&mut a).as_bytes().to_vec();
                let bb: Vec<u8> = DuckString::new(&mut b).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(ab), FunctionValue::Binary(bb)])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Boolean(b) => b,
                    FunctionValue::Int8(i) => i != 0,
                    FunctionValue::Int32(i) => i != 0,
                    FunctionValue::Int64(i) => i != 0,
                    FunctionValue::Null => {
                        nulls.push(i);
                        false
                    }
                    other => {
                        return Err(format!(
                            "BlobBlobToBoolScalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Boolean),
        )]
    }
}

// ---- (blob) -> f64 ----
fn register_blob_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobToF64Scalar>(sql_name, &def)
}
struct BlobToF64Scalar;
impl VScalar for BlobToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let raw = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        // Collect nulls separately because as_mut_slice_with_len
        // and set_null both borrow `out` mutably — can't have both
        // alive at the same iteration. Two-pass keeps lifetimes
        // disjoint.
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null && v0.row_is_null(i as u64) {
                    nulls.push(i);
                    continue;
                }
                let mut s_raw = raw[i];
                let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes)])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(f) => f,
                    FunctionValue::Float32(f) => f as f64,
                    FunctionValue::Int32(i) => i as f64,
                    FunctionValue::Int64(i) => i as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "BlobToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![LogicalTypeHandle::from(LogicalTypeId::Blob)],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (blob, f64) -> blob ----
fn register_blob_f64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64ToBlobScalar>(sql_name, &def)
}
struct BlobF64ToBlobScalar;
impl VScalar for BlobF64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let mut s_raw = r0[i];
            let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
            let r = state
                .execute(&[FunctionValue::Binary(bytes), FunctionValue::Float64(r1[i])])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (blob) -> text ----
fn register_blob_to_text(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobToTextScalar>(sql_name, &def)
}
struct BlobToTextScalar;
impl VScalar for BlobToTextScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let raw = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && v0.row_is_null(i as u64) {
                out.set_null(i);
                continue;
            }
            let mut s_raw = raw[i];
            let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
            let r = state
                .execute(&[FunctionValue::Binary(bytes)])
                .map_err(|e| format!("{e:?}"))?;
            match r {
                FunctionValue::String(s) => out.insert(i, s.as_str()),
                FunctionValue::Binary(b) => out.insert(i, b.as_slice()),
                FunctionValue::Null => out.set_null(i),
                other => {
                    return Err(format!(
                        "BlobToTextScalar: unexpected variant `{}`",
                        other.type_name()
                    )
                    .into())
                }
            }
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![LogicalTypeHandle::from(LogicalTypeId::Blob)],
            LogicalTypeHandle::from(LogicalTypeId::Varchar),
        )]
    }
}

// ---- (blob, blob) -> blob ----
fn register_blob_blob_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobToBlobScalar>(sql_name, &def)
}
struct BlobBlobToBlobScalar;
impl VScalar for BlobBlobToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let mut a = r0[i];
            let mut b = r1[i];
            let ab: Vec<u8> = DuckString::new(&mut a).as_bytes().to_vec();
            let bb: Vec<u8> = DuckString::new(&mut b).as_bytes().to_vec();
            let r = state
                .execute(&[FunctionValue::Binary(ab), FunctionValue::Binary(bb)])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (blob, blob) -> f64 ----
fn register_blob_blob_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobToF64Scalar>(sql_name, &def)
}
struct BlobBlobToF64Scalar;
impl VScalar for BlobBlobToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut a = r0[i];
                let mut b = r1[i];
                let ab: Vec<u8> = DuckString::new(&mut a).as_bytes().to_vec();
                let bb: Vec<u8> = DuckString::new(&mut b).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(ab), FunctionValue::Binary(bb)])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(f) => f,
                    FunctionValue::Float32(f) => f as f64,
                    FunctionValue::Int32(i) => i as f64,
                    FunctionValue::Int64(i) => i as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "BlobBlobToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (blob) -> u32 ----
fn register_blob_to_u32(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobToU32Scalar>(sql_name, &def)
}
struct BlobToU32Scalar;
impl VScalar for BlobToU32Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let raw = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<u32>(n) };
            for i in 0..n {
                if propagates_null && v0.row_is_null(i as u64) {
                    nulls.push(i);
                    continue;
                }
                let mut s_raw = raw[i];
                let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes)])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::UInt32(u) => u,
                    FunctionValue::UInt64(u) => u as u32,
                    FunctionValue::Int32(i) => i as u32,
                    FunctionValue::Int64(i) => i as u32,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobToU32Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![LogicalTypeHandle::from(LogicalTypeId::Blob)],
            LogicalTypeHandle::from(LogicalTypeId::UInteger),
        )]
    }
}

// ---- (blob) -> i32 ----
fn register_blob_to_i32(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobToI32Scalar>(sql_name, &def)
}
struct BlobToI32Scalar;
impl VScalar for BlobToI32Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let raw = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i32>(n) };
            for i in 0..n {
                if propagates_null && v0.row_is_null(i as u64) {
                    nulls.push(i);
                    continue;
                }
                let mut s_raw = raw[i];
                let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes)])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int32(v) => v,
                    FunctionValue::Int64(v) => v as i32,
                    FunctionValue::UInt32(v) => v as i32,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobToI32Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![LogicalTypeHandle::from(LogicalTypeId::Blob)],
            LogicalTypeHandle::from(LogicalTypeId::Integer),
        )]
    }
}

// ---- (blob, u32) -> blob ----
fn register_blob_u32_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32ToBlobScalar>(sql_name, &def)
}
struct BlobU32ToBlobScalar;
impl VScalar for BlobU32ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let mut s_raw = r0[i];
            let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
            let r = state
                .execute(&[FunctionValue::Binary(bytes), FunctionValue::UInt32(r1[i])])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (blob, i32) -> blob ----
fn register_blob_i32_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI32ToBlobScalar>(sql_name, &def)
}
struct BlobI32ToBlobScalar;
impl VScalar for BlobI32ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let mut s_raw = r0[i];
            let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
            let r = state
                .execute(&[FunctionValue::Binary(bytes), FunctionValue::Int32(r1[i])])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (blob, blob, f64) -> bool ----
fn register_blob_blob_f64_to_bool(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobF64ToBoolScalar>(sql_name, &def)
}
struct BlobBlobF64ToBoolScalar;
impl VScalar for BlobBlobF64ToBoolScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<bool>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut a = r0[i];
                let mut b = r1[i];
                let ab: Vec<u8> = DuckString::new(&mut a).as_bytes().to_vec();
                let bb: Vec<u8> = DuckString::new(&mut b).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(ab),
                        FunctionValue::Binary(bb),
                        FunctionValue::Float64(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Boolean(b) => b,
                    FunctionValue::Int8(v) => v != 0,
                    FunctionValue::Int32(v) => v != 0,
                    FunctionValue::Int64(v) => v != 0,
                    FunctionValue::Null => {
                        nulls.push(i);
                        false
                    }
                    other => {
                        return Err(format!(
                            "BlobBlobF64ToBoolScalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Boolean),
        )]
    }
}

// ---- (blob, text) -> blob ----
fn register_blob_text_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobTextToBlobScalar>(sql_name, &def)
}
struct BlobTextToBlobScalar;
impl VScalar for BlobTextToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let mut a = r0[i];
            let mut b = r1[i];
            let bytes: Vec<u8> = DuckString::new(&mut a).as_bytes().to_vec();
            // Second arg is VARCHAR — read as String via as_str.
            let text: String = DuckString::new(&mut b).as_str().into_owned();
            let r = state
                .execute(&[FunctionValue::Binary(bytes), FunctionValue::String(text)])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (blob) -> bool ----
fn register_blob_to_bool(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobToBoolScalar>(sql_name, &def)
}
struct BlobToBoolScalar;
impl VScalar for BlobToBoolScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let raw = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<bool>(n) };
            for i in 0..n {
                if propagates_null && v0.row_is_null(i as u64) {
                    nulls.push(i);
                    continue;
                }
                let mut s_raw = raw[i];
                let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes)])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Boolean(b) => b,
                    FunctionValue::Int8(v) => v != 0,
                    FunctionValue::Int32(v) => v != 0,
                    FunctionValue::Int64(v) => v != 0,
                    FunctionValue::Null => {
                        nulls.push(i);
                        false
                    }
                    other => {
                        return Err(format!(
                            "BlobToBoolScalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![LogicalTypeHandle::from(LogicalTypeId::Blob)],
            LogicalTypeHandle::from(LogicalTypeId::Boolean),
        )]
    }
}

// ---- (blob, f64, f64) -> blob ----
fn register_blob_f64_f64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64F64ToBlobScalar>(sql_name, &def)
}
struct BlobF64F64ToBlobScalar;
impl VScalar for BlobF64F64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_raw = r0[i];
            let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Float64(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (blob, u32) -> text ----
fn register_blob_u32_to_text(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32ToTextScalar>(sql_name, &def)
}
struct BlobU32ToTextScalar;
impl VScalar for BlobU32ToTextScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let mut s_raw = r0[i];
            let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
            let r = state
                .execute(&[FunctionValue::Binary(bytes), FunctionValue::UInt32(r1[i])])
                .map_err(|e| format!("{e:?}"))?;
            match r {
                FunctionValue::String(s) => out.insert(i, s.as_str()),
                FunctionValue::Binary(b) => out.insert(i, b.as_slice()),
                FunctionValue::Null => out.set_null(i),
                other => {
                    return Err(format!(
                        "BlobU32ToTextScalar: unexpected variant `{}`",
                        other.type_name()
                    )
                    .into())
                }
            }
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Varchar),
        )]
    }
}

// ---- (blob, u32, u32) -> blob ----
fn register_blob_u32_u32_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32U32ToBlobScalar>(sql_name, &def)
}
struct BlobU32U32ToBlobScalar;
impl VScalar for BlobU32U32ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<u32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_raw = r0[i];
            let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes),
                    FunctionValue::UInt32(r1[i]),
                    FunctionValue::UInt32(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (blob, f64, f64, f64, f64) -> blob ----  e.g. ST_MakeEnvelope
fn register_blob_4f64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<Blob4F64ToBlobScalar>(sql_name, &def)
}
struct Blob4F64ToBlobScalar;
impl VScalar for Blob4F64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let v4 = input.flat_vector(4);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<f64>(n) };
        let r4 = unsafe { v4.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64)
                    || v4.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_raw = r0[i];
            let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Float64(r2[i]),
                    FunctionValue::Float64(r3[i]),
                    FunctionValue::Float64(r4[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (text) -> text ----
fn register_text_to_text(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<TextToTextScalar>(sql_name, &def)
}
struct TextToTextScalar;
impl VScalar for TextToTextScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let raw = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && v0.row_is_null(i as u64) {
                out.set_null(i);
                continue;
            }
            let mut s_raw = raw[i];
            let s: String = DuckString::new(&mut s_raw).as_str().into_owned();
            let r = state
                .execute(&[FunctionValue::String(s)])
                .map_err(|e| format!("{e:?}"))?;
            match r {
                FunctionValue::String(s) => out.insert(i, s.as_str()),
                FunctionValue::Binary(b) => out.insert(i, b.as_slice()),
                FunctionValue::Null => out.set_null(i),
                other => {
                    return Err(format!(
                        "TextToTextScalar: unexpected variant `{}`",
                        other.type_name()
                    )
                    .into())
                }
            }
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![LogicalTypeHandle::from(LogicalTypeId::Varchar)],
            LogicalTypeHandle::from(LogicalTypeId::Varchar),
        )]
    }
}

// ---- shared blob-output writer ----
fn insert_blob_result<S>(
    out: &mut duckdb::core::FlatVector<'_>,
    idx: usize,
    r: FunctionValue,
) -> std::result::Result<(), Box<dyn std::error::Error>> {
    match r {
        FunctionValue::Binary(b) => out.insert(idx, b.as_slice()),
        FunctionValue::String(s) => out.insert(idx, s.as_bytes()),
        FunctionValue::Null => out.set_null(idx),
        other => {
            return Err(format!(
                "{}: unexpected blob-output variant `{}`",
                std::any::type_name::<S>().split("::").last().unwrap_or("?"),
                other.type_name()
            )
            .into())
        }
    }
    Ok(())
}

// ---- Phase 4e helpers — mobilitydb-driven shapes ----

// ---- (blob) -> i64 ----
fn register_blob_to_i64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobToI64Scalar>(sql_name, &def)
}
struct BlobToI64Scalar;
impl VScalar for BlobToI64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let raw = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i64>(n) };
            for i in 0..n {
                if propagates_null && v0.row_is_null(i as u64) {
                    nulls.push(i);
                    continue;
                }
                let mut s_raw = raw[i];
                let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes)])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int64(v) => v,
                    FunctionValue::Int32(v) => v as i64,
                    FunctionValue::UInt64(v) => v as i64,
                    FunctionValue::UInt32(v) => v as i64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobToI64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![LogicalTypeHandle::from(LogicalTypeId::Blob)],
            LogicalTypeHandle::from(LogicalTypeId::Bigint),
        )]
    }
}

// ---- (blob) -> u64 ----
fn register_blob_to_u64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobToU64Scalar>(sql_name, &def)
}
struct BlobToU64Scalar;
impl VScalar for BlobToU64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let raw = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<u64>(n) };
            for i in 0..n {
                if propagates_null && v0.row_is_null(i as u64) {
                    nulls.push(i);
                    continue;
                }
                let mut s_raw = raw[i];
                let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes)])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::UInt64(v) => v,
                    FunctionValue::UInt32(v) => v as u64,
                    FunctionValue::Int64(v) => v as u64,
                    FunctionValue::Int32(v) => v as u64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobToU64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![LogicalTypeHandle::from(LogicalTypeId::Blob)],
            LogicalTypeHandle::from(LogicalTypeId::UBigint),
        )]
    }
}

// ---- (blob, i64) -> blob ----
fn register_blob_i64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI64ToBlobScalar>(sql_name, &def)
}
struct BlobI64ToBlobScalar;
impl VScalar for BlobI64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let mut s_raw = r0[i];
            let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
            let r = state
                .execute(&[FunctionValue::Binary(bytes), FunctionValue::Int64(r1[i])])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (blob, i64) -> bool ----
fn register_blob_i64_to_bool(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI64ToBoolScalar>(sql_name, &def)
}
struct BlobI64ToBoolScalar;
impl VScalar for BlobI64ToBoolScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<bool>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s_raw = r0[i];
                let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes), FunctionValue::Int64(r1[i])])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Boolean(b) => b,
                    FunctionValue::Null => {
                        nulls.push(i);
                        false
                    }
                    other => {
                        return Err(format!(
                            "BlobI64ToBoolScalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Boolean),
        )]
    }
}

// ---- (blob, i64) -> f64 ----
fn register_blob_i64_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI64ToF64Scalar>(sql_name, &def)
}
struct BlobI64ToF64Scalar;
impl VScalar for BlobI64ToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s_raw = r0[i];
                let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes), FunctionValue::Int64(r1[i])])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "BlobI64ToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (blob, f64) -> bool ----
fn register_blob_f64_to_bool(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64ToBoolScalar>(sql_name, &def)
}
struct BlobF64ToBoolScalar;
impl VScalar for BlobF64ToBoolScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<bool>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s_raw = r0[i];
                let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes), FunctionValue::Float64(r1[i])])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Boolean(b) => b,
                    FunctionValue::Null => {
                        nulls.push(i);
                        false
                    }
                    other => {
                        return Err(format!(
                            "BlobF64ToBoolScalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Boolean),
        )]
    }
}

// ---- (blob, blob) -> i64 ----
fn register_blob_blob_to_i64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobToI64Scalar>(sql_name, &def)
}
struct BlobBlobToI64Scalar;
impl VScalar for BlobBlobToI64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i64>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s0 = r0[i];
                let mut s1 = r1[i];
                let b0: Vec<u8> = DuckString::new(&mut s0).as_bytes().to_vec();
                let b1: Vec<u8> = DuckString::new(&mut s1).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(b0), FunctionValue::Binary(b1)])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int64(v) => v,
                    FunctionValue::Int32(v) => v as i64,
                    FunctionValue::UInt64(v) => v as i64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobBlobToI64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Bigint),
        )]
    }
}

// ---- (blob, i64, i64) -> blob ----
fn register_blob_i64_i64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI64I64ToBlobScalar>(sql_name, &def)
}
struct BlobI64I64ToBlobScalar;
impl VScalar for BlobI64I64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_raw = r0[i];
            let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes),
                    FunctionValue::Int64(r1[i]),
                    FunctionValue::Int64(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (f64, f64, f64, f64, i64, i64) -> blob ----
//
// stbox_make-style constructor: 4 spatial bounds + 2 time
// bounds, no leading geometry blob.
fn register_4f64_2i64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<F4I2ToBlobScalar>(sql_name, &def)
}
struct F4I2ToBlobScalar;
impl VScalar for F4I2ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let v4 = input.flat_vector(4);
        let v5 = input.flat_vector(5);
        let r0 = unsafe { v0.as_slice_with_len::<f64>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<f64>(n) };
        let r4 = unsafe { v4.as_slice_with_len::<i64>(n) };
        let r5 = unsafe { v5.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64)
                    || v4.row_is_null(i as u64)
                    || v5.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let r = state
                .execute(&[
                    FunctionValue::Float64(r0[i]),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Float64(r2[i]),
                    FunctionValue::Float64(r3[i]),
                    FunctionValue::Int64(r4[i]),
                    FunctionValue::Int64(r5[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- Phase 4f helpers — last long-tail mobilitydb shapes ----

// ---- (blob, f64, f64) -> bool ----
fn register_blob_f64_f64_to_bool(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64F64ToBoolScalar>(sql_name, &def)
}
struct BlobF64F64ToBoolScalar;
impl VScalar for BlobF64F64ToBoolScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<bool>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_raw = r0[i];
                let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes),
                        FunctionValue::Float64(r1[i]),
                        FunctionValue::Float64(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Boolean(b) => b,
                    FunctionValue::Null => {
                        nulls.push(i);
                        false
                    }
                    other => {
                        return Err(format!(
                            "BlobF64F64ToBoolScalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Boolean),
        )]
    }
}

// ---- (blob, i64, i64) -> i32 ----
fn register_blob_i64_i64_to_i32(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI64I64ToI32Scalar>(sql_name, &def)
}
struct BlobI64I64ToI32Scalar;
impl VScalar for BlobI64I64ToI32Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i32>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_raw = r0[i];
                let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes),
                        FunctionValue::Int64(r1[i]),
                        FunctionValue::Int64(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int32(v) => v,
                    FunctionValue::Int64(v) => v as i32,
                    FunctionValue::UInt32(v) => v as i32,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobI64I64ToI32Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Integer),
        )]
    }
}

// ---- (blob, f64, i64) -> i32 ----
fn register_blob_f64_i64_to_i32(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64I64ToI32Scalar>(sql_name, &def)
}
struct BlobF64I64ToI32Scalar;
impl VScalar for BlobF64I64ToI32Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i32>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_raw = r0[i];
                let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes),
                        FunctionValue::Float64(r1[i]),
                        FunctionValue::Int64(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int32(v) => v,
                    FunctionValue::Int64(v) => v as i32,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobF64I64ToI32Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Integer),
        )]
    }
}

// ---- (i64, i64, bool, bool) -> blob ----
// tstzspan_make-style constructor: two timestamp bounds + two
// inclusivity flags.
fn register_2i64_2bool_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<I64I64BoolBoolToBlobScalar>(sql_name, &def)
}
struct I64I64BoolBoolToBlobScalar;
impl VScalar for I64I64BoolBoolToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<i64>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<bool>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<bool>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let r = state
                .execute(&[
                    FunctionValue::Int64(r0[i]),
                    FunctionValue::Int64(r1[i]),
                    FunctionValue::Boolean(r2[i]),
                    FunctionValue::Boolean(r3[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
                LogicalTypeHandle::from(LogicalTypeId::Boolean),
                LogicalTypeHandle::from(LogicalTypeId::Boolean),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (i32, i32, bool, bool) -> blob ----
// datespan_make-style constructor.
fn register_2i32_2bool_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<I32I32BoolBoolToBlobScalar>(sql_name, &def)
}
struct I32I32BoolBoolToBlobScalar;
impl VScalar for I32I32BoolBoolToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<i32>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<bool>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<bool>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let r = state
                .execute(&[
                    FunctionValue::Int32(r0[i]),
                    FunctionValue::Int32(r1[i]),
                    FunctionValue::Boolean(r2[i]),
                    FunctionValue::Boolean(r3[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Integer),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
                LogicalTypeHandle::from(LogicalTypeId::Boolean),
                LogicalTypeHandle::from(LogicalTypeId::Boolean),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (blob, u32) -> i64 ----
fn register_blob_u32_to_i64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32ToI64Scalar>(sql_name, &def)
}
struct BlobU32ToI64Scalar;
impl VScalar for BlobU32ToI64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i64>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s_raw = r0[i];
                let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes), FunctionValue::UInt32(r1[i])])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int64(v) => v,
                    FunctionValue::Int32(v) => v as i64,
                    FunctionValue::UInt64(v) => v as i64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobU32ToI64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Bigint),
        )]
    }
}

// ---- (blob, blob, u32) -> blob ----
fn register_blob_blob_u32_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobU32ToBlobScalar>(sql_name, &def)
}
struct BlobBlobU32ToBlobScalar;
impl VScalar for BlobBlobU32ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<u32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s0 = r0[i];
            let mut s1 = r1[i];
            let b0: Vec<u8> = DuckString::new(&mut s0).as_bytes().to_vec();
            let b1: Vec<u8> = DuckString::new(&mut s1).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(b0),
                    FunctionValue::Binary(b1),
                    FunctionValue::UInt32(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- Phase 4g helpers — ttext-driven shapes ----

// ---- (blob, text) -> bool ----
fn register_blob_text_to_bool(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobTextToBoolScalar>(sql_name, &def)
}
struct BlobTextToBoolScalar;
impl VScalar for BlobTextToBoolScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<bool>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s0 = r0[i];
                let mut s1 = r1[i];
                let bytes: Vec<u8> = DuckString::new(&mut s0).as_bytes().to_vec();
                let s: String = DuckString::new(&mut s1).as_str().to_string();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes), FunctionValue::String(s)])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Boolean(b) => b,
                    FunctionValue::Null => {
                        nulls.push(i);
                        false
                    }
                    other => {
                        return Err(format!(
                            "BlobTextToBoolScalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Boolean),
        )]
    }
}

// ---- (blob, blob) -> text ----
fn register_blob_blob_to_text(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobToTextScalar>(sql_name, &def)
}
struct BlobBlobToTextScalar;
impl VScalar for BlobBlobToTextScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let mut s0 = r0[i];
            let mut s1 = r1[i];
            let b0: Vec<u8> = DuckString::new(&mut s0).as_bytes().to_vec();
            let b1: Vec<u8> = DuckString::new(&mut s1).as_bytes().to_vec();
            let r = state
                .execute(&[FunctionValue::Binary(b0), FunctionValue::Binary(b1)])
                .map_err(|e| format!("{e:?}"))?;
            match r {
                FunctionValue::String(s) => out.insert(i, s.as_str()),
                FunctionValue::Binary(b) => out.insert(i, b.as_slice()),
                FunctionValue::Null => out.set_null(i),
                other => {
                    return Err(format!(
                        "BlobBlobToTextScalar: unexpected variant `{}`",
                        other.type_name()
                    )
                    .into())
                }
            }
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Varchar),
        )]
    }
}

// ---- (blob, i64) -> text ----
fn register_blob_i64_to_text(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI64ToTextScalar>(sql_name, &def)
}
struct BlobI64ToTextScalar;
impl VScalar for BlobI64ToTextScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let mut s_raw = r0[i];
            let bytes: Vec<u8> = DuckString::new(&mut s_raw).as_bytes().to_vec();
            let r = state
                .execute(&[FunctionValue::Binary(bytes), FunctionValue::Int64(r1[i])])
                .map_err(|e| format!("{e:?}"))?;
            match r {
                FunctionValue::String(s) => out.insert(i, s.as_str()),
                FunctionValue::Binary(b) => out.insert(i, b.as_slice()),
                FunctionValue::Null => out.set_null(i),
                other => {
                    return Err(format!(
                        "BlobI64ToTextScalar: unexpected variant `{}`",
                        other.type_name()
                    )
                    .into())
                }
            }
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Varchar),
        )]
    }
}

// ---- (blob, text, text) -> blob ----
fn register_blob_text_text_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobTextTextToBlobScalar>(sql_name, &def)
}
struct BlobTextTextToBlobScalar;
impl VScalar for BlobTextTextToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s0 = r0[i];
            let mut s1 = r1[i];
            let mut s2 = r2[i];
            let bytes: Vec<u8> = DuckString::new(&mut s0).as_bytes().to_vec();
            let t1: String = DuckString::new(&mut s1).as_str().to_string();
            let t2: String = DuckString::new(&mut s2).as_str().to_string();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes),
                    FunctionValue::String(t1),
                    FunctionValue::String(t2),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (text, blob) -> blob ----
// Reversed-arg constructors (e.g. ttext-prepend-str where the
// first arg is the prefix string and the second is the
// sequence).
fn register_text_blob_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<TextBlobToBlobScalar>(sql_name, &def)
}
struct TextBlobToBlobScalar;
impl VScalar for TextBlobToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let mut s0 = r0[i];
            let mut s1 = r1[i];
            let t: String = DuckString::new(&mut s0).as_str().to_string();
            let bytes: Vec<u8> = DuckString::new(&mut s1).as_bytes().to_vec();
            let r = state
                .execute(&[FunctionValue::String(t), FunctionValue::Binary(bytes)])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- Phase 4h helpers — complete-coverage tail ----

// ---- (binary, binary) -> uint32 ----
fn register_blob_blob_to_u32(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobToU32Scalar>(sql_name, &def)
}
struct BlobBlobToU32Scalar;
impl VScalar for BlobBlobToU32Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<u32>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let mut s_1 = r1[i];
                let bytes_1: Vec<u8> = DuckString::new(&mut s_1).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Binary(bytes_1),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::UInt32(v) => v,
                    FunctionValue::UInt64(v) => v as u32,
                    FunctionValue::Int32(v) => v as u32,
                    FunctionValue::Int64(v) => v as u32,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobBlobToU32Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::UInteger),
        )]
    }
}

// ---- (binary, binary, float64) -> float64 ----
fn register_blob_blob_f64_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobF64ToF64Scalar>(sql_name, &def)
}
struct BlobBlobF64ToF64Scalar;
impl VScalar for BlobBlobF64ToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let mut s_1 = r1[i];
                let bytes_1: Vec<u8> = DuckString::new(&mut s_1).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Binary(bytes_1),
                        FunctionValue::Float64(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Int32(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "BlobBlobF64ToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (binary, binary, float64, float64) -> float64 ----
fn register_blob_blob_f64_f64_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobF64F64ToF64Scalar>(sql_name, &def)
}
struct BlobBlobF64F64ToF64Scalar;
impl VScalar for BlobBlobF64F64ToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64)
                        || v3.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let mut s_1 = r1[i];
                let bytes_1: Vec<u8> = DuckString::new(&mut s_1).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Binary(bytes_1),
                        FunctionValue::Float64(r2[i]),
                        FunctionValue::Float64(r3[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Int32(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "BlobBlobF64F64ToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (binary, binary, float64, uint32) -> float64 ----
fn register_blob_blob_f64_u32_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobF64U32ToF64Scalar>(sql_name, &def)
}
struct BlobBlobF64U32ToF64Scalar;
impl VScalar for BlobBlobF64U32ToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<u32>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64)
                        || v3.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let mut s_1 = r1[i];
                let bytes_1: Vec<u8> = DuckString::new(&mut s_1).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Binary(bytes_1),
                        FunctionValue::Float64(r2[i]),
                        FunctionValue::UInt32(r3[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Int32(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "BlobBlobF64U32ToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (binary, binary, int64) -> float64 ----
fn register_blob_blob_i64_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobI64ToF64Scalar>(sql_name, &def)
}
struct BlobBlobI64ToF64Scalar;
impl VScalar for BlobBlobI64ToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let mut s_1 = r1[i];
                let bytes_1: Vec<u8> = DuckString::new(&mut s_1).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Binary(bytes_1),
                        FunctionValue::Int64(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Int32(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "BlobBlobI64ToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (binary, boolean) -> binary ----
fn register_blob_bool_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBoolToBlobScalar>(sql_name, &def)
}
struct BlobBoolToBlobScalar;
impl VScalar for BlobBoolToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<bool>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::Boolean(r1[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Boolean),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, float64) -> int32 ----
fn register_blob_f64_to_i32(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64ToI32Scalar>(sql_name, &def)
}
struct BlobF64ToI32Scalar;
impl VScalar for BlobF64ToI32Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i32>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Float64(r1[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int32(v) => v,
                    FunctionValue::Int64(v) => v as i32,
                    FunctionValue::UInt32(v) => v as i32,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobF64ToI32Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Integer),
        )]
    }
}

// ---- (binary, float64, float64) -> float64 ----
fn register_blob_f64_f64_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64F64ToF64Scalar>(sql_name, &def)
}
struct BlobF64F64ToF64Scalar;
impl VScalar for BlobF64F64ToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Float64(r1[i]),
                        FunctionValue::Float64(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Int32(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "BlobF64F64ToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (binary, float64, float64, float64) -> binary ----
fn register_blob_f64_f64_f64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64F64F64ToBlobScalar>(sql_name, &def)
}
struct BlobF64F64F64ToBlobScalar;
impl VScalar for BlobF64F64F64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Float64(r2[i]),
                    FunctionValue::Float64(r3[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, float64, float64, float64) -> boolean ----
fn register_blob_f64_f64_f64_to_bool(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64F64F64ToBoolScalar>(sql_name, &def)
}
struct BlobF64F64F64ToBoolScalar;
impl VScalar for BlobF64F64F64ToBoolScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<bool>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64)
                        || v3.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Float64(r1[i]),
                        FunctionValue::Float64(r2[i]),
                        FunctionValue::Float64(r3[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Boolean(v) => v,
                    FunctionValue::Null => {
                        nulls.push(i);
                        false
                    }
                    other => {
                        return Err(format!(
                            "BlobF64F64F64ToBoolScalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Boolean),
        )]
    }
}

// ---- (binary, float64, float64, float64, int64) -> float64 ----
fn register_blob_f64_f64_f64_i64_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64F64F64I64ToF64Scalar>(sql_name, &def)
}
struct BlobF64F64F64I64ToF64Scalar;
impl VScalar for BlobF64F64F64I64ToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let v4 = input.flat_vector(4);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<f64>(n) };
        let r4 = unsafe { v4.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64)
                        || v3.row_is_null(i as u64)
                        || v4.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Float64(r1[i]),
                        FunctionValue::Float64(r2[i]),
                        FunctionValue::Float64(r3[i]),
                        FunctionValue::Int64(r4[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Int32(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "BlobF64F64F64I64ToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (binary, float64, float64, int64) -> binary ----
fn register_blob_f64_f64_i64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64F64I64ToBlobScalar>(sql_name, &def)
}
struct BlobF64F64I64ToBlobScalar;
impl VScalar for BlobF64F64I64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Float64(r2[i]),
                    FunctionValue::Int64(r3[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, float64, float64, int64) -> boolean ----
fn register_blob_f64_f64_i64_to_bool(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64F64I64ToBoolScalar>(sql_name, &def)
}
struct BlobF64F64I64ToBoolScalar;
impl VScalar for BlobF64F64I64ToBoolScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<bool>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64)
                        || v3.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Float64(r1[i]),
                        FunctionValue::Float64(r2[i]),
                        FunctionValue::Int64(r3[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Boolean(v) => v,
                    FunctionValue::Null => {
                        nulls.push(i);
                        false
                    }
                    other => {
                        return Err(format!(
                            "BlobF64F64I64ToBoolScalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Boolean),
        )]
    }
}

// ---- (binary, float64, int64) -> binary ----
fn register_blob_f64_i64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64I64ToBlobScalar>(sql_name, &def)
}
struct BlobF64I64ToBlobScalar;
impl VScalar for BlobF64I64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Int64(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, float64, int64) -> int64 ----
fn register_blob_f64_i64_to_i64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64I64ToI64Scalar>(sql_name, &def)
}
struct BlobF64I64ToI64Scalar;
impl VScalar for BlobF64I64ToI64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i64>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Float64(r1[i]),
                        FunctionValue::Int64(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int64(v) => v,
                    FunctionValue::Int32(v) => v as i64,
                    FunctionValue::UInt32(v) => v as i64,
                    FunctionValue::UInt64(v) => v as i64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobF64I64ToI64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Bigint),
        )]
    }
}

// ---- (binary, float64, uint32) -> int32 ----
fn register_blob_f64_u32_to_i32(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64U32ToI32Scalar>(sql_name, &def)
}
struct BlobF64U32ToI32Scalar;
impl VScalar for BlobF64U32ToI32Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<u32>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i32>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Float64(r1[i]),
                        FunctionValue::UInt32(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int32(v) => v,
                    FunctionValue::Int64(v) => v as i32,
                    FunctionValue::UInt32(v) => v as i32,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobF64U32ToI32Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Integer),
        )]
    }
}

// ---- (binary, int32) -> boolean ----
fn register_blob_i32_to_bool(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI32ToBoolScalar>(sql_name, &def)
}
struct BlobI32ToBoolScalar;
impl VScalar for BlobI32ToBoolScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i32>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<bool>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes_0), FunctionValue::Int32(r1[i])])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Boolean(v) => v,
                    FunctionValue::Null => {
                        nulls.push(i);
                        false
                    }
                    other => {
                        return Err(format!(
                            "BlobI32ToBoolScalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Boolean),
        )]
    }
}

// ---- (binary, int64) -> int32 ----
fn register_blob_i64_to_i32(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI64ToI32Scalar>(sql_name, &def)
}
struct BlobI64ToI32Scalar;
impl VScalar for BlobI64ToI32Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i32>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes_0), FunctionValue::Int64(r1[i])])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int32(v) => v,
                    FunctionValue::Int64(v) => v as i32,
                    FunctionValue::UInt32(v) => v as i32,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobI64ToI32Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Integer),
        )]
    }
}

// ---- (binary, int64) -> int64 ----
fn register_blob_i64_to_i64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI64ToI64Scalar>(sql_name, &def)
}
struct BlobI64ToI64Scalar;
impl VScalar for BlobI64ToI64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i64>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes_0), FunctionValue::Int64(r1[i])])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int64(v) => v,
                    FunctionValue::Int32(v) => v as i64,
                    FunctionValue::UInt32(v) => v as i64,
                    FunctionValue::UInt64(v) => v as i64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobI64ToI64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Bigint),
        )]
    }
}

// ---- (binary, int64, int64) -> boolean ----
fn register_blob_i64_i64_to_bool(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI64I64ToBoolScalar>(sql_name, &def)
}
struct BlobI64I64ToBoolScalar;
impl VScalar for BlobI64I64ToBoolScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<bool>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Int64(r1[i]),
                        FunctionValue::Int64(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Boolean(v) => v,
                    FunctionValue::Null => {
                        nulls.push(i);
                        false
                    }
                    other => {
                        return Err(format!(
                            "BlobI64I64ToBoolScalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Boolean),
        )]
    }
}

// ---- (binary, int64, int64) -> float64 ----
fn register_blob_i64_i64_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI64I64ToF64Scalar>(sql_name, &def)
}
struct BlobI64I64ToF64Scalar;
impl VScalar for BlobI64I64ToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Int64(r1[i]),
                        FunctionValue::Int64(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Int32(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "BlobI64I64ToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (binary, int64, int64) -> int64 ----
fn register_blob_i64_i64_to_i64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI64I64ToI64Scalar>(sql_name, &def)
}
struct BlobI64I64ToI64Scalar;
impl VScalar for BlobI64I64ToI64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i64>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Int64(r1[i]),
                        FunctionValue::Int64(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int64(v) => v,
                    FunctionValue::Int32(v) => v as i64,
                    FunctionValue::UInt32(v) => v as i64,
                    FunctionValue::UInt64(v) => v as i64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobI64I64ToI64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Bigint),
        )]
    }
}

// ---- (binary, int64, int64) -> text ----
fn register_blob_i64_i64_to_text(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI64I64ToTextScalar>(sql_name, &def)
}
struct BlobI64I64ToTextScalar;
impl VScalar for BlobI64I64ToTextScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::Int64(r1[i]),
                    FunctionValue::Int64(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            match r {
                FunctionValue::String(s) => out.insert(i, s.as_str()),
                FunctionValue::Binary(b) => out.insert(i, b.as_slice()),
                FunctionValue::Null => out.set_null(i),
                other => {
                    return Err(format!(
                        "BlobI64I64ToTextScalar: unexpected variant `{}`",
                        other.type_name()
                    )
                    .into())
                }
            }
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Varchar),
        )]
    }
}

// ---- (binary, uint32) -> float64 ----
fn register_blob_u32_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32ToF64Scalar>(sql_name, &def)
}
struct BlobU32ToF64Scalar;
impl VScalar for BlobU32ToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes_0), FunctionValue::UInt32(r1[i])])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Int32(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "BlobU32ToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (binary, uint32) -> int32 ----
fn register_blob_u32_to_i32(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32ToI32Scalar>(sql_name, &def)
}
struct BlobU32ToI32Scalar;
impl VScalar for BlobU32ToI32Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i32>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes_0), FunctionValue::UInt32(r1[i])])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int32(v) => v,
                    FunctionValue::Int64(v) => v as i32,
                    FunctionValue::UInt32(v) => v as i32,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobU32ToI32Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Integer),
        )]
    }
}

// ---- (binary, uint32, float64) -> float64 ----
fn register_blob_u32_f64_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32F64ToF64Scalar>(sql_name, &def)
}
struct BlobU32F64ToF64Scalar;
impl VScalar for BlobU32F64ToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::UInt32(r1[i]),
                        FunctionValue::Float64(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Int32(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "BlobU32F64ToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (float64, float64) -> float64 ----
fn register_f64_f64_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<F64F64ToF64Scalar>(sql_name, &def)
}
struct F64F64ToF64Scalar;
impl VScalar for F64F64ToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<f64>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let r = state
                    .execute(&[FunctionValue::Float64(r0[i]), FunctionValue::Float64(r1[i])])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Int32(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "F64F64ToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (float64, float64, boolean, boolean) -> binary ----
fn register_f64_f64_bool_bool_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<F64F64BoolBoolToBlobScalar>(sql_name, &def)
}
struct F64F64BoolBoolToBlobScalar;
impl VScalar for F64F64BoolBoolToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<f64>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<bool>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<bool>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let r = state
                .execute(&[
                    FunctionValue::Float64(r0[i]),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Boolean(r2[i]),
                    FunctionValue::Boolean(r3[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Boolean),
                LogicalTypeHandle::from(LogicalTypeId::Boolean),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (float64, float64, float64, float64) -> binary ----
fn register_f64_f64_f64_f64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<F64F64F64F64ToBlobScalar>(sql_name, &def)
}
struct F64F64F64F64ToBlobScalar;
impl VScalar for F64F64F64F64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<f64>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let r = state
                .execute(&[
                    FunctionValue::Float64(r0[i]),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Float64(r2[i]),
                    FunctionValue::Float64(r3[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (float64, float64, float64, float64) -> float64 ----
fn register_f64_f64_f64_f64_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<F64F64F64F64ToF64Scalar>(sql_name, &def)
}
struct F64F64F64F64ToF64Scalar;
impl VScalar for F64F64F64F64ToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<f64>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64)
                        || v3.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let r = state
                    .execute(&[
                        FunctionValue::Float64(r0[i]),
                        FunctionValue::Float64(r1[i]),
                        FunctionValue::Float64(r2[i]),
                        FunctionValue::Float64(r3[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Int32(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "F64F64F64F64ToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (int64, float64) -> binary ----
fn register_i64_f64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<I64F64ToBlobScalar>(sql_name, &def)
}
struct I64F64ToBlobScalar;
impl VScalar for I64F64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<i64>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let r = state
                .execute(&[FunctionValue::Int64(r0[i]), FunctionValue::Float64(r1[i])])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (int64, float64, float64) -> binary ----
fn register_i64_f64_f64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<I64F64F64ToBlobScalar>(sql_name, &def)
}
struct I64F64F64ToBlobScalar;
impl VScalar for I64F64F64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<i64>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let r = state
                .execute(&[
                    FunctionValue::Int64(r0[i]),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Float64(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (text, int64, int64) -> binary ----
fn register_text_i64_i64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<TextI64I64ToBlobScalar>(sql_name, &def)
}
struct TextI64I64ToBlobScalar;
impl VScalar for TextI64I64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let t_0: String = DuckString::new(&mut s_0).as_str().to_string();
            let r = state
                .execute(&[
                    FunctionValue::String(t_0),
                    FunctionValue::Int64(r1[i]),
                    FunctionValue::Int64(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (text, int64, int64) -> float64 ----
fn register_text_i64_i64_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<TextI64I64ToF64Scalar>(sql_name, &def)
}
struct TextI64I64ToF64Scalar;
impl VScalar for TextI64I64ToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let t_0: String = DuckString::new(&mut s_0).as_str().to_string();
                let r = state
                    .execute(&[
                        FunctionValue::String(t_0),
                        FunctionValue::Int64(r1[i]),
                        FunctionValue::Int64(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Int32(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "TextI64I64ToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
                LogicalTypeHandle::from(LogicalTypeId::Bigint),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (uint64) -> int64 ----
fn register_u64_to_i64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<U64ToI64Scalar>(sql_name, &def)
}
struct U64ToI64Scalar;
impl VScalar for U64ToI64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let r0 = unsafe { v0.as_slice_with_len::<u64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i64>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let r = state
                    .execute(&[FunctionValue::UInt64(r0[i])])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int64(v) => v,
                    FunctionValue::Int32(v) => v as i64,
                    FunctionValue::UInt32(v) => v as i64,
                    FunctionValue::UInt64(v) => v as i64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "U64ToI64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![LogicalTypeHandle::from(LogicalTypeId::UBigint)],
            LogicalTypeHandle::from(LogicalTypeId::Bigint),
        )]
    }
}

// ---- Phase 4i helpers — postgis-driven shapes ----

// ---- (binary, binary) -> int32 ----
fn register_blob_blob_to_i32(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobToI32Scalar>(sql_name, &def)
}
struct BlobBlobToI32Scalar;
impl VScalar for BlobBlobToI32Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i32>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let mut s_1 = r1[i];
                let bytes_1: Vec<u8> = DuckString::new(&mut s_1).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Binary(bytes_1),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int32(v) => v,
                    FunctionValue::Int64(v) => v as i32,
                    FunctionValue::UInt32(v) => v as i32,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobBlobToI32Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Integer),
        )]
    }
}

// ---- (binary, binary, binary) -> float64 ----
fn register_blob_blob_blob_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobBlobToF64Scalar>(sql_name, &def)
}
struct BlobBlobBlobToF64Scalar;
impl VScalar for BlobBlobBlobToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let mut s_1 = r1[i];
                let bytes_1: Vec<u8> = DuckString::new(&mut s_1).as_bytes().to_vec();
                let mut s_2 = r2[i];
                let bytes_2: Vec<u8> = DuckString::new(&mut s_2).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Binary(bytes_1),
                        FunctionValue::Binary(bytes_2),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Int32(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "BlobBlobBlobToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (binary, binary, float64) -> binary ----
fn register_blob_blob_f64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobF64ToBlobScalar>(sql_name, &def)
}
struct BlobBlobF64ToBlobScalar;
impl VScalar for BlobBlobF64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let mut s_1 = r1[i];
            let bytes_1: Vec<u8> = DuckString::new(&mut s_1).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::Binary(bytes_1),
                    FunctionValue::Float64(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, binary, float64) -> uint32 ----
fn register_blob_blob_f64_to_u32(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobBlobF64ToU32Scalar>(sql_name, &def)
}
struct BlobBlobF64ToU32Scalar;
impl VScalar for BlobBlobF64ToU32Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<u32>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let mut s_1 = r1[i];
                let bytes_1: Vec<u8> = DuckString::new(&mut s_1).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Binary(bytes_1),
                        FunctionValue::Float64(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::UInt32(v) => v,
                    FunctionValue::UInt64(v) => v as u32,
                    FunctionValue::Int32(v) => v as u32,
                    FunctionValue::Int64(v) => v as u32,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobBlobF64ToU32Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::UInteger),
        )]
    }
}

// ---- (binary, float64, boolean) -> binary ----
fn register_blob_f64_bool_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64BoolToBlobScalar>(sql_name, &def)
}
struct BlobF64BoolToBlobScalar;
impl VScalar for BlobF64BoolToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<bool>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Boolean(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Boolean),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, float64, float64) -> int32 ----
fn register_blob_f64_f64_to_i32(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64F64ToI32Scalar>(sql_name, &def)
}
struct BlobF64F64ToI32Scalar;
impl VScalar for BlobF64F64ToI32Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<i32>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::Float64(r1[i]),
                        FunctionValue::Float64(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Int32(v) => v,
                    FunctionValue::Int64(v) => v as i32,
                    FunctionValue::UInt32(v) => v as i32,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "BlobF64F64ToI32Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Integer),
        )]
    }
}

// ---- (binary, float64, float64) -> text ----
fn register_blob_f64_f64_to_text(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64F64ToTextScalar>(sql_name, &def)
}
struct BlobF64F64ToTextScalar;
impl VScalar for BlobF64F64ToTextScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Float64(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            match r {
                FunctionValue::String(s) => out.insert(i, s.as_str()),
                FunctionValue::Binary(b) => out.insert(i, b.as_slice()),
                FunctionValue::Null => out.set_null(i),
                other => {
                    return Err(format!(
                        "BlobF64F64ToTextScalar: unexpected variant `{}`",
                        other.type_name()
                    )
                    .into())
                }
            }
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Varchar),
        )]
    }
}

// ---- (binary, float64, float64, float64, float64, float64, float64) -> binary ----
fn register_blob_f64_f64_f64_f64_f64_f64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64F64F64F64F64F64ToBlobScalar>(sql_name, &def)
}
struct BlobF64F64F64F64F64F64ToBlobScalar;
impl VScalar for BlobF64F64F64F64F64F64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let v4 = input.flat_vector(4);
        let v5 = input.flat_vector(5);
        let v6 = input.flat_vector(6);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<f64>(n) };
        let r4 = unsafe { v4.as_slice_with_len::<f64>(n) };
        let r5 = unsafe { v5.as_slice_with_len::<f64>(n) };
        let r6 = unsafe { v6.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64)
                    || v4.row_is_null(i as u64)
                    || v5.row_is_null(i as u64)
                    || v6.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Float64(r2[i]),
                    FunctionValue::Float64(r3[i]),
                    FunctionValue::Float64(r4[i]),
                    FunctionValue::Float64(r5[i]),
                    FunctionValue::Float64(r6[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, float64, float64, text) -> binary ----
fn register_blob_f64_f64_text_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64F64TextToBlobScalar>(sql_name, &def)
}
struct BlobF64F64TextToBlobScalar;
impl VScalar for BlobF64F64TextToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let mut s_3 = r3[i];
            let t_3: String = DuckString::new(&mut s_3).as_str().to_string();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Float64(r2[i]),
                    FunctionValue::String(t_3),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, float64, int32) -> binary ----
fn register_blob_f64_i32_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64I32ToBlobScalar>(sql_name, &def)
}
struct BlobF64I32ToBlobScalar;
impl VScalar for BlobF64I32ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Int32(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, float64, int32) -> text ----
fn register_blob_f64_i32_to_text(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobF64I32ToTextScalar>(sql_name, &def)
}
struct BlobF64I32ToTextScalar;
impl VScalar for BlobF64I32ToTextScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Int32(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            match r {
                FunctionValue::String(s) => out.insert(i, s.as_str()),
                FunctionValue::Binary(b) => out.insert(i, b.as_slice()),
                FunctionValue::Null => out.set_null(i),
                other => {
                    return Err(format!(
                        "BlobF64I32ToTextScalar: unexpected variant `{}`",
                        other.type_name()
                    )
                    .into())
                }
            }
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Varchar),
        )]
    }
}

// ---- (binary, int32) -> text ----
fn register_blob_i32_to_text(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI32ToTextScalar>(sql_name, &def)
}
struct BlobI32ToTextScalar;
impl VScalar for BlobI32ToTextScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[FunctionValue::Binary(bytes_0), FunctionValue::Int32(r1[i])])
                .map_err(|e| format!("{e:?}"))?;
            match r {
                FunctionValue::String(s) => out.insert(i, s.as_str()),
                FunctionValue::Binary(b) => out.insert(i, b.as_slice()),
                FunctionValue::Null => out.set_null(i),
                other => {
                    return Err(format!(
                        "BlobI32ToTextScalar: unexpected variant `{}`",
                        other.type_name()
                    )
                    .into())
                }
            }
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Varchar),
        )]
    }
}

// ---- (binary, int32, binary) -> binary ----
fn register_blob_i32_blob_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI32BlobToBlobScalar>(sql_name, &def)
}
struct BlobI32BlobToBlobScalar;
impl VScalar for BlobI32BlobToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let mut s_2 = r2[i];
            let bytes_2: Vec<u8> = DuckString::new(&mut s_2).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::Int32(r1[i]),
                    FunctionValue::Binary(bytes_2),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, int32, binary) -> text ----
fn register_blob_i32_blob_to_text(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI32BlobToTextScalar>(sql_name, &def)
}
struct BlobI32BlobToTextScalar;
impl VScalar for BlobI32BlobToTextScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let mut s_2 = r2[i];
            let bytes_2: Vec<u8> = DuckString::new(&mut s_2).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::Int32(r1[i]),
                    FunctionValue::Binary(bytes_2),
                ])
                .map_err(|e| format!("{e:?}"))?;
            match r {
                FunctionValue::String(s) => out.insert(i, s.as_str()),
                FunctionValue::Binary(b) => out.insert(i, b.as_slice()),
                FunctionValue::Null => out.set_null(i),
                other => {
                    return Err(format!(
                        "BlobI32BlobToTextScalar: unexpected variant `{}`",
                        other.type_name()
                    )
                    .into())
                }
            }
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Varchar),
        )]
    }
}

// ---- (binary, int32, int32) -> binary ----
fn register_blob_i32_i32_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobI32I32ToBlobScalar>(sql_name, &def)
}
struct BlobI32I32ToBlobScalar;
impl VScalar for BlobI32I32ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::Int32(r1[i]),
                    FunctionValue::Int32(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, text, float64) -> binary ----
fn register_blob_text_f64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobTextF64ToBlobScalar>(sql_name, &def)
}
struct BlobTextF64ToBlobScalar;
impl VScalar for BlobTextF64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let mut s_1 = r1[i];
            let t_1: String = DuckString::new(&mut s_1).as_str().to_string();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::String(t_1),
                    FunctionValue::Float64(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, text, int32) -> binary ----
fn register_blob_text_i32_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobTextI32ToBlobScalar>(sql_name, &def)
}
struct BlobTextI32ToBlobScalar;
impl VScalar for BlobTextI32ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let mut s_1 = r1[i];
            let t_1: String = DuckString::new(&mut s_1).as_str().to_string();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::String(t_1),
                    FunctionValue::Int32(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, uint32) -> boolean ----
fn register_blob_u32_to_bool(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32ToBoolScalar>(sql_name, &def)
}
struct BlobU32ToBoolScalar;
impl VScalar for BlobU32ToBoolScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<bool>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[FunctionValue::Binary(bytes_0), FunctionValue::UInt32(r1[i])])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Boolean(v) => v,
                    FunctionValue::Null => {
                        nulls.push(i);
                        false
                    }
                    other => {
                        return Err(format!(
                            "BlobU32ToBoolScalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Boolean),
        )]
    }
}

// ---- (binary, uint32, binary) -> binary ----
fn register_blob_u32_blob_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32BlobToBlobScalar>(sql_name, &def)
}
struct BlobU32BlobToBlobScalar;
impl VScalar for BlobU32BlobToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let mut s_2 = r2[i];
            let bytes_2: Vec<u8> = DuckString::new(&mut s_2).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::UInt32(r1[i]),
                    FunctionValue::Binary(bytes_2),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, uint32, binary) -> text ----
fn register_blob_u32_blob_to_text(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32BlobToTextScalar>(sql_name, &def)
}
struct BlobU32BlobToTextScalar;
impl VScalar for BlobU32BlobToTextScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let mut s_2 = r2[i];
            let bytes_2: Vec<u8> = DuckString::new(&mut s_2).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::UInt32(r1[i]),
                    FunctionValue::Binary(bytes_2),
                ])
                .map_err(|e| format!("{e:?}"))?;
            match r {
                FunctionValue::String(s) => out.insert(i, s.as_str()),
                FunctionValue::Binary(b) => out.insert(i, b.as_slice()),
                FunctionValue::Null => out.set_null(i),
                other => {
                    return Err(format!(
                        "BlobU32BlobToTextScalar: unexpected variant `{}`",
                        other.type_name()
                    )
                    .into())
                }
            }
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Varchar),
        )]
    }
}

// ---- (binary, uint32, binary, uint32, text, text) -> binary ----
fn register_blob_u32_blob_u32_text_text_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32BlobU32TextTextToBlobScalar>(sql_name, &def)
}
struct BlobU32BlobU32TextTextToBlobScalar;
impl VScalar for BlobU32BlobU32TextTextToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let v4 = input.flat_vector(4);
        let v5 = input.flat_vector(5);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<duckdb_string_t>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<u32>(n) };
        let r4 = unsafe { v4.as_slice_with_len::<duckdb_string_t>(n) };
        let r5 = unsafe { v5.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64)
                    || v4.row_is_null(i as u64)
                    || v5.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let mut s_2 = r2[i];
            let bytes_2: Vec<u8> = DuckString::new(&mut s_2).as_bytes().to_vec();
            let mut s_4 = r4[i];
            let t_4: String = DuckString::new(&mut s_4).as_str().to_string();
            let mut s_5 = r5[i];
            let t_5: String = DuckString::new(&mut s_5).as_str().to_string();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::UInt32(r1[i]),
                    FunctionValue::Binary(bytes_2),
                    FunctionValue::UInt32(r3[i]),
                    FunctionValue::String(t_4),
                    FunctionValue::String(t_5),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, uint32, float64) -> text ----
fn register_blob_u32_f64_to_text(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32F64ToTextScalar>(sql_name, &def)
}
struct BlobU32F64ToTextScalar;
impl VScalar for BlobU32F64ToTextScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::UInt32(r1[i]),
                    FunctionValue::Float64(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            match r {
                FunctionValue::String(s) => out.insert(i, s.as_str()),
                FunctionValue::Binary(b) => out.insert(i, b.as_slice()),
                FunctionValue::Null => out.set_null(i),
                other => {
                    return Err(format!(
                        "BlobU32F64ToTextScalar: unexpected variant `{}`",
                        other.type_name()
                    )
                    .into())
                }
            }
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Varchar),
        )]
    }
}

// ---- (binary, uint32, float64, float64) -> binary ----
fn register_blob_u32_f64_f64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32F64F64ToBlobScalar>(sql_name, &def)
}
struct BlobU32F64F64ToBlobScalar;
impl VScalar for BlobU32F64F64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::UInt32(r1[i]),
                    FunctionValue::Float64(r2[i]),
                    FunctionValue::Float64(r3[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, uint32, text) -> text ----
fn register_blob_u32_text_to_text(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32TextToTextScalar>(sql_name, &def)
}
struct BlobU32TextToTextScalar;
impl VScalar for BlobU32TextToTextScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let mut s_2 = r2[i];
            let t_2: String = DuckString::new(&mut s_2).as_str().to_string();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::UInt32(r1[i]),
                    FunctionValue::String(t_2),
                ])
                .map_err(|e| format!("{e:?}"))?;
            match r {
                FunctionValue::String(s) => out.insert(i, s.as_str()),
                FunctionValue::Binary(b) => out.insert(i, b.as_slice()),
                FunctionValue::Null => out.set_null(i),
                other => {
                    return Err(format!(
                        "BlobU32TextToTextScalar: unexpected variant `{}`",
                        other.type_name()
                    )
                    .into())
                }
            }
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Varchar),
        )]
    }
}

// ---- (binary, uint32, text, text) -> binary ----
fn register_blob_u32_text_text_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32TextTextToBlobScalar>(sql_name, &def)
}
struct BlobU32TextTextToBlobScalar;
impl VScalar for BlobU32TextTextToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<duckdb_string_t>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let mut s_2 = r2[i];
            let t_2: String = DuckString::new(&mut s_2).as_str().to_string();
            let mut s_3 = r3[i];
            let t_3: String = DuckString::new(&mut s_3).as_str().to_string();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::UInt32(r1[i]),
                    FunctionValue::String(t_2),
                    FunctionValue::String(t_3),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, uint32, uint32) -> float64 ----
fn register_blob_u32_u32_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32U32ToF64Scalar>(sql_name, &def)
}
struct BlobU32U32ToF64Scalar;
impl VScalar for BlobU32U32ToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<u32>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::UInt32(r1[i]),
                        FunctionValue::UInt32(r2[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Int32(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "BlobU32U32ToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (binary, uint32, uint32) -> text ----
fn register_blob_u32_u32_to_text(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32U32ToTextScalar>(sql_name, &def)
}
struct BlobU32U32ToTextScalar;
impl VScalar for BlobU32U32ToTextScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<u32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::UInt32(r1[i]),
                    FunctionValue::UInt32(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            match r {
                FunctionValue::String(s) => out.insert(i, s.as_str()),
                FunctionValue::Binary(b) => out.insert(i, b.as_slice()),
                FunctionValue::Null => out.set_null(i),
                other => {
                    return Err(format!(
                        "BlobU32U32ToTextScalar: unexpected variant `{}`",
                        other.type_name()
                    )
                    .into())
                }
            }
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Varchar),
        )]
    }
}

// ---- (binary, uint32, uint32, binary) -> binary ----
fn register_blob_u32_u32_blob_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32U32BlobToBlobScalar>(sql_name, &def)
}
struct BlobU32U32BlobToBlobScalar;
impl VScalar for BlobU32U32BlobToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<u32>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let mut s_3 = r3[i];
            let bytes_3: Vec<u8> = DuckString::new(&mut s_3).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::UInt32(r1[i]),
                    FunctionValue::UInt32(r2[i]),
                    FunctionValue::Binary(bytes_3),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, uint32, uint32, binary) -> text ----
fn register_blob_u32_u32_blob_to_text(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32U32BlobToTextScalar>(sql_name, &def)
}
struct BlobU32U32BlobToTextScalar;
impl VScalar for BlobU32U32BlobToTextScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<u32>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let mut s_3 = r3[i];
            let bytes_3: Vec<u8> = DuckString::new(&mut s_3).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::UInt32(r1[i]),
                    FunctionValue::UInt32(r2[i]),
                    FunctionValue::Binary(bytes_3),
                ])
                .map_err(|e| format!("{e:?}"))?;
            match r {
                FunctionValue::String(s) => out.insert(i, s.as_str()),
                FunctionValue::Binary(b) => out.insert(i, b.as_slice()),
                FunctionValue::Null => out.set_null(i),
                other => {
                    return Err(format!(
                        "BlobU32U32BlobToTextScalar: unexpected variant `{}`",
                        other.type_name()
                    )
                    .into())
                }
            }
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Varchar),
        )]
    }
}

// ---- (binary, uint32, uint32, text) -> binary ----
fn register_blob_u32_u32_text_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32U32TextToBlobScalar>(sql_name, &def)
}
struct BlobU32U32TextToBlobScalar;
impl VScalar for BlobU32U32TextToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<u32>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let mut s_3 = r3[i];
            let t_3: String = DuckString::new(&mut s_3).as_str().to_string();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::UInt32(r1[i]),
                    FunctionValue::UInt32(r2[i]),
                    FunctionValue::String(t_3),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, uint32, uint32, uint32) -> float64 ----
fn register_blob_u32_u32_u32_to_f64(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32U32U32ToF64Scalar>(sql_name, &def)
}
struct BlobU32U32U32ToF64Scalar;
impl VScalar for BlobU32U32U32ToF64Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<u32>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<u32>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<f64>(n) };
            for i in 0..n {
                if propagates_null
                    && (v0.row_is_null(i as u64)
                        || v1.row_is_null(i as u64)
                        || v2.row_is_null(i as u64)
                        || v3.row_is_null(i as u64))
                {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
                let r = state
                    .execute(&[
                        FunctionValue::Binary(bytes_0),
                        FunctionValue::UInt32(r1[i]),
                        FunctionValue::UInt32(r2[i]),
                        FunctionValue::UInt32(r3[i]),
                    ])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Float64(v) => v,
                    FunctionValue::Float32(v) => v as f64,
                    FunctionValue::Int64(v) => v as f64,
                    FunctionValue::Int32(v) => v as f64,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0.0
                    }
                    other => {
                        return Err(format!(
                            "BlobU32U32U32ToF64Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Double),
        )]
    }
}

// ---- (binary, uint32, uint32, uint32, binary) -> binary ----
fn register_blob_u32_u32_u32_blob_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32U32U32BlobToBlobScalar>(sql_name, &def)
}
struct BlobU32U32U32BlobToBlobScalar;
impl VScalar for BlobU32U32U32BlobToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let v4 = input.flat_vector(4);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<u32>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<u32>(n) };
        let r4 = unsafe { v4.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64)
                    || v4.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let mut s_4 = r4[i];
            let bytes_4: Vec<u8> = DuckString::new(&mut s_4).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::UInt32(r1[i]),
                    FunctionValue::UInt32(r2[i]),
                    FunctionValue::UInt32(r3[i]),
                    FunctionValue::Binary(bytes_4),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::Blob),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (binary, uint32, uint32, uint32, float64) -> binary ----
fn register_blob_u32_u32_u32_f64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<BlobU32U32U32F64ToBlobScalar>(sql_name, &def)
}
struct BlobU32U32U32F64ToBlobScalar;
impl VScalar for BlobU32U32U32F64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let v4 = input.flat_vector(4);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<u32>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<u32>(n) };
        let r4 = unsafe { v4.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64)
                    || v4.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let bytes_0: Vec<u8> = DuckString::new(&mut s_0).as_bytes().to_vec();
            let r = state
                .execute(&[
                    FunctionValue::Binary(bytes_0),
                    FunctionValue::UInt32(r1[i]),
                    FunctionValue::UInt32(r2[i]),
                    FunctionValue::UInt32(r3[i]),
                    FunctionValue::Float64(r4[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Blob),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (float64, float64) -> binary ----
fn register_f64_f64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<F64F64ToBlobScalar>(sql_name, &def)
}
struct F64F64ToBlobScalar;
impl VScalar for F64F64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<f64>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let r = state
                .execute(&[FunctionValue::Float64(r0[i]), FunctionValue::Float64(r1[i])])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (float64, float64, float64) -> binary ----
fn register_f64_f64_f64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<F64F64F64ToBlobScalar>(sql_name, &def)
}
struct F64F64F64ToBlobScalar;
impl VScalar for F64F64F64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<f64>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let r = state
                .execute(&[
                    FunctionValue::Float64(r0[i]),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Float64(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (float64, float64, float64, float64, int32) -> binary ----
fn register_f64_f64_f64_f64_i32_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<F64F64F64F64I32ToBlobScalar>(sql_name, &def)
}
struct F64F64F64F64I32ToBlobScalar;
impl VScalar for F64F64F64F64I32ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let v3 = input.flat_vector(3);
        let v4 = input.flat_vector(4);
        let r0 = unsafe { v0.as_slice_with_len::<f64>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<f64>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let r3 = unsafe { v3.as_slice_with_len::<f64>(n) };
        let r4 = unsafe { v4.as_slice_with_len::<i32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64)
                    || v3.row_is_null(i as u64)
                    || v4.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let r = state
                .execute(&[
                    FunctionValue::Float64(r0[i]),
                    FunctionValue::Float64(r1[i]),
                    FunctionValue::Float64(r2[i]),
                    FunctionValue::Float64(r3[i]),
                    FunctionValue::Int32(r4[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (float64, int32, int32) -> binary ----
fn register_f64_i32_i32_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<F64I32I32ToBlobScalar>(sql_name, &def)
}
struct F64I32I32ToBlobScalar;
impl VScalar for F64I32I32ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<f64>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let r = state
                .execute(&[
                    FunctionValue::Float64(r0[i]),
                    FunctionValue::Int32(r1[i]),
                    FunctionValue::Int32(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Double),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (int32, int32, int32) -> binary ----
fn register_i32_i32_i32_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<I32I32I32ToBlobScalar>(sql_name, &def)
}
struct I32I32I32ToBlobScalar;
impl VScalar for I32I32I32ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<i32>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<i32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let r = state
                .execute(&[
                    FunctionValue::Int32(r0[i]),
                    FunctionValue::Int32(r1[i]),
                    FunctionValue::Int32(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Integer),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (text) -> uint32 ----
fn register_text_to_u32(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<TextToU32Scalar>(sql_name, &def)
}
struct TextToU32Scalar;
impl VScalar for TextToU32Scalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<u32>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let t_0: String = DuckString::new(&mut s_0).as_str().to_string();
                let r = state
                    .execute(&[FunctionValue::String(t_0)])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::UInt32(v) => v,
                    FunctionValue::UInt64(v) => v as u32,
                    FunctionValue::Int32(v) => v as u32,
                    FunctionValue::Int64(v) => v as u32,
                    FunctionValue::Null => {
                        nulls.push(i);
                        0
                    }
                    other => {
                        return Err(format!(
                            "TextToU32Scalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![LogicalTypeHandle::from(LogicalTypeId::Varchar)],
            LogicalTypeHandle::from(LogicalTypeId::UInteger),
        )]
    }
}

// ---- (text, int32) -> binary ----
fn register_text_i32_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<TextI32ToBlobScalar>(sql_name, &def)
}
struct TextI32ToBlobScalar;
impl VScalar for TextI32ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let t_0: String = DuckString::new(&mut s_0).as_str().to_string();
            let r = state
                .execute(&[FunctionValue::String(t_0), FunctionValue::Int32(r1[i])])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (text, int32, float64) -> binary ----
fn register_text_i32_f64_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<TextI32F64ToBlobScalar>(sql_name, &def)
}
struct TextI32F64ToBlobScalar;
impl VScalar for TextI32F64ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let v2 = input.flat_vector(2);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<i32>(n) };
        let r2 = unsafe { v2.as_slice_with_len::<f64>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null
                && (v0.row_is_null(i as u64)
                    || v1.row_is_null(i as u64)
                    || v2.row_is_null(i as u64))
            {
                out.set_null(i);
                continue;
            }
            let mut s_0 = r0[i];
            let t_0: String = DuckString::new(&mut s_0).as_str().to_string();
            let r = state
                .execute(&[
                    FunctionValue::String(t_0),
                    FunctionValue::Int32(r1[i]),
                    FunctionValue::Float64(r2[i]),
                ])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
                LogicalTypeHandle::from(LogicalTypeId::Integer),
                LogicalTypeHandle::from(LogicalTypeId::Double),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// ---- (text, text) -> boolean ----
fn register_text_text_to_bool(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<TextTextToBoolScalar>(sql_name, &def)
}
struct TextTextToBoolScalar;
impl VScalar for TextTextToBoolScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<duckdb_string_t>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<duckdb_string_t>(n) };
        let mut out = output.flat_vector();
        let mut nulls: Vec<usize> = Vec::new();
        let propagates_null = state.propagates_null();
        {
            let out_slice = unsafe { out.as_mut_slice_with_len::<bool>(n) };
            for i in 0..n {
                if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                    nulls.push(i);
                    continue;
                }
                let mut s_0 = r0[i];
                let t_0: String = DuckString::new(&mut s_0).as_str().to_string();
                let mut s_1 = r1[i];
                let t_1: String = DuckString::new(&mut s_1).as_str().to_string();
                let r = state
                    .execute(&[FunctionValue::String(t_0), FunctionValue::String(t_1)])
                    .map_err(|e| format!("{e:?}"))?;
                out_slice[i] = match r {
                    FunctionValue::Boolean(v) => v,
                    FunctionValue::Null => {
                        nulls.push(i);
                        false
                    }
                    other => {
                        return Err(format!(
                            "TextTextToBoolScalar: unexpected variant `{}`",
                            other.type_name()
                        )
                        .into())
                    }
                };
            }
        }
        for i in nulls {
            out.set_null(i);
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
                LogicalTypeHandle::from(LogicalTypeId::Varchar),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Boolean),
        )]
    }
}

// ---- (uint32, uint32) -> binary ----
fn register_u32_u32_to_blob(conn: &Connection, sql_name: &str) -> Result<()> {
    let def = lookup(sql_name)?;
    conn.register_scalar_function_with_state::<U32U32ToBlobScalar>(sql_name, &def)
}
struct U32U32ToBlobScalar;
impl VScalar for U32U32ToBlobScalar {
    type State = Arc<dyn ScalarFunctionDef>;
    fn invoke(
        state: &Self::State,
        input: &mut DataChunkHandle,
        output: &mut dyn WritableVector,
    ) -> std::result::Result<(), Box<dyn std::error::Error>> {
        let n = input.len();
        let v0 = input.flat_vector(0);
        let v1 = input.flat_vector(1);
        let r0 = unsafe { v0.as_slice_with_len::<u32>(n) };
        let r1 = unsafe { v1.as_slice_with_len::<u32>(n) };
        let mut out = output.flat_vector();
        let propagates_null = state.propagates_null();
        for i in 0..n {
            if propagates_null && (v0.row_is_null(i as u64) || v1.row_is_null(i as u64)) {
                out.set_null(i);
                continue;
            }
            let r = state
                .execute(&[FunctionValue::UInt32(r0[i]), FunctionValue::UInt32(r1[i])])
                .map_err(|e| format!("{e:?}"))?;
            insert_blob_result::<Self>(&mut out, i, r)?;
        }
        Ok(())
    }
    fn signatures() -> Vec<ScalarFunctionSignature> {
        vec![ScalarFunctionSignature::exact(
            vec![
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
                LogicalTypeHandle::from(LogicalTypeId::UInteger),
            ],
            LogicalTypeHandle::from(LogicalTypeId::Blob),
        )]
    }
}

// Deferred shapes (Phase 3 target):
//      3  [binary,float64,binary] -> binary
//      2  [float64,int32,int32,binary] -> binary
//      1  [binary,float64,float64,boolean] -> binary
//      1  [binary,int32,int32,int32,boolean] -> binary
//      1  [binary,float64,text] -> binary
//      1  [binary,int32,int32,int32,boolean,boolean] -> binary
//      1  [binary,binary,binary,binary] -> float64
//      1  [binary,binary,boolean] -> binary
//      1  [binary,binary,int32] -> binary
//      1  [binary,int32,int32,int32] -> binary
//      1  [uint32,uint32,float64,float64,float64,float64,float64,float64,int32] -> binary
//      1  [binary,text,float64,float64] -> binary
//      1  [binary,float64,int32,int32] -> binary
//      1  [binary,binary,boolean] -> text
